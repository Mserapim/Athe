import { Route } from '@angular/router';
import { ClockingComponent } from './clocking.component';

export const ClockingRoute: Route[] = [
    {
        path: 'registro-de-ponto',
        component: ClockingComponent,
    },
];

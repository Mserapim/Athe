/** autogenerated **/
/**
 *
 **/
 Ext._define('common.services.scheduled_services.Restful', {
    extend: 'core.Restful',

    resource: 'SvcScheduledServicesRestful',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = common.services.scheduled_services.Restful.superclass.getFields.call(this, cfg).concat([
                {type: "string",  name: "name"},
                {type: "int", name: "classcode", useNull: true},
                {type: "string", name: "classcode_unicode"},
                {type: "string",  name: "command"},
                {type: "string",  name: "description"},
                {type: "date",  name: "executado_em", dateFormat: "d/m/Y H:i"},
                {type: "int",  name: "executado_por", useNull: true},
                {type: "string",  name: "executado_por_unicode"},
                {type: "bool",  name: "executado"},
                {type: "int",  name: "id"},
                {type: "bool",  name: "em_execucao"},
            ]);
        return this._fields;
    },

    executeAction: function (method, params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: "Processando",
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message,
                });
            },
        };

        this.doRequest(
            this.getRoute(method, null, "POST", {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback(cbSuccess || { fn: Ext.emptyFn }, rst);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback(cbFailure || emptyFailure, "Recurso indisponivel no momento.");
                },
                callback: function () {
                    core.invokeCallback(cbCallback || { fn: Ext.emptyFn });
                },
            })
        );
    },
});

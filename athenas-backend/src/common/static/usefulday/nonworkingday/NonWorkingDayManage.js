/** autogenerated **/
Ext._define('common.usefulday.nonworkingday.NonWorkingDayManage', {
    extend: 'toolkit.widget.TabPanel',

    getNonWorkingDayGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('common.usefulday.nonworkingday.NonWorkingDayGrid', {
                region: 'center',
                flex: 1,
                hideItemsToolbar: ['download'],
                viewConfig: {
                    scope: this,
                    getRowClass: function (record) {
                        if (record.get('has_places') == false && record.get('abrangency') == 3) {
                            return 'x-grid3-red';
                        }
                    }
                }
            });
        }

        this._grid.on({
            beforerender: function () {
                this.setFilterProperty('start_date__year', new Date().format('Y'), 1000, true);
            }
        }
        );
        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Gestor de Dias Úteis'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getNonWorkingDayGrid(),
                ]
            }
        );

        common.usefulday.nonworkingday.NonWorkingDayManage.superclass.constructor.call(this, cfg);
    }
});

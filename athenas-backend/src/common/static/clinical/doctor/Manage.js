/** autogenerated **/
Ext._define('common.clinical.doctor.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getDoctorGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('common.clinical.doctor.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Gestor de Médicos'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getDoctorGrid(),
                ]
            }
        );

        common.clinical.doctor.Manage.superclass.constructor.call(this, cfg);
    }
});

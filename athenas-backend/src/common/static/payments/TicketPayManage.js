/** autogenerated **/
Ext._define('common.payments.TicketPayManage', {
    extend: 'toolkit.widget.TabPanel',

    getTicketPayGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('common.payments.TicketPayGrid', {
                region: 'center',
                gridAutoLoad: true,

            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Boletos'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getTicketPayGrid(),
                ]
            }
        );

        common.payments.TicketPayManage.superclass.constructor.call(this, cfg);
    }
});


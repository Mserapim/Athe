/** autogenerated **/
Ext._define('common.official_journal.official_diary.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getOfficialDiaryGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('common.official_journal.official_diary.Grid', {
                region: 'center',
                gridAutoLoad: true
            });

        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Diário Oficial'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getOfficialDiaryGrid(),
                ]
            }
        );

        common.official_journal.official_diary.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('common.official_journal.diaryorder.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getDiaryOrderGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('common.official_journal.diaryorder.Grid', {
                region: 'center',
                gridAutoLoad: true,
                minWidth: 500
            });

            this._grid.getSelectionModel().on({
                scope: this,
                rowselect: function(sm, index, data) {
                    this.orderDiary(data.get('pk'));
                },
                rowdeselect: function() {
                    this.orderDiary(null);
                }
            });
        }

        return this._grid;
    },

    getDepartmentOrganGrid: function() {
        if(!this._departmentOrganGrid) {
            this._departmentOrganGrid = Ext._create('common.official_journal.diaryorder.DepartmentOrganGrid', {
                region: 'east',
                gridAutoLoad: false,
                minWidth: 650,
                width: Ext.getBody().getBox().width * 0.5,
                split: true,
                allowAdd: false,
                allowUpdate: false,
                allowRemove: false,
                columnAction: false
            });

            var tbar = this._departmentOrganGrid.getToolbar();

            for(var i = 0; i < 4; i++)
                tbar.remove(0);
        }

        return this._departmentOrganGrid;
    },

    orderDiary: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._orderDiary = value;

            !prevent && this.observeOrderDiary();
        }

        return this._orderDiary;
    },

    observeOrderDiary: function() {
        var value = this.orderDiary();

        if(value) {
            this.getDepartmentOrganGrid().enable();
            this.getDepartmentOrganGrid().setFilterProperty(
                'general_organ__id',
                value,
                1001
            );
        }
        else {
            this.getDepartmentOrganGrid().disable();
            this.getDepartmentOrganGrid().setFilterProperty(
                'general_organ__id',
                0,
                1001,
                false
            );
            this.getDepartmentOrganGrid().getStore().removeAll();
        }
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Gestor de Departamentos'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getDiaryOrderGrid(),
                    this.getDepartmentOrganGrid()
                ]
            }
        );

        common.official_journal.diaryorder.Manage.superclass.constructor.call(this, cfg);
        this.orderDiary(null);
    }
});


/** autogenerated **/
Ext._define('health.sst.WorkAccidentCommunicationManage', {
    extend: 'toolkit.widget.TabPanel',

    getWorkAccidentCommunicationGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.sst.WorkAccidentCommunicationGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'CAT - Comunicação de Acidente de Trabalho'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getWorkAccidentCommunicationGrid(),
                ]
            }
        );

        health.sst.WorkAccidentCommunicationManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('health.sst.HarmfulAgentManage', {
    extend: 'toolkit.widget.TabPanel',

    getHarmfulAgentGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.sst.HarmfulAgentGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Agente Nocivo'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getHarmfulAgentGrid(),
                ]
            }
        );

        health.sst.HarmfulAgentManage.superclass.constructor.call(this, cfg);
    }
});


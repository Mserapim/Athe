/** autogenerated **/
/**
 *
 **/
Ext._define('health.sst.MonitorOccupationalHealthGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'health.sst.MonitorOccupationalHealthWindow',

    configOrderToolBar: [
        "add",
        "edit",
        "remove",
        "-",
        "release",
        "-",
        "search",
        "->",
        "download"
    ],

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Descrição', dataIndex: 'unicode', id: 'autoExpandColumn'},
                    {header: 'Servidor', dataIndex: 'employee_unicode', width: 120},
                    {header: 'Data', dataIndex: 'monitoring_date', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Tipo', dataIndex: 'type_aso', width: 90},
                    {header: 'Resultado', dataIndex: 'result_display', width: 90, renderer: function(value) { return (value ? 'Apto' : 'Inapto'); }},
                    {header: 'Médico atestado', dataIndex: 'doctor_unicode', width: 120, hidden: true},
                    {header: 'Médico atestado', dataIndex: 'doctor_manager_unicode', width: 120, hidden: true},
                    {
                        header: "Moficado em",
                        dataIndex: "modified_at",
                        width: 90,
                        renderer: Ext.util.Format.dateRenderer("d/m/Y H:i"),
                        hidden: true,
                    },
                    { header: "Moficado por", dataIndex: "modified_by_unicode", width: 120, hidden: true },
                    {
                        header: "Criado em",
                        dataIndex: "created_at",
                        width: 90,
                        renderer: Ext.util.Format.dateRenderer("d/m/Y H:i"),
                        hidden: true,
                    },
                    { header: "Criado por", dataIndex: "created_by_unicode", width: 120, hidden: true },
                ]
            );

        return this._columnModel;
    },

    getReleaseAction: function (cfg) {
        if (!this._setMainAction) {
            this._setMainAction = Ext._create("Ext.Button", {
                text: "Apto",
                iconCls: "icon-core icon-core-success",
                scope: this,
                handler: function () {
                    if (this.getSelectionModel().getSelected()) {
                        var params = {
                            actionCustom: "release",
                            monitor_occupational_health: this.getSelectionModel().getSelected().id,
                        };

                        this._process(params);
                    } else {
                        Ext.Msg.show({
                            title: "Liberar Periodo Aquisitivo",
                            msg: "Selecione um período",
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK,
                        });
                    }
                },
            });
        }
        return this._setMainAction;
    },

_process: function (params, window_) {
        var rest = this.factoryRestful();
        var mask = Ext._create("Ext.LoadMask", this.getEl(), { msg: "Processando informações." });
        var _grid = this;
        var wnd = window_ == null ? this : window_;

        console.info(wnd.externalCallback);

        if (wnd.externalCallback == undefined)
            wnd.externalCallback = {
                fn: function (message) {
                    console.info(message);
                    _grid.getStore().load();
                },
            };
        mask.show();
        rest._process(
            params,
            {
                scope: this,
                fn: function (rst) {
                    console.info(rst);
                    core.invokeCallback(wnd.externalCallback || { fn: Ext.emptyFn }, rst);
                },
            },
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: "Informando",
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: message,
                    });
                },
            },
            {
                fn: function () {
                    mask.hide();
                },
            }
        );
    },
});

core.RestfulGrid.register(
    'health.sst.MonitorOccupationalHealthRestful',
    'health.sst.MonitorOccupationalHealthGrid'
);


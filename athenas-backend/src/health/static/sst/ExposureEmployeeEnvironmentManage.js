/** autogenerated **/
Ext._define('health.sst.ExposureEmployeeEnvironmentManage', {
    extend: 'toolkit.widget.TabPanel',

    getExposureEmployeeEnvironmentGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.sst.ExposureEmployeeEnvironmentGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Exposição do Servidor'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getExposureEmployeeEnvironmentGrid(),
                ]
            }
        );

        health.sst.ExposureEmployeeEnvironmentManage.superclass.constructor.call(this, cfg);
    }
});


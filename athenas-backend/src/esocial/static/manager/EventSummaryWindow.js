/** autogenerated **/
/**
 *
 **/
Ext._define('esocial.manager.EventSummaryWindow', {
    extend: 'Ext.Window',

    title: 'Layout Window',
    closable: true,

    border: false,
    layout: 'fit',

    getGrid: function (cfg) {
        if (!this._grid) {
            this._grid = Ext._create('esocial.manager.EventSummaryGrid', {
                // title: 'Eventos não enviados',
            });
        }
        return(this._grid)
    },

    getPanel: function (cfg) {
        if (!this._panel) {
            this._panel = this.getGrid();
        }
        return this._panel;
    },

    constructor: function (cfg) {
        cfg = (cfg ? cfg : {});

        Ext.apply(
            cfg,
            {
                title: 'Sumário de eventos não enviados',
                modal: false,
                resizable: false,
                items: this.getPanel(),
            }
        );

        esocial.manager.EventSummaryWindow.superclass.constructor.call(this, cfg);
    }

});


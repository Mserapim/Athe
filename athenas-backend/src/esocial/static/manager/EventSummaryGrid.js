/** autogenerated **/
/**
 *
 **/
Ext._define("esocial.manager.EventSummaryGrid", {
    extend: 'core.RestfulGrid',
    rest: 'esocial.manager.EventSummaryRestful',
    hideItemsToolbar: ['add', 'edit', 'remove', 'search', 'download'],
    hideActions: ['remove',],

    getTotal: function (value, metadata, record, rowindex, colindex, store) {
        console.info(record);
        return (record.data.INC + record.data.MOD + record.data.RET + record.data.EXC + record.data.INV);
    },

    getToolbar: function (cfg) {
        return undefined;
    },

    getColumnModel: function () {
        if (this._columnModel) {
            return this._columnModel;
        }

        this._columnModel = Ext._create(
            'Ext.grid.ColumnModel',
            [
                Ext._create('Ext.grid.RowNumberer'),
                { header: 'Evento', dataIndex: 'acronym2',id: 'autoExpandColumn'},
                { header: 'Inclusões', dataIndex: 'INC', width: 85},
                { header: 'Modificações', dataIndex: 'MOD', width: 85},
                { header: 'Retificações', dataIndex: 'RET', width: 85},
                { header: 'Exclusões', dataIndex: 'EXC', width: 85 },
                { header: 'Inválidos', dataIndex: 'INV', width: 85 },
                { header: 'Total', dataIndex: 'Total', width: 85, renderer: this.getTotal},
            ]
        );

        return this._columnModel;
    },
    // constructor: function (cfg) {
    //     cfg = (cfg ? cfg : {});

    //     Ext.apply(
    //         cfg,
    //         {
    //             title: 'Atualização do Certificado',
    //         }
    //     );

    //     esocial.manager.EventSummaryGrid.superclass.constructor.call(this, cfg);
    // }
});

core.RestfulGrid.register(
    'esocial.manager.EventSummaryRestful',
    'esocial.manager.EventSummaryGrid'
);

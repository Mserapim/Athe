/** autogenerated **/
Ext._define('esocial.payrollperiod.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getPayrollPeriodGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('esocial.payrollperiod.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Período (Folhas) do eSocial'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getPayrollPeriodGrid(),
                ]
            }
        );

        esocial.payrollperiod.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.anotacao.anotacaotemposervico.Grid', {
    extend: 'rh.anotacao.anotacaogeral.Grid',

    restWindow: 'rh.anotacao.anotacaotemposervico.Window',

    constructor: function(cfg) {
        rh.anotacao.anotacaotemposervico.Grid.superclass.constructor.call(this, cfg);
    },

    getColumnModelItems: function(){
        if(!this._columnModelItems){
            this._columnModelItems = rh.anotacao.anotacaotemposervico.Grid.superclass.getColumnModelItems.call(this, {});
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'tipo_documento_display');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'data_documento');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'movimento_origem_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'numero_documento');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'numero_processo');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'publicacao_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'resumo');            
            this._columnModelItems = this._columnModelItems.concat([
                {header: 'Tipo de Regime', dataIndex: 'tipo_regime_display', width: 110},
                {header: 'Finalidade', dataIndex: 'tempo_servico_finalidade_unicode', width: 120, sortable: true},
                {header: 'Pessoa Jurídica', dataIndex: 'pessoa_juridica_unicode', width: 90, sortable: true, hidden: true},
                {header: 'Data Início', dataIndex: 'data_inicio', width: 80, renderer: Ext.util.Format.dateRenderer('d/m/Y'), sortable: true},
                {header: 'Data Fim', dataIndex: 'data_fim', width: 80, renderer: Ext.util.Format.dateRenderer('d/m/Y'), sortable: true},
                {header: 'Tempo Bruto', dataIndex: 'tempo_bruto', width: 90, sortable: true},
                {header: 'Dedução', dataIndex: 'deducao', width: 70, sortable: true},
                {header: 'Tempo Líquido', dataIndex: 'tempo_liquido', width: 90, sortable: true},
                {header: 'Ano(s)', dataIndex: 'anos', width: 70, sortable: true},
                {header: 'Mês(es)', dataIndex: 'meses', width: 70, sortable: true},
                {header: 'Dia(s)', dataIndex: 'dias', width: 70, sortable: true},
            ]);
        }
        return this._columnModelItems;
    }
});

core.RestfulGrid.register(
    'rh.anotacao.anotacaotemposervico.Restful',
    'rh.anotacao.anotacaotemposervico.Grid'
);


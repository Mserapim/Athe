/** autogenerated **/
/**
 *
 **/
Ext._define('rh.workload.WorkloadWindow', {
    extend: 'core.RestfulWindow',

    rest: 'rh.workload.WorkloadRestful',

    width: 500,

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.employeeField(),
                    this.jornadaTrabalhoField(),
                    // this.workloadTypeField(),
                    // this.quantityField(),
                    this.dateStartField(),
                    this.dateEndField(),
                    this.publicationField(),
                    this.textField(),
                ]
            });

        return this._formPanel;
    },

    employeeField: function(cfg){
        if(!this._employeeField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                fieldLabel: 'Servidor',
                allowBlank: true,
                rest: 'rh.employee.Restful',
                name: 'servidor'
            });
            this._employeeField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._employeeField;
    },

    jornadaTrabalhoField: function(cfg){
        if(!this._jornadaTrabalhoField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                fieldLabel: 'Jornada de Trabalho Diária',
                allowBlank: false,
                rest: 'rh.hoursworkcontract.Restful', 
                name: 'jornada_trabalho',
                width: 365,
            });
            this._jornadaTrabalhoField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._jornadaTrabalhoField;
    },

    workloadTypeField: function(cfg){
        if(!this._workloadTypeField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                fieldLabel: 'Tipo',
                allowBlank: false,
                hiddenName: 'tipo',
                width: 365,
                name: 'tipo',
                choiceId: 'rh.WORKLOAD_TYPE'
            });
            this._workloadTypeField = Ext._create('standard.fields.ChoiceField', cfg);
        }
        return this._workloadTypeField;
    },

    quantityField: function(cfg){
        if(!this._quantityField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                decimalPrecision: 2,
                fieldLabel: 'Quantidade',
                allowBlank: false,
                allowDecimals: true,
                name: 'quantidade',
                width: 365,
            });
            this._quantityField = Ext._create('Ext.form.TextField', cfg);
        }
        return this._quantityField;
    },

    dateStartField: function(cfg){
        if(!this._dateStartField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                allowBlank: false,
                fieldLabel: 'Data In\u00edcio',
                name: 'data_inicio',
                xtype: 'datefield',
                width: 365
            });
            this._dateStartField = Ext._create('Ext.form.DateField', cfg);
        }
        return this._dateStartField;
    },

    dateEndField: function(cfg){
        if(!this._dateEndField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                allowBlank: true,
                fieldLabel: 'Data Fim',
                name: 'data_fim',
                xtype: 'datefield',
                width: 365
            });
            this._dateEndField = Ext._create('Ext.form.DateField', cfg);
        }
        return this._dateEndField;
    },

    publicationField: function(cfg){
        if(!this._publicationField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                fieldLabel: 'Publicação',
                allowBlank: true,
                rest: 'rh.publicacao.Restful',
                name: 'publicacao',
                width: 365
            });
            this._publicationField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._publicationField;
    },

    textField: function(cfg){
        if(!this._textField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {
                allowBlank: true,
                fieldLabel: 'Texto',
                name: 'texto',
            });
            this._textField = Ext._create('toolkit.fields.CKEditor', cfg);
        }
        return this._textField;
    },

    save: function(close) {
        var msg = "";
        if (this.title && this.title.indexOf("Editar") !== -1) {
            msg = "Tem certeza que deseja salvar essas alterações?";
        } else {
            msg = "Se o Servidor já possuir Carga Horária ativa, o sistema irá inativar a anterior e cadastrará a nova. Confirma?";
        }
        
        Ext.Msg.show({
            title: this.title,
            icon: Ext.Msg.QUESTION,
            buttons: Ext.Msg.YESNO,
            msg: msg,
            scope: this,
            fn: function(btn) {
                if (btn == 'no') return;
                rh.workload.WorkloadWindow.superclass.save.call(this, close);
            }
        });
    }

});


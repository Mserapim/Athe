/** autogenerated **/
/**
 *
 **/
Ext._define('scmmp.membroprocesso.MembroProcessoGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'scmmp.membroprocesso.MembroProcessoWindow',

    configOrderToolBar: ['adicionarMembro', 'edit', 'remove', '-', 'sancaoMembro', '-', 'search',],

    getAdicionarMembroAction: function(cfg) {
        if(!this.actionAdicionar)
            this.actionAdicionar = Ext._create('Ext.Button', {
                text: 'Novo',
                iconCls: 'icon-scmmp icon-scmmp-add',
                scope: this,
                handler: this._adicionarMembro
            });
    
        return this.actionAdicionar;
    },

    getSancaoMembroAction: function(cfg) {
        if(!this.actionsancao)
            this.actionsancao = Ext._create('Ext.Button', {
                text: 'Sanção Judicial',
                iconCls: 'icon-scmmp icon-scmmp-decisao',
                scope: this,
                menu: [
                    {
                        text: 'Cadastrar Sanção',
                        scope: this,
                        group: 'tipo',
                        filter: 'todos',
                        iconCls: 'icon-scmmp icon-scmmp-add',
                        handler: this._adicionarSancao
                    },
                    {
                        text: 'Visualizar Sanção',
                        scope: this,
                        iconCls: 'icon-scmmp icon-scmmp-document-validate',
                        handler: this._verSancao
                    },
                ]
            });
    
        return this.actionsancao;
    },

    _adicionarSancao: function(){
        
        var selected = this.getSelectionModel().getSelected();
        if (selected){

            Ext._create('scmmp.sancaojudicial.SancaoJudicialWindow',{
                action: 'create',
                title: 'Sanção Judicial',
                scope:this,
                params: {'processo_judicial': this.idProcesso, 'membro_processo': selected.data.pk},
                callback: {
                success: {
                        scope: this,
                        fn: function() {
                            this.getStore().reload();
                        }
                    }
                }
            }).show();
        }else{
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.INFO,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um Membro'
            });
        }

    },
    
    _adicionarMembro: function(){
        Ext._create('scmmp.membroprocesso.MembroProcessoWindow',{
            action: 'create',
            title: 'Adicionar Membro',
            scope:this,
            params: {'processo_judicial': this.idProcesso},
            callback: {
                success: {
                    scope: this,
                    fn: function() {
                        this.getStore().reload();
                    }
                }
            }
        }).show();
    },    

    _verSancao: function(){

        var selected = this.getSelectionModel().getSelected();
        if (selected){

            grid = Ext._create('scmmp.sancaojudicial.SancaoJudicialGrid', {
                border: false,
                height: 400,
                gridAutoLoad: false,
                columnAction: false,
                allowUpdate: false,
                allowRemove: false,
            });

            grid.setFilterProperty('membro_processo', selected.data.pk);
            grid.setParam('membro_processo', selected.data.pk);


            Ext._create('Ext.Window', {
                width: 900,
                modal: true,
                title: 'Visualização de Sanções',
                items: grid
            }).show();

        }else{
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.INFO,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um Membro'
            });
        }
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: '', dataIndex: 'icons', width: 60, renderer: core.rendererIconGrid},
                    {header: 'Membro', dataIndex: 'membro_unicode', id: 'autoExpandColumn', },
                    {header: 'Situação', dataIndex: 'situacao_display', width: 120},
                    {header: 'Data', dataIndex: 'data_situacao', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                ]
            );

        return this._columnModel;
    }
});

core.RestfulGrid.register(
    'scmmp.membroprocesso.MembroProcessoRestful',
    'scmmp.membroprocesso.MembroProcessoGrid'
);


/** autogenerated **/
/**
 *
 **/
Ext._define('estagio.decisaoestagio.DecisaoEstagioGrid', {

     extend: 'core.RestfulGrid',
    
    restWindow: 'estagio.decisaoestagio.DecisaoEstagioWindow',

    hideItemsToolbar: ['add','edit', 'remove', 'download'],

    configOrderToolBar: ['openRecomendar', 'openVisualizacao', 'openRelatorio', 'search', '-', '->'],

    getOpenRecomendarAction: function(){
        if(!this.openGerenciamento)
            this.openGerenciamento = Ext._create('Ext.Button', {
                text: 'Decisão',
                iconCls: 'icon-gep-decisao',
                scope: this,
                handler: this.decisao
            });
    
        return this.openGerenciamento;
    },

    decisao: function(param){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            var wnd = Ext._create('estagio.decisaoestagio.DecisaoEstagioWindowForm', {
                modal: true,
                params: {
                    'pk': selected.data.pk,
                },
            });
            wnd.show()
        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um item'
            });
        }
        // new toolkit.gep.DecisaoGestorForm({
        //         'action': 'decisao_gestor_orgao',
        //         'params': {
        //             'pk_comissao_servidor': selected.data.pk
        //         }, 
        //         'callback': {
        //             'success': {
        //                 'scope': this,
        //                 'handler': function() {
        //                     this.getStore().reload()
        //                 }
        //             }
        //         }
        //     }).show();
      
    },

    getOpenRelatorioAction: function(){
        if(!this._openRelatorio)
            this._openRelatorio = Ext._create('Ext.Button', {
                text: 'Relatório',
                iconCls: 'icon-estagio icon-archive-pdf',
                scope: this,
                menu: [
                    {
                        text: 'Relatório Notas de Avaliação',
                        scope: this,
                        group: 'tipo',
                        filter: 'todos',
                        iconCls: 'icon-estagio icon-archive-pdf',
                        handler: function() { this.getReportSimple(1); }
                    },
                    {
                        text: 'Relatório de Avaliação',
                        scope: this,
                        group: 'tipo',
                        filter: 'bloquear',
                        iconCls: 'icon-estagio icon-archive-pdf',
                        handler: this.getReport
                    },
                    {
                        text: 'Relatório Decisão Estágio',
                        scope: this,
                        iconCls: 'icon-estagio icon-archive-pdf',
                        handler: function() { this.getReportSimple(2); }
                    },
                ]
            });
    
        return this._openRelatorio;
    },

    getReport: function(){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            var wnd = Ext._create('estagio.gestor.RelatorioWindow', {
                modal: true,
                params: {
                    'servidor': selected.data.servidor_id,
                    'cargo': selected.data.cargo_id,
                    'questionario_avaliacao': selected.data.questionario_id,
                    'questionario_manifestacao': selected.data.questionario_manifestacao_id,
                },
            });
            wnd.show()
        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um servidor'
            });
        }
    },

    getReportSimple: function(param){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            if (param ==1)
                // new toolkit.widget.ExtReportBuild('GepPrintAvaliacao', '/to/mpe/rh/estagio_probatorio/notas/rh_ep_main').runReport(
                //     '', {servidor: selected.data.servidor_id, cargo: selected.data.cargo_id}
                // );
                engine.mq.Report.request({
                    report: '/to/mpe/rh/estagio_probatorio/notas/notas',
                    el: this.getEl(),
                    waitMessage: 'Gerando os documentos...',
                    params: {
                        outfile: 'relatorio-' + selected.data.posse_servidor_unicode,
                        report_name: 'Relatorio Estágio Probatório',
                        servidor: selected.data.servidor_id, 
                        cargo: selected.data.cargo_id
                    }
                });

            else if (param ==2)
                // new toolkit.widget.ExtReportBuild('GepPrintDecisaoEstagio', '/to/mpe/rh/estagio_probatorio/especial/avaliacao/rh_ep_especial_avaliacao_main').runReport(
                //     '', {servidor: selected.data.servidor_id, cargo: selected.data.cargo_id}
                // );
                engine.mq.Report.request({
                    report: '/to/mpe/rh/estagio_probatorio/especial/avaliacao/avaliacao',
                    el: this.getEl(),
                    waitMessage: 'Gerando os documentos...',
                    params: {
                        outfile: 'relatorio-' + selected.data.posse_servidor_unicode,
                        report_name: 'Relatorio Estágio Probatório',
                        servidor: selected.data.servidor_id, 
                        cargo: selected.data.cargo_id
                    }
                });

        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um servidor'
            });
        }

    },

    getOpenVisualizacaoAction: function(){
        if(!this.openVisualizacao)
            this.openVisualizacao = Ext._create('Ext.Button', {
                text: 'Visualizar',
                iconCls: 'icon-estagio icon-view',
                scope: this,
                menu: [
                    {
                        text: 'Avaliações',
                        scope: this,
                        group: 'tipo',
                        filter: 'todos',
                        iconCls: 'icon-estagio icon-view',
                        handler: this.getAvaliacoes
                    },
                    {
                        text: 'Manifestações',
                        scope: this,
                        iconCls: 'icon-estagio icon-view',
                        handler: this.getManifestacao
                    },
                ]
            });
    
        return this.openVisualizacao;
    },

    getAvaliacoes: function(){
        var selected = this.getSelectionModel().getSelected();
        if(selected) {
            var url = toolkit.util.Normalize.controller_action('GepEstagioProbatorioServidor','get_resposta_avaliacao',[selected.data.questionario_id,selected.data.estagio_prob_servidor]);
            new toolkit.questionario.VerResposta({
                'title': 'Avaliações de Estágio Probatório Servidor: ' + selected.data.estagio_prob_servidor_unicode,
                'callback': {
                    'success': {
                        'scope': this,
                        'handler': function() {
                            this.getStore().reload()
                        }
                    }
                }
            }, selected.data.estagio_prob_servidor, url ).show();
        }
        else {
            Ext.Msg.show({
                'title': 'Atenção',
                'msg': 'Selecione',
                'icon': Ext.Msg.WARNING,
                'buttons': Ext.Msg.OK
            });
        }

    },

    getManifestacao: function(){
        var selected = this.getSelectionModel().getSelected();
        if(selected) {
            var url = toolkit.util.Normalize.controller_action('GepEstagioProbatorioServidor','get_resposta_avaliacao',[selected.data.questionario_manifestacao_id,selected.data.estagio_prob_servidor]);
            
            new toolkit.questionario.VerResposta({
                'title': 'Manifestação de Estágio Probatório: ' + selected.data.estagio_prob_servidor_unicode,
                'callback': {
                    'success': {
                        'scope': this,
                        'handler': function() {
                            this.getStore().reload()
                        }
                    }
                }
            }, selected.data.estagio_prob_servidor, url ).show();
        }
        else {
            Ext.Msg.show({
                'title': 'Atenção',
                'msg': 'Selecione',
                'icon': Ext.Msg.WARNING,
                'buttons': Ext.Msg.OK
            });
        }
    }, 

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Status', dataIndex: 'status', width: 120, renderer: core.rendererIconGrid},
                    {header: 'Servidor', dataIndex: 'estagio_prob_servidor_unicode', id: 'autoExpandColumn'},
                    {header: 'Data Exercício', dataIndex: 'data_exercicio', width: 150, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Última Avaliação', dataIndex: 'ultima_avaliacao', width: 100, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Média', dataIndex: 'media', width: 80},
                    
                ]
            );

        return this._columnModel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.apply(
            cfg,
            {
                columnAction: false,
                allowCreate: false,
                allowUpdate: false,
                allowRemove: false,
            }
        );

        estagio.decisaoestagio.DecisaoEstagioGrid.superclass.constructor.call(this, cfg);
    }
});

core.RestfulGrid.register(
    'estagio.decisaoestagio.DecisaoEstagioRestful',
    'estagio.decisaoestagio.DecisaoEstagioGrid'
);


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.replacement.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.replacement.Restful',

    height: 250,
    width: 550,
    saveAndContinue: true,

    _prepareSuccessCallback: function (callback) {
        callback = rh.replacement.Window.superclass._prepareSuccessCallback.call(this, callback);
        callback = core.nullValue(callback, {});

        var owner = this;
        callback.success = {
            fn: function (args) {
                if (owner.getFormPanel().getForm().getValues().publication_update) {
                    var rest = owner.factoryRestful();
                    var params = { document: owner.getFormPanel().getForm().getValues().document };
                    var mask = new Ext.LoadMask(owner.getEl(), { msg: 'criando Exercício...' });
                    mask.show();
                    rest.updateDocument(
                        params,
                        {
                            scope: owner,
                            fn: function (rst) {
                                Ext.Msg.show({
                                    title: 'Informando',
                                    icon: Ext.Msg.INFO,
                                    buttons: Ext.Msg.OK,
                                    msg: rst.message
                                });
                                owner.close();
                            }
                        },
                        {
                            fn: function (message) {
                                Ext.Msg.show({
                                    title: 'Informando',
                                    icon: Ext.Msg.ERROR,
                                    buttons: Ext.Msg.OK,
                                    msg: message
                                });
                            }
                        },
                        {
                            fn: function () {
                                mask.hide();
                            }
                        }
                    );
                }
            }
        };
        return callback;
    },

    getFormPanel: function (cfg) {
        if (!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Local",
                        allowBlank: false,
                        rest: "rh.workplace.Restful",
                        name: "replaced"
                    },
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Substituto",
                        allowBlank: false,
                        rest: "rh.workplace.Restful",
                        name: "substitute"
                    },
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Publicação",
                        allowBlank: false,
                        rest: "rh.publicacao.Restful",
                        name: "document"
                    },
                    {
                        xtype: 'checkbox',
                        fieldLabel: 'Atualizar publicação em toda tabela?',
                        allowBlank: true,
                        name: 'publication_update'
                    },
                    {
                        allowBlank: false,
                        fieldLabel: "Ordem",
                        name: "order",
                        xtype: "textfield"
                    },
                ]
            });

        return this._formPanel;
    }
});


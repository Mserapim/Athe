/** autogenerated **/
Ext._define('rh.pvf.absence.maternity.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getMaternityAbsenceGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.absence.maternity.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'MaternityAbsence'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getMaternityAbsenceGrid(),
                ]
            }
        );

        rh.pvf.absence.maternity.Manage.superclass.constructor.call(this, cfg);
    }
});


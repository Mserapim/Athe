/** autogenerated **/
Ext._define('rh.pvf.absence.marriage.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getMarriageAbsenceGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.absence.marriage.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'MarriageAbsence'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getMarriageAbsenceGrid(),
                ]
            }
        );

        rh.pvf.absence.marriage.Manage.superclass.constructor.call(this, cfg);
    }
});


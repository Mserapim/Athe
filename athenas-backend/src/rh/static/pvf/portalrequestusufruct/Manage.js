/** autogenerated **/
Ext._define('rh.pvf.portalrequestusufruct.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getPortalRequestUsufructGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.portalrequestusufruct.Grid', {
                region: 'center',
                gridAutoLoad: false
                
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'PortalRequestUsufruct'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getPortalRequestUsufructGrid(),
                ]
            }
        );

        rh.pvf.portalrequestusufruct.Manage.superclass.constructor.call(this, cfg);
    }
});


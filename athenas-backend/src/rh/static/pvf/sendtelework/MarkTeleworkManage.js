/** autogenerated **/
Ext._define('rh.pvf.sendtelework.MarkTeleworkManage', {
    extend: 'toolkit.widget.TabPanel',

    getMarkTeleworkGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.sendtelework.MarkTeleworkGrid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Metas do Teletrabalho'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getMarkTeleworkGrid(),
                ]
            }
        );

        rh.pvf.sendtelework.MarkTeleworkManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.digitaldocument.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.digitaldocument.Restful',

    width: 540,

    constructor: function(cfg) {
        cfg = cfg || {};
        rh.digitaldocument.Window.superclass.constructor.call(this, cfg);
        this._observe();
        this._do_exclude_document_type(cfg.exclude_document_type);
    },

    _observe: function() {
        var values = this.getParams();
        if(values != undefined && values.employee != undefined){
            this.getEmployeeField().setReadOnly(true);
            // this.getPersonField().setReadOnly(true);
        }else{
            this.getEmployeeField().setReadOnly(false);
            // this.getPersonField().setReadOnly(false);
        }
        if(values != undefined && values.document_type != undefined)
            this.getDocumentTypeChoiceField().setReadOnly(true);
        else
            this.getDocumentTypeChoiceField().setReadOnly(false);
        this._do_exclude_document_type(values.exclude_document_type);
    },

    _do_exclude_document_type: function(exclude_document_type) {
        exclude_document_type = exclude_document_type || [];
        var store = this.getDocumentTypeChoiceField().getStore();
        var filter = Ext.decode(store.baseParams.filter);
        filter.push({property: 'value__in', value: exclude_document_type, stage: -1});
        store.baseParams.filter = Ext.encode(filter);
        store.load();
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.getEmployeeField(),
                    // this.getPersonField(),
                    {
                        maxLength: 100,
                        allowBlank: false,
                        fieldLabel: "Nome *",
                        name: "name",
                        xtype: "textfield"
                    },
                    {
                        xtype: 'ged-fileuploadfield-3',
                        fieldLabel: 'Arquivo *',
                        allowBlank: false,
                        name: 'file',
                        hiddenName: 'file',
                        _downloadFile: false
                    },
                    this.getDocumentTypeChoiceField(),
                    {
                        allowBlank: true,
                        fieldLabel: "Data Início",
                        name: "date_start",
                        xtype: "datefield",
                        width: 215
                    },
                    {
                        allowBlank: true,
                        fieldLabel: "Data Fim",
                        name: "date_end",
                        xtype: "datefield",
                        width: 215
                    },
                    {
                        fieldLabel: 'Descrição',
                        // hideLabel: true,
                        name: "description",
                        xtype: "ckeditor"
                    },
                ]
            });
        return this._formPanel;
    },

    getEmployeeField: function(cfg){
        if(!this._employeeField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Servidor *",
                    allowBlank: false,
                    rest: "rh.employee.Restful",
                    name: "employee",
                }
            );
            this._employeeField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._employeeField;
    },

    getPersonField: function(cfg){
        if(!this._personField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Pessoa",
                    allowBlank: true,
                    rest: "rh.pessoa.Restful",
                    name: "person",
                }
            );
            this._personField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._personField;
    },

    getDocumentTypeChoiceField: function(cfg) {
        if (!this._documentTypeChoiceField){
            this._documentTypeChoiceField = Ext._create('standard.fields.ChoiceField', {
                fieldLabel: 'Tipo *',
                hiddenName: 'document_type',
                choiceId: 'rh.DIGITAL_DOCUMENT_TYPE',
                width: 215
            });
            var store = this._documentTypeChoiceField.getStore();
            var filter = Ext.decode(store.baseParams.filter);
            filter.push({property: 'value__in', value: [7, 8], stage: -1});
            store.baseParams.filter = Ext.encode(filter);
            store.load();
        }
        return this._documentTypeChoiceField;
    },

});


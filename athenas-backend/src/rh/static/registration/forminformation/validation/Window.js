/** autogenerated **/
/**
 *
 **/
Ext._define('rh.registration.forminformation.validation.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.registration.forminformation.validation.Restful',

    width: 500,
    height: 450,

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                height: 430,
                items: [
                    {
                        fieldLabel: 'Validado por',
                        xtype: 'displayfield',
                        name: 'validate_employee',
                    },
                    {
                        fieldLabel: 'Validado em',
                        xtype: 'displayfield',
                        name: 'created_at',
                        listeners: {
                            scope: this,
                            afterrender: function(field){
                                field.setValue(Ext.util.Format.dateRenderer('d/m/Y H:i')(field.getValue()));
                            }
                        }
                    },
                    {
                        hideLabel: true,
                        xtype: 'xhtmleditor',
                        name: 'text',
                        readOnly: true,
                        layout: 'fit',
                        width: 470,
                        height: 350,
                        autoScroll: true,
                        listeners:{
                            scope: this,
                            afterrender: function(field){
                                field.getToolbar().hide()
                            }
                        }
                    }
                ]
            });

        return this._formPanel;
    },

    getButtons: function(cfg) {
        if(!this._buttons) {
            this._buttons = [
                {
                    'text': 'Fechar',
                    'scope': this,
                    'handler': this.destroy
                }
            ];
        }

        return this._buttons;
    },
});


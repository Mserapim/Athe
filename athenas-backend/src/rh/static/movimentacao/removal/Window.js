/** autogenerated **/
/**
 *
 **/
Ext._define('rh.movimentacao.removal.Window', {
    extend: 'rh.movimentacao.pessoal.Window',

    rest: 'rh.movimentacao.removal.Restful',

    width: 550,
    height: 470,

    constructor: function(cfg) {
        rh.movimentacao.removal.Window.superclass.constructor.call(this, cfg);
    },

    getPanelInformationItems: function(cfg_window){
        var items = rh.movimentacao.removal.Window.superclass.getPanelInformationItems.call(this, cfg_window);
        items.push({
            xtype: 'choicefield',
            fieldLabel: 'Remoção *',
            hiddenName: 'remocao',
            choiceId: 'rh.LEVEL_REMOVAL_REASON'
        });
        items.push(this.getPublicationField(cfg_window, {}));
        items.push(Ext._create('core.fields.AutocompleteField', {
            fieldLabel: "Permutado",
            allowBlank: true,
            rest: "rh.employee.Restful",
            name: "servidor_permuta"
        }));
        items.push(Ext._create('core.fields.AutocompleteField', {
            fieldLabel: "Nova Lotação",
            allowBlank: true,
            rest: "rh.workplace.Restful",
            name: "lotacao_destino"
        }));
        items.push({
            allowBlank: true,
            fieldLabel: "Data Vigência *",
            name: "data_vigencia",
            xtype: "datefield"
        });
        return items;
    },

    getTabPanel: function(cfg_window, cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(
            cfg,
            {
                height: 400,
            }
        );
        return rh.movimentacao.removal.Window.superclass.getTabPanel.call(this, {}, cfg);
    },

    getJobPosition: function(cfg_window, cfg) {
        if(!this._jobPositionField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg, 
                {
                    fieldLabel: "Cargo",
                    allowBlank: true,
                    rest: "rh.quadro.Restful",
                    name: "quadro"
                }
            );
            this._jobPositionField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._jobPositionField;
    },
});

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.movimentacao.pessoal.Window', {
    extend: 'rh.raw.employee.Window',

    rest: 'rh.movimentacao.pessoal.Restful',

    constructor: function(cfg) {
        rh.movimentacao.pessoal.Window.superclass.constructor.call(this, cfg);
    },

    getPanelInformationItems: function(cfg_window){
        return rh.movimentacao.pessoal.Window.superclass.getPanelInformationItems.call(this, cfg_window);
    },

    getPublicationField: function(cfg_window, cfg){
        if(!this._publicationField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    fieldLabel: "Publicação *",
                    allowBlank: false,
                    rest: "rh.publicacao.Restful",
                }
            );
            Ext.apply(cfg, {name: "publicacao_movimentacao"});
            this._publicationField = Ext._create('rh.raw.AutocompleteField', cfg);
        }
        return this._publicationField;
    },

    getPublicationFieldChange: function(cfg_window, cfg){
        if(!this._publicationChange){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg, 
                {
                    fieldLabel: "Publicação Alteração",
                    allowBlank: true,
                    rest: "rh.publicacao.Restful",
                }
            );
            Ext.apply(cfg, {name: "publicacao_alteracao"});
            this._publicationChange = Ext._create('rh.raw.AutocompleteField', cfg);
        }
        return this._publicationChange;
    }
});

/** autogenerated **/
Ext._define('rh.movimentacao.teletrabalho.meta.Manage', {
    extend: 'toolkit.widget.TabPanel',

    Grid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.movimentacao.teletrabalho.meta.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Meta do Teletrabalho'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.Grid(),
                ]
            }
        );

        rh.movimentacao.teletrabalho.meta.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.movimentacao.progression.Window', {
    extend: 'rh.movimentacao.pessoal.Window',

    rest: 'rh.movimentacao.progression.Restful',

    constructor: function (cfg) {
        rh.movimentacao.progression.Window.superclass.constructor.call(this, cfg);
    },

    _observe: function () {
        rh.movimentacao.progression.Window.superclass._observe.call(this, {});
        var values = this.getParams();
        if (values != undefined && values.servidor != undefined) {
            this.getPossessionField().setPreFilter([
                {
                    property: 'servidor__id',
                    value: values.servidor,
                    stage: 0,
                }
            ]);
            this.getProgressionField().setPreFilter([
                {
                    property: 'servidor__id',
                    value: values.servidor,
                    stage: 0,
                }
            ]);
        }
    },

    getPanelInformationItems: function (cfg_window) {
        var items = [];
        items.push(this.getPossessionField());
        items.push(this.getProgressionField());
        items.push(this.getPublicationField(cfg_window, {}));
        items.push(Ext._create('core.fields.AutocompleteField', {
            fieldLabel: "Refer\u00eancia N\u00edveis *",
            allowBlank: true,
            rest: "rh.gfp.estrutura_salarial.ReferenciaNiveis2DRestful",
            name: "referencia_nivel2d"
        }));
        items.push({
            allowBlank: true,
            fieldLabel: "Data referência inicial",
            name: "data_referencia_inicial",
            xtype: "datefield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Data Referência",
            name: "data_referencia",
            xtype: "datefield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Data início vigência *",
            name: "data_inicio_vigencia",
            xtype: "datefield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Data fim vigência",
            name: "data_fim_vigencia",
            xtype: "datefield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Dias suspenso *",
            name: "dias_suspenso",
            xtype: "textfield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Dias suspenso afastamento *",
            name: "dias_suspenso_afastamento",
            xtype: "textfield"
        });
        return items;
    },

    getPossessionField: function (cfg_window, cfg) {
        if (!this._possessionField) {
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Posse *",
                    allowBlank: false,
                    rest: "rh.movimentacao.possession.AllPossessionsRestful",
                    name: "movimentacao_posse"
                }
            );
            this._possessionField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._possessionField;
    },

    getProgressionField: function (cfg_window, cfg) {
        if (!this._progressionField) {
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Progressão anterior",
                    allowBlank: true,
                    rest: "rh.movimentacao.progression.Restful",
                    name: "progressao_anterior"
                }
            );
            this._progressionField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._progressionField;
    }
});

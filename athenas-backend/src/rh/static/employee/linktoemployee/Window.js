/** autogenerated **/
/**
 *
 **/
Ext._define('rh.employee.linktoemployee.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.employee.linktoemployee.Restful',

    width: 500,

    constructor: function(cfg) {
        rh.employee.linktoemployee.Window.superclass.constructor.call(this, cfg);
        this._observe();
    },

    _observe: function() {
        var values = this.getParams();
        if(values != undefined && values.servidor != undefined){
            this.getEmployeeField().setValue(values.servidor);
            this.getEmployeeField().setReadOnly(true);
        }
        else
            this.getEmployeeField().setReadOnly(false);
    },

    getFormPanel: function(cfg_window, cfg) {
        if(!this._formPanel){
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.getEmployeeField(),
                    this.getEmployeeLinkedField(),
                    {
                        fieldLabel: 'Vínculo',
                        xtype: 'choicefield',
                        hiddenName: 'vinculo',
                        choiceId: 'rh.GRAU_PARENTESCO_CHOICES',
                        allowBlank: false
                    }
                ]
            });        
        }
        return this._formPanel;
    },
    
    getEmployeeField: function(cfg){
        if(!this._employeeField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Servidor",
                    allowBlank: false,
                    rest: "rh.employee.Restful",
                    name: "servidor",
                }
            );
            this._employeeField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._employeeField;
    },

    getEmployeeLinkedField: function(cfg){
        if(!this._employeeLinkedField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Servidor Vinculado",
                    allowBlank: false,
                    rest: "rh.employee.Restful",
                    name: "servidor_vinculado",
                }
            );
            this._employeeLinkedField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._employeeLinkedField;
    },
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.employee.workplace.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.employee.workplace.Restful',

    width: 500,

    constructor: function(cfg) {
        rh.employee.workplace.Window.superclass.constructor.call(this, cfg);
        if(!this.getProvisoryField().getValue()) this.getReasonProvisoryField().hide();
        this._observe();
    },

    _observe: function() {
        var values = core.nullValue(this.values, {});
        if(values.servidor != undefined){
            this.getEmployeeField().setValue(values.servidor);
            this.getEmployeeField().setReadOnly(true);
        }
        else
            this.getEmployeeField().setReadOnly(false);

        if(values.designacao != undefined)
            this.getDesignationField().setValue(values.designacao);
        if(values.servidor != undefined){
            this.getPossessionField().setPreFilter([
                {
                    property: 'servidor__id',
                    value: values.servidor,
                    stage: 0,
                }
            ]);
        }
        if(values.lotacao != undefined){
            this.getWorkplaceField().setValue(values.lotacao);
            this.getWorkplaceField().setReadOnly(true);
            this.getAssignmentField().setPreFilter([
                {
                    property: 'lotacao__id',
                    value: values.lotacao,
                    stage: 1,
                }
            ]);
        }else{
            this.getWorkplaceField().setValue(0);
            this.getWorkplaceField().setReadOnly(false);
        }
        if(values.movimentacao_posse != undefined)
            this.getPossessionField().setValue(values.movimentacao_posse);
        else if(!values.movimentacao_posse)
            this.getPossessionField().setValue(0);
        if(values.child_of != undefined)
            this.getFormPanel.child_of = values.child_of;
        else
            this.getFormPanel.child_of = undefined;
        
        if(values.from_substitution)
            this.getSubstitutionField().show()
        else
            this.getSubstitutionField().hide()
    },

    getFormPanel: function(cfg_window, cfg) {
        if(!this._formPanel){
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.getEmployeeField(),
                    this.getPossessionField(cfg_window),
                    this.getSubstitutionField(cfg_window),
                    {
                        xtype: "combo", 
                        fieldLabel: "Área de Atuação", 
                        allowBlank: true,
                        lazyRender: true, 
                        hiddenName: "occup_area", 
                        mode: "local", 
                        triggerAction: "all", 
                        store: [
                            [1, "ÁREA FIM"], 
                            [2, "ÁREA MEIO"], 
                        ], 
                        name: "occup_area",
                        width: 365
                    },
                    this.getAssignmentField(),
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Publicação *",
                        allowBlank: false,
                        rest: "rh.publicacao.Restful",
                        name: "publicacao"
                    },
                    this.getWorkplaceField(),
                    {
                        allowBlank: false,
                        fieldLabel: "Início *",
                        name: "data_vigencia_inicio",
                        xtype: "datefield"
                    },
                    {
                        allowBlank: true,
                        fieldLabel: "Fim",
                        name: "data_vigencia_fim",
                        xtype: "datefield"
                    },
                    {
                        allowBlank: true,
                        fieldLabel: "Agendamento principal",
                        name: "main_schedule_date",
                        xtype: "datefield"
                    },
                    this.getDesignationField(),
                    this.getProvisoryField(),
                    this.getReasonProvisoryField(),
                    this.getResposibleField(),
                    this.getPartialResposibleField(),
                    {
                        xtype: "hidden",
                        name: "ativo"
                    },
                    this.getOrdinanceField()
                ]
            });
        }
        return this._formPanel;
    },

    getResposibleField: function(cfg){
        if(!this._resposibleField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    boxLabel: "Responsável",
                    fieldLabel: "&nbsp;",
                    labelSeparator: "&nbsp;",
                    allowBlank: true,
                    name: "responsible",
                    listeners: {
                        scope: this,
                        check: function (menu, checked) {
                            if (checked)
                                this.getPartialResposibleField().setValue('');
                        }
                    }
                }
            );
            this._resposibleField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._resposibleField;
    },

    getPartialResposibleField: function(cfg){
        if(!this._partialResposibleField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    boxLabel: "Responsável Parcial",
                    fieldLabel: "&nbsp;",
                    labelSeparator: "&nbsp;",
                    allowBlank: true,
                    name: "partial_responsible",
                    listeners: {
                        scope: this,
                        check: function (menu, checked) {
                            if (checked)
                                this.getResposibleField().setValue('')
                        }
                    }
                }
            );
            this._partialResposibleField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._partialResposibleField;
    },

    getDesignationField: function(cfg){
        if(!this._designationField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    boxLabel: "Designação de Exercício",
                    fieldLabel: "&nbsp;",
                    labelSeparator: "&nbsp;",
                    allowBlank: true,
                    name: "designacao",
                }
            );
            this._designationField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._designationField;
    },

    getProvisoryField: function(cfg){
        if(!this._provisoryField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    xtype: 'checkbox',
                    boxLabel: 'Provisória',
                    fieldLabel: '&nbsp;',
                    labelSeparator: '&nbsp;',
                    allowBlank: true,
                    name: 'provisorio',
                    listeners: {
                        scope: this,
                        check: function (menu, checked) {
                            console.log('teste');
                            if(checked) this.getReasonProvisoryField().show();
                            else{
                                this.getReasonProvisoryField().setValue('');
                                this.getReasonProvisoryField().hide();
                            }
                        }
                    }
                }
            );
            this._provisoryField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._provisoryField;
    },

    getReasonProvisoryField: function (cfg) {
        if (!this._reason) {
            this._reason = Ext._create('toolkit.fields.CKEditor', {
                fieldLabel: 'Motivo/Provisória',
                allowBlank: true,
                height: 50,
                name: 'provisional_reason',
                editorConfig: {
                    toolbarStartupExpanded: false
                },
                submitValue: true,
            });
        }
        return this._reason;
    },

    getElectoralRefusedField: function(cfg){
        if(!this._electoral_refusedField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    xtype: "checkbox",
                    boxLabel: "Declínio Eleitoral",
                    fieldLabel: "&nbsp;",
                    labelSeparator: "&nbsp;",
                    allowBlank: true,
                    name: "electoral_refused"
                }
            );
            this._electoral_refusedField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._electoral_refusedField;
    },

    getCommissionField: function(cfg){
        if(!this._commissionField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    xtype: 'checkbox',
                    boxLabel: 'Comissão',
                    fieldLabel: '&nbsp;',
                    labelSeparator: '&nbsp;',
                    allowBlank: true,
                    name: 'commission',
                    value: false
                }
            );
            this._commissionField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._commissionField;
    },

    getEmployeeField: function(cfg_window, cfg){
        if(!this._employeeField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    fieldLabel: "Servidor *",
                    allowBlank: false,
                    rest: "rh.employee.Restful",
                    name: "servidor",
                }
            );
            this._employeeField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._employeeField;
    },

    getWorkplaceField: function(cfg){
        if(!this._workplaceField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Local *",
                    allowBlank: false,
                    rest: "rh.workplace.Restful",
                    name: "lotacao",
                }
            );
            this._workplaceField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._workplaceField;
    },

    getAssignmentField: function(cfg){
        if(!this._assignmentField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Atribuição",
                    allowBlank: false,
                    rest: "rh.workplace.atribuicao.Restful",
                    name: "atribuicao",
                }
            );
            this._assignmentField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._assignmentField;
    },

    getPossessionField: function(cfg_window, cfg){
        if(!this._possessionField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: "Posse *",
                    allowBlank: false,
                    rest: "rh.movimentacao.possession.AllPossessionsRestful",
                    name: "movimentacao_posse"
                }
            );
            this._possessionField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._possessionField;
    },

    getOrdinanceField: function(cfg_window, cfg){
        if(!this._ordinanceField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    checked: false,
                    boxLabel: 'Por portaria',
                    allowBlank: true,
                    name: 'ordinance',
                    fieldLabel: "&nbsp;",
                    labelSeparator: "&nbsp;",
                }
            );
            this._ordinanceField = Ext._create('Ext.form.Checkbox', cfg);
        }
        return this._ordinanceField;
    },

    getSubstitutionField: function(cfg_window, cfg){
        if(!this._substitutionField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    fieldLabel: 'Substituição',
                    xtype: 'displayfield',
                    name: 'created_by_departure_unicode',
                    hidden: false?cfg_window.values.from_substitution:true
                },
            );
            this._substitutionField = Ext._create('Ext.form.DisplayField', cfg);
        }
        return this._substitutionField;
    }
});


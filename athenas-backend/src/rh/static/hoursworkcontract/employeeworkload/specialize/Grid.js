/** autogenerated **/
/**
 *
 **/
Ext._define('rh.hoursworkcontract.employeeworkload.specialize.Grid', {
    extend: 'rh.hoursworkcontract.employeeworkload.Grid',

    restWindow: 'rh.hoursworkcontract.employeeworkload.specialize.Window',

    configOrderToolBar: ['add', 'edit', 'remove', '-', 'applyEmployeeWorkload', '-', 'search',  '->', 'download'],

    getConfigActionsItems: function(cfg){
        var menu = rh.hoursworkcontract.workload.manager.Grid.superclass.getConfigActionsItems.call(this, cfg);
        var _owner = this;
        menu['applyEmployeeWorkload'] = {
                text: 'Aplicar Escala',
                iconCls: 'icon-rh icon-core-indirect',
                scope: this,
                handler: function(){
                    if(this.params.hours_work_contract_workload){
                        this.windowApplyWorkload({
                            _workhourContract: this.params.hours_work_contract_workload,
                            _owner: this,
                            externalCallback: {fn: function(){
                                _owner.getStore().load();
                            }}
                        }).show();
                    }
                    else Ext.Msg.show({
                        title: this.title,
                        icon: Ext.Msg.INFO,
                        buttons: Ext.Msg.OK,
                        msg: 'Só é possível!'
                    });
                }
        };
        return menu;
    },

    windowApplyWorkload: function(cfg) {
        cfg = core.nullValue(cfg, {});
        this._windowApply = Ext._create('rh.hoursworkcontract.employeeworkload.specialize.WindowApplyWorkload', cfg);
        return this._windowApply;
    },

});

core.RestfulGrid.register(
    'rh.hoursworkcontract.employeeworkload.specialize.Restful',
    'rh.hoursworkcontract.employeeworkload.specialize.Grid'
);


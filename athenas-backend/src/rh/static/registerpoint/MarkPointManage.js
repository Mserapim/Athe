/** autogenerated **/
Ext._define('rh.registerpoint.MarkPointManage', {
    extend: 'toolkit.widget.TabPanel',

    getMarkPointGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.registerpoint.MarkPointGrid', {
                region: 'center',
                gridAutoLoad: true,
                columnAction: false,
                configOrderToolBar: ['search', '->'],
                doubleClickHandler: function () { }
                
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Gestor de Registro de Ponto'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getMarkPointGrid(),
                ]
            }
        );

        rh.registerpoint.MarkPointManage.superclass.constructor.call(this, cfg);
    }
});


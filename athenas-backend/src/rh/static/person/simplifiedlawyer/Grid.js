/** autogenerated **/
/**
 *
 **/
Ext._define('rh.person.simplifiedlawyer.Grid', {
    extend: 'rh.person.simplifiednaturalperson.Grid',

    restWindow: 'rh.person.simplifiedlawyer.Window',

    getColumnItems: function() {
        if(!this._items) {
            this._items = rh.person.simplifiedlawyer.Grid.superclass.getColumnItems.call(this, {});
            this._items = this._items.slice(0,2).concat([
                {header: 'OAB', dataIndex: 'oab', width: 90, sortable: true},
            ]).concat(this._items.slice(2));
        }

        return this._items;
    }
});

core.RestfulGrid.register(
    'rh.person.simplifiedlawyer.Restful',
    'rh.person.simplifiedlawyer.Grid'
);

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.person.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.person.Restful',
    width: 545,

    constructor: function(cfg) {
        cfg = cfg || {};

        Ext.applyIf(cfg, {
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.person(instance.pk);

                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        rh.person.Window.superclass.constructor.call(this, cfg);

        this.person(cfg.oId === undefined ? null : cfg.oId);
    },

    person: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._person = value;

            !prevent && this.observePerson();
        }

        return this._person;
    },

    observePerson: function() {
        value = this.person();

        if(value) {
            this.getAddressGrid().enable();
            this.getAddressGrid().setParam('person', value);
            this.getAddressGrid().setFilterProperty('person', value, 100);

            this.getPhoneGrid().enable();
            this.getPhoneGrid().setParam('person', value);
            this.getPhoneGrid().setFilterProperty('person', value, 100);

        } else {
            this.getAddressGrid().disable();
            this.getAddressGrid().setParam('person', 0);
            this.getAddressGrid().setFilterProperty('person', 0, 100, false);

            this.getPhoneGrid().disable();
            this.getPhoneGrid().setParam('person', 0);
            this.getPhoneGrid().setFilterProperty('person', 0, 100, false);
        }
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                new Ext.TabPanel({
                    activeTab: 0,
                    tabPosition: 'top',
                    border: false,
                    items: [
                        this.getTabMain(cfg),
                        this.getTabContacts(cfg),
                        this.getTabAddress(cfg),
                    ]
                })
            ]
            });

        return this._formPanel;
    },

    getTabMain: function(cfg) {
        if(!this._tabMain)
            this._tabMain = Ext._create('Ext.Panel', {
                layout: 'form',
                title: 'Principal',
                iconCls: 'icon-rh icon-core-main-tab',
                border: false,
                frame: true,
                scope: this,
                autoHeight: true,
                items: [
                   {
                       maxLength: 100,
                       allowBlank: false,
                       fieldLabel: 'Nome',
                       name: 'nome',
                       xtype: 'textfield',
                       width: 400
                   },
                   {
                       xtype: 'checkbox',
                       fieldLabel: '&nbsp;',
                       labelSeparator: '&nbsp;',
                       boxLabel: 'Habilita Protocolo',
                       allowBlank: true,
                       name: 'enable_protocol'
                   },
                ]
            });
        return this._tabMain;
    },

    getTabContacts: function(cfg) {
        if(!this._tabContacts)
            this._tabContacts = Ext._create('Ext.Panel', {
                layout: 'form',
                title: 'Contatos',
                iconCls: 'icon-rh icon-core-contacts-tab',
                border: false,
                frame: true,
                scope: this,
                autoHeight: true,
                items: [
                    this.getPhoneGrid(cfg)
                ]
            });
        return this._tabContacts;
    },


    getAddressGrid: function(cfg) {
        if(!this._addressGrid) {
            this._addressGrid = Ext._create('rh.endereco.EnderecoGrid',{
                hideItemsToolbar: ['search', 'download'],
                region: 'center',
                border: false,
                scope: this,
                height: 265,
                columnAction: false,
            });
        }
        return this._addressGrid;
    },



    getTabAddress: function(cfg) {
        if(!this._tabAddress)
            this._tabAddress = Ext._create('Ext.Panel', {
                layout: 'form',
                title: 'Endereço',
                iconCls: 'icon-rh icon-core-address-tab',
                border: false,
                frame: true,
                scope: this,
                autoHeight: true,
                items: [
                    this.getAddressGrid(cfg)
                ]
            });
        return this._tabAddress;
    },

    getPhoneGrid: function(cfg) {
        if(!this._phoneGrid) {
            this._phoneGrid = Ext._create('rh.telefone.TelefoneGrid',{
                hideItemsToolbar: ['search', 'download'],
                title: 'Telefones',
                region: 'center',
                border: false,
                scope: this,
                height: 185,
                columnAction: false,
            });
        }
        return this._phoneGrid;
    },

});

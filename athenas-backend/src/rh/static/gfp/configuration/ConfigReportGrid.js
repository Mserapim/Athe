/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.configuration.ConfigReportGrid', {
    extend: 'core.RestfulGrid',

    rest: "rh.gfp.configuration.ConfigReportRestful",

    restWindow: 'rh.gfp.configuration.ConfigReportWindow',

    configOrderToolBar: ['add', 'edit', 'remove', '-','includeGaeco','excludeGaeco', '-', 'search'],

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Descricao', dataIndex: 'unicode', id: 'autoExpandColumn',hidden: true},
                    {header: 'Seção', dataIndex: 'section_display', width: 200},
                    {header: 'Tipo', dataIndex: 'type_event_display', width: 120},
                    {header: 'Natureza', dataIndex: 'nature', width: 90},
                    {header: 'Subelemento', dataIndex: 'subelement', width: 90},
                    {header: 'Credor', dataIndex: 'creditor', width: 90},
                    {header: 'Texto', dataIndex: 'text', width: 300},
                    {header: 'Tipo Repasse', dataIndex: 'transfer_type_display', width: 120},
                    {header: 'Tipo Formula', dataIndex: 'type_formula_display', width: 120},
                    {header: 'Tipo Relatório', dataIndex: 'type_report_display', width: 120},
                    {header: 'Categoria', dataIndex: 'type_by_possession', width: 120},
                    // {header: 'Incluir Matricula', dataIndex: 'include_registration', width: 90},
                    // {header: 'Excluir Matricula', dataIndex: 'exclude_registration', width: 90},
                ]
            );

        return this._columnModel;
    },

    getIncludeGaecoAction:function(){
        return{
            text: 'Incluir Matriculas Gaeco',
            scope: this,
            iconCls: true,
            icon: '/' + global.Context + '/static/rh/images/arrow-up-double.png',
            handler: function () {
                if (this.getSelectionModel().getSelected()) {
                    var cod = this.getSelectionModel().getSelected().id
                    Ext.Msg.show({
                        title:"Incluir Matriculas",
                        icon: Ext.Msg.QUESTION,
                        buttons: Ext.Msg.YESNO,
                        msg: 'Deseja mesmo incluir matriculas no relatório os servidores marcados com empenho Gaeco?',
                        scope: this,
            
                        fn: function (btn) {
                            if (btn == 'no') return;
                            var params = {
                                actionCustom: "include_register",
                                pk: cod,
                            };
                            this._process(params);
                        }
                    });

                } else {
                    Ext.Msg.show({
                        title: "Incluir",
                        msg: "Selecione uma configuração!",
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                    });
                }
            }
        }
    
    },

    getExcludeGaecoAction:function(){
        return{
            text: 'Excluir Matriculas Gaeco',
            scope: this,
            iconCls: true,
            icon: '/' + global.Context + '/static/rh/images/list-remove.png',
            handler: function () {
                if (this.getSelectionModel().getSelected()) {
                    var cod = this.getSelectionModel().getSelected().id
                    Ext.Msg.show({
                        title:"Excluir Matriculas",
                        icon: Ext.Msg.QUESTION,
                        buttons: Ext.Msg.YESNO,
                        msg: 'Deseja mesmo excluir matriculas no relatório os servidores marcados com empenho Gaeco?',
                        scope: this,
                        fn: function (btn) {
                            if (btn == 'no') return;
                            var params = {
                                actionCustom: "exclude_register",
                                pk: cod,
                            };
                            this._process(params);
                        }
                    });
                   
                } else {
                    Ext.Msg.show({
                        title: "Excluir",
                        msg: "Selecione uma configuração!",
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                    });
                }
            }
        }
    
    },

    _process: function (params) {
        var rest = Ext._create(this.rest, {});
        var mask = Ext._create("Ext.LoadMask", this.getEl(), { msg: "Processando informações." });
        var wnd = this;


        mask.show();
        rest._process(
            params,
            {
                scope: this,
                fn: function (rst) {
                    core.invokeCallback(wnd.externalCallback || { fn: Ext.emptyFn }, rst.message);
                    this.getStore().reload()
                },
            },
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: "Informando",
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: message,
                    });
                },
            },
            {
                fn: function () {
                    mask.hide();
                    Ext.Msg.show({
                        title: "Informando",
                        icon: Ext.Msg.INFO,
                        buttons: Ext.Msg.OK,
                        msg: 'Procedimento realizado com sucesso.',
                    });
                },
               
            }
        );
    },
});






core.RestfulGrid.register(
    'rh.gfp.configuration.ConfigReportRestful',
    'rh.gfp.configuration.ConfigReportGrid'
);


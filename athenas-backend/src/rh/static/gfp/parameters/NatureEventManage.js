/** autogenerated **/
Ext._define('rh.gfp.parameters.NatureEventManage', {
    extend: 'toolkit.widget.TabPanel',

    getNatureEventGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.gfp.parameters.NatureEventGrid', {
                region: 'center',
                gridAutoLoad: true
            });
            this._grid.setFilterProperty('active', true, 1011);
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Natureza das Rubricas (eSocial)'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getNatureEventGrid(),
                ]
            }
        );

        rh.gfp.parameters.NatureEventManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.payroll.PayrollTypeRestful', {
    extend: 'core.Restful',

    resource: 'GFPPayrollType',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = rh.gfp.payroll.PayrollTypeRestful.superclass.getFields.call(this, cfg).concat([
                {type: "float",name: "margem",useNull: true},
                {type: "string",name: "abreviatura"},
                {type: "string",name: "titulo"},
                {type: "int",name: "modelo",useNull: true},
                {type: "string",name: "modelo_unicode"},
                {type: "int",name: "publicacao_processo",useNull: true},
                {type: "string",name: "publicacao_processo_unicode"},
                {type: "string",name: "processo"},
                {type: "int",name: "carater",useNull: true},
                {type: "string",name: "carater_display"},
                {type: "bool",name: "ativo"},
                {type: "bool",name: "principal"},
                {type: "string",name: "numero"},
            ]);

        return this._fields;
    },

    _process: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            params.actionCustom,
            null,
            'POST',
            {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if (rst.success) {
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    } else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    }
});

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.payroll.SpecieEventGrid', {
    extend: 'core.RestfulGrid',

    rest: 'rh.gfp.payroll.SpecieEventRestful',
    restWindow: 'rh.gfp.payroll.SpecieEventWindow',

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Número', dataIndex: 'specie_number', width: 60},
                    {header: 'Título', dataIndex: 'title', id: 'autoExpandColumn'},
                    {header: 'Inverter Tipo', dataIndex: 'invert_type', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }},
                    {header: 'Concatenar Nome?', dataIndex: 'concatenate_name', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }},
                ]
            );

        return this._columnModel;
    },

    // viewConfig: {
    //     scope: this,
    //     getRowClass: function(record, index) {

    //         var selected_genre = this.grid.genreGrid.getSelectionModel().getSelected();

    //         if(selected_genre){

    //             var index = this.grid.eventGrid.getStore().findExact('specie_event', record.get('pk'));
    //             if(index >= 0){
    //                 return 'x-grid3-unabled';
    //             }
    //         }
    //     }
    // },

});

core.RestfulGrid.register(
    'rh.gfp.payroll.SpecieEventRestful',
    'rh.gfp.payroll.SpecieEventGrid'
);


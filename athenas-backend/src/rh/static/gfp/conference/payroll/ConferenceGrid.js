/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.conference.payroll.ConferenceGrid', {
    extend: 'core.RestfulGrid',

    
    rest: "rh.gfp.conference.payroll.ConferenceRestful",

    restWindow: 'rh.gfp.conference.payroll.ConferenceWindow',

    hideActions: ['edit'],


    configOrderToolBar: ['add','remove','finish', '-','search', '->', 'download'],


    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Descricao', dataIndex: 'unicode',hidden: true},
                    {header: 'Folha', dataIndex: 'payroll_unicode', id: 'autoExpandColumn', width: 120},
                    {header: 'Criado Por', dataIndex: 'created_by_unicode', width: 300},
                    {header: 'Criado Em', dataIndex: 'created_at', width: 150, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Finalizado Por', dataIndex: 'finished_by_unicode', width: 300},
                    {header: 'Finalizado Em', dataIndex: 'finished_at', width: 150, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: '%', dataIndex: 'payroll_pendencies', width: 90, renderer: toolkit.util.formatPercent},

                ]
            );


        return this._columnModel;
    },

    getFinishAction:function(){
        return{
            text: 'Finalizar',
            scope: this,
            iconCls: true,
            icon: '/' + global.Context + '/static/rh/images/list-remove.png',
            handler: this._finish
        }
        
    },

    _finish: function () {
        if (!this.getSelectionModel().getSelected()) {
            Ext.Msg.show({
                title: this.title,
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: "Selecione um item.",
            });
        } else {
            Ext.Msg.show({
                title: this.title,
                icon: Ext.Msg.QUESTION,
                buttons: Ext.Msg.YESNO,
                msg: "Finalizar Conferência selecionada?",
                scope: this,

                fn: function (btn) {
                    if (btn == "no") return;
                    var sel = this.getSelectionModel().getSelected();
                    var params = {
                        actionCustom: "finish",
                        pk: sel.id,
                    };

                    this._process(params);
                },
            });
        }
    },

    defaultClickFunction: function() {
       
    },

    _process: function (params) {
        var rest = Ext._create(this.rest, {});
        var mask = Ext._create("Ext.LoadMask", this.getEl(), { msg: "Processando informações." });
        var wnd = this;


        mask.show();
        rest._process(
            params,
            {
                scope: this,
                fn: function (rst) {
                    core.invokeCallback(wnd.externalCallback || { fn: Ext.emptyFn }, rst.message);
                    this.getStore().reload()
                },
            },
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: "Informando",
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: message,
                    });
                },
            },
            {
                fn: function () {
                    mask.hide();
                },
            }
        );
    },


   
});

core.RestfulGrid.register(
    'rh.gfp.conference.payroll.ConferenceRestful',
    'rh.gfp.conference.payroll.ConferenceGrid'
);


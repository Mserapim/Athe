/** autogenerated **/
Ext._define('rh.gfp.progression.horizontal_config.Manage', {
    extend: 'toolkit.widget.TabPanel',

    Grid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.gfp.progression.horizontal_config.Grid', {
                region: 'center',
                // gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Configuração de Progressão Horizontal'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.Grid(),
                ]
            }
        );

        rh.gfp.progression.horizontal_config.Manage.superclass.constructor.call(this, cfg);
    }
});


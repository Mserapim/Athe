/** autogenerated **/
Ext._define('rh.gfp.vacation.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getVacationGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.gfp.vacation.Grid', {
                region: 'center',
                gridAutoLoad: true
                
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Gestor de Gratificações a Abonos'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getVacationGrid(),
                ]
            }
        );

        rh.gfp.vacation.Manage.superclass.constructor.call(this, cfg);
    }
});


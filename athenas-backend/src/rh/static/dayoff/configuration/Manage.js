/** autogenerated **/
Ext._define('rh.dayoff.configuration.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getConfigurationGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.configuration.Grid', {
                region: 'center'
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Configuração de Usufrutos e Folgas'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getConfigurationGrid(),
                ]
            }
        );

        rh.dayoff.configuration.Manage.superclass.constructor.call(this, cfg);
    }
});


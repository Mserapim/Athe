/** autogenerated **/
Ext._define('rh.dayoff.mpmt.payment_vacation.vacation.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getVacationGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.mpmt.payment_vacation.vacation.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Conferência de Pagamentos de Férias'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getVacationGrid(),
                ]
            }
        );

        rh.dayoff.mpmt.payment_vacation.vacation.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('rh.dayoff.mpmt.activity.sell.Manage', {
    extend: 'rh.dayoff.mpmt.activity.Manage',

    getActivityGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.mpmt.activity.Grid', {
                resource: 'DAYOFFSellActivity',
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Atividades de Venda'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getActivityGrid(),
                ]
            }
        );

        rh.dayoff.mpmt.activity.sell.Manage.superclass.constructor.call(this, cfg);
    }
});


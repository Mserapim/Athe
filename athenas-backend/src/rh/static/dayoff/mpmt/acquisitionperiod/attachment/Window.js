/** autogenerated **/
/**
 *
 **/
Ext._define('rh.dayoff.mpmt.acquisitionperiod.attachment.Window', {
    extend: 'core.RestfulWindow',
    rest: 'rh.dayoff.mpmt.acquisitionperiod.attachment.Restful',

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(cfg, {
            width: 390,
            height: 310,
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });
        rh.dayoff.mpmt.acquisitionperiod.attachment.Window.superclass.constructor.call(this, cfg);
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                frame: true,
                border: false,
                height: 290,
                items: [
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Servidor",
                        allowBlank: true,
                        rest: "rh.dayoff.mpmt.acquisitionperiod.Restful",
                        name: "acquisition_period",
                        readOnly: true
                    },
                    {
                        xtype: 'textfield',
                        name: 'description',
                        fieldLabel: 'Descrição',
                        allowBlank: true,
                        width: 250,
                    },
                    {
                        xtype: 'textfield',
                        name: 'information',
                        fieldLabel: 'Informação',
                        allowBlank: true,
                        width: 250,
                    },
                    {
                        xtype: 'datefield',
                        name: 'date_start',
                        fieldLabel: 'Data início',
                        allowBlank: false,
                    },
                    {
                        xtype: 'datefield',
                        name: 'date_end',
                        fieldLabel: 'Data fim',
                        allowBlank: false,
                    },
                    {
                        xtype: 'numberfield',
                        name: 'days_law',
                        fieldLabel: 'Quantidade de dias direito',
                        allowBlank: false,
                        allowDecimals: true,
                        width: 250,
                    },
                    {
                        fieldLabel: 'Status',
                        xtype: 'choicefield',
                        hiddenName: 'status',
                        choiceId: 'dayoff.DAYOFF_ACQUISITION_PERIOD_ATTACHMENT_STATUS',
                        allowBlank: true
                    },
                    this.getAttachmentField(),
                ]
            });
        return this._formPanel;
    },

    getAttachmentField: function () {
        if (!this._attachmentField)
            this._attachmentField = Ext._create('Ext.Panel', {
                border: false,
                frame: false,
                layout: 'form',
                items: [
                    this.getAttachmentButton(),
                    this.getAttachment(),
                ]
            });

        return this._attachmentField;
    },

    getAttachment: function (v) {
        if (!this._attachment) {
            this._attachment = Ext._create('Ext.form.TextField', {
                fieldLabel: 'Anexo',
                name: 'attachment',
                allowBlank: true,
                hidden: true
            });
        }
        return this._attachment;
    },

    getAttachmentButton: function () {
        if (!this._attachmentButton) {
            this._attachmentButton = Ext._create('Ext.Button', {
                text: 'Visualizar Anexo',
                fieldLabel: 'Anexo',
                anchor: '50%',
                scope: this,
                handler: this.getAttachmentWindow,
            });
        }
        return this._attachmentButton;
    },

    getAttachmentWindow: function () {
        attachment_value = this.getFormPanel().getForm().findField('attachment').getValue()
        if (attachment_value === undefined || attachment_value === "") {
            action = 'create'
            oId_value = null
        }
        else {
            action = 'update';
            oId_value = attachment_value;
        }
        Ext._create('rh.dayoff.attachment.Window', {
            title: 'Anexo',
            oId: oId_value,
            action: action,
            values: 'remote',
            callback: {
                success: {
                    scope: this,
                    fn: function (instance) {
                        this.getFormPanel().getForm().findField('attachment').setValue(instance.pk)
                    }
                }
            }
        }).show();
    },
});


/** autogenerated **/
Ext._define('rh.dayoff.groupperiod.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getGroupAcquisitionPeriodGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.groupperiod.Grid', {
                region: 'center',
            });
        }

        return this._grid;
    },

    getGrids: function () {
        return [
            this.getGroupAcquisitionPeriodGrid()
        ]
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Grupo - Periodo de Aquisição'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getGrids(),
                ]
            }
        );

        rh.dayoff.groupperiod.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.dayoff.activity.Restful', {
    extend: 'core.Restful',

    resource: 'DAYOFFActivity',

    getFields: function (cfg) {
        if (!this._fields)
            this._fields = rh.dayoff.activity.Restful.superclass.getFields.call(this, cfg).concat([
                { type: 'int', name: 'status', useNull: true },
                { type: 'string', name: 'status_display' },
                { type: 'string', name: 'status_next_display' },
                { type: 'date', name: 'immediate_authorization_at', dateFormat: 'd/m/Y H:i' },
                { type: 'int', name: 'mediate_authorization_by', useNull: true },
                { type: 'string', name: 'mediate_authorization_by_unicode' },
                { type: 'int', name: 'attachment', useNull: true },
                { type: 'string', name: 'attachment_unicode' },
                { type: 'string', name: 'justification' },
                { type: 'int', name: 'immediate_authorization_by', useNull: true },
                { type: 'string', name: 'immediate_authorization_by_unicode' },
                { type: 'date', name: 'created_at', dateFormat: 'd/m/Y H:i' },
                { type: 'int', name: 'created_by', useNull: true },
                { type: 'string', name: 'created_by_unicode' },
                { type: 'date', name: 'modified_at', dateFormat: 'd/m/Y H:i' },
                { type: 'string', name: 'modified_by_unicode' },
                { type: 'int', name: 'modified_by', useNull: true },
                { type: 'int', name: 'acquisition_period', useNull: true },
                { type: 'int', name: 'days_left_cache', useNull: true },
                { type: 'int', name: 'days_in_cache', useNull: true },
                { type: 'int', name: 'days_out_cache', useNull: true },
                { type: 'string', name: 'acquisition_period_unicode' },
                { type: 'string', name: 'employee_unicode' },
                { type: 'int', name: 'type_of_activity' },
                { type: 'string', name: 'type_of_activity_display' },
                { type: 'string', name: 'acquisition_period_title' },
                { type: 'date', name: 'mediate_authorization_at', dateFormat: 'd/m/Y H:i' },
                { type: 'int', name: 'type_of_usufruct' },
                { type: 'string', name: 'type_of_usufruct_display' },
                { type: 'string', name: 'booked_usufructs_display' },
                { type: 'string', name: 'modifieds_usufructs_display' },
                { type: 'date', name: 'admin_authorization_at', dateFormat: 'd/m/Y H:i' },
                { type: 'int', name: 'admin_authorization_by', useNull: true },
                { type: 'string', name: 'admin_authorization_by_unicode' },
                { type: 'string', name: 'employee_admin_authorization_by' },
                { type: 'int', name: 'homologation_by', useNull: true },
                { type: 'string', name: 'homologation_by_unicode' },
                { type: 'date', name: 'homologation_at', dateFormat: 'd/m/Y H:i' },
                { type: 'string', name: 'action_custom' },
                { type: 'string', name: 'booked_days_cache' },
                { type: 'string', name: 'days_to_enjoy_cache' },
                { type: 'string', name: 'days_not_booked_cache' },
                { type: 'string', name: 'chief_immediate_unicode' },
                { type: 'string', name: 'employee_workplace_unicode' },
                { type: 'string', name: 'group_period' },
            ]);

        return this._fields;
    },

    _process: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            params.actionCustom,
            null,
            'POST',
            {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    if (rst.success) {
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    }
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },

    _update_activity: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            'update_activity',
            null,
            'POST',
            {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    if (rst.success) {
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    }
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },
});

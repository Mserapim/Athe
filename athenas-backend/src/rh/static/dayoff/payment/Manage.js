/** autogenerated **/
Ext._define('rh.dayoff.payment.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getPaymentGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.dayoff.payment.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Payment'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getPaymentGrid(),
                ]
            }
        );

        rh.dayoff.payment.Manage.superclass.constructor.call(this, cfg);
    }
});


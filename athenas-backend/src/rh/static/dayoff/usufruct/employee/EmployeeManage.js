/** autogenerated **/
Ext._define('rh.dayoff.usufruct.employee.EmployeeManage', {
    extend: 'rh.dayoff.usufruct.Manage',

    getUsufructGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.usufruct.employee.EmployeeGrid', {
                region: 'center',
                gridAutoLoad: true,
                doubleClickHandler: function () { }
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Usufrutos Servidor'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getUsufructGrid(),
                ]
            }
        );

        rh.dayoff.usufruct.employee.EmployeeManage.superclass.constructor.call(this, cfg);
    }
});


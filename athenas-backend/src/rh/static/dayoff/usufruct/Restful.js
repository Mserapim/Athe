/** autogenerated **/
/**
 *
 **/
Ext._define('rh.dayoff.usufruct.Restful', {
    extend: 'core.Restful',

    resource: 'DAYOFFUsufruct',

    getFields: function (cfg) {
        if (!this._fields)
            this._fields = rh.dayoff.usufruct.Restful.superclass.getFields.call(this, cfg).concat([
                { name: 'icons', type: 'auto' },
                { name: 'created_by', type: 'int', useNull: true },
                { name: 'created_by_unicode', type: 'string' },
                { name: 'modified_by', type: 'int', useNull: true },
                { name: 'modified_by_unicode', type: 'string' },
                { name: 'created_at', type: 'date', dateFormat: 'd/m/Y H:i' },
                { name: 'modified_at', type: 'date', dateFormat: 'd/m/Y H:i' },
                { name: 'status', type: 'int', useNull: true },
                { name: 'status_display', type: 'string' },
                { name: 'start_date', type: 'date', dateFormat: 'd/m/Y' },
                { name: 'end_date', type: 'date', dateFormat: 'd/m/Y' },
                { name: 'end_date_extected', type: 'date', dateFormat: 'd/m/Y' },
                { name: 'days', type: 'int', useNull: true },
                { name: 'from_scale', type: 'bool' },
                { name: 'activity', type: 'int', useNull: true },
                { name: 'activity_unicode', type: 'string' },
                { name: 'employee_unicode', type: 'string' },
                { name: 'authorized_by_unicode', type: 'string' },
                { name: 'authorized_at', type: 'date', dateFormat: 'd/m/Y H:i' },
            ]);

        return this._fields;
    },

    _process: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            params.actionCustom,
            null,
            'POST',
            {
                params: Ext.encode(params),
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if (rst.success)
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },

    getConflictData: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            'get_conflict_data',
            null,
            'GET',
            {
                params: Ext.encode(params),
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if (rst.success)
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },
});

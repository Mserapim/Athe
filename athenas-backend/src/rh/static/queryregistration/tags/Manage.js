/** autogenerated **/
Ext._define('rh.queryregistration.tags.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getTagsGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.queryregistration.tags.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Tags'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getTagsGrid(),
                ]
            }
        );

        rh.queryregistration.tags.Manage.superclass.constructor.call(this, cfg);
    }
});

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.bank.BankGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.bank.BankWindow',

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    {header: 'Chave', dataIndex: 'id', width: 50},
                    {header: 'Nome', dataIndex: 'nome', id: 'autoExpandColumn'},
                    {header: 'Número', dataIndex: 'numero', width: 90},
                    {header: 'Sigla', dataIndex: 'sigla', width: 90},
                    {header: 'Tem Convênio?', dataIndex: 'tem_convenio_display', width: 120},
                    {header: 'Número Convênio', dataIndex: 'numero_convenio', width: 90},
                    {header: 'Agência', dataIndex: 'agencia', width: 90},
                    {header: 'DV Agência', dataIndex: 'dv_agencia', width: 90},
                    {header: 'Conta', dataIndex: 'conta', width: 90},
                    {header: 'DV Conta', dataIndex: 'dv_conta', width: 90},
                    {header: 'Descrição', dataIndex: 'descricao', width: 90}
                ]
            );

        return this._columnModel;
    },

    'getFilterMenu': function() {
        return [
            {
                'text': 'Com Convênio',
                'checked': false,
                'hideOnClick': false,
                'scope': this,
                'handler': this.filterConvenio
            },
            {
                'text': 'Com Conta',
                'checked': true,
                'hideOnClick': false,
                'scope': this,
                'handler': this.filterConta
            }
        ];
    },

    'filterConvenio': function() {
        this._filterConvenio = core.nullValue(this._filterConvenio, 1);

        if(this._filterConvenio == 1) {
            this.setFilterProperty('numero_convenio__isnull', false, 1011);
            this._filterConvenio = 2;
        }
        else {
            this.removeFilterProperty('numero_convenio__isnull', 1011);
            this._filterConvenio = 1;
        }
    },
    
    'filterConta': function() {
        this._filterConta = core.nullValue(this._filterConta, 1);

        if(this._filterConta == 1) {
            this.setFilterProperty('conta__gt','', 1011);
            this._filterConta = 2;
        }
        else {
            this.removeFilterProperty('conta__gt', 1011);
            this._filterConta = 1;
        }
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            gridAutoLoad: true
        });
        rh.bank.BankGrid.superclass.constructor.call(this, cfg);
        this.filterConta()
    },

});


core.RestfulGrid.register(
    'rh.bank.BankRestful',
    'rh.bank.BankGrid'
);

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.ferias.pas.EmployeeAcquisitionPeriodRestful', {
    extend: 'core.Restful',

    resource: 'FRSEmployeeAcquisitionPeriod',

    getFields: function (cfg) {
        if (!this._fields)
            this._fields = rh.ferias.pas.EmployeeAcquisitionPeriodRestful.superclass.getFields.call(this, cfg).concat([
                { type: 'date', name: 'data_fim_usufruto', dateFormat: 'd/m/Y' },
                { type: 'int', name: 'modified_by', useNull: true },
                { type: 'string', name: 'modified_by_unicode' },
                { type: 'int', name: 'quantidade_dias', useNull: true },
                { type: 'date', name: 'data_inicio_usufruto', dateFormat: 'd/m/Y' },
                { type: 'date', name: 'created_at', dateFormat: 'd/m/Y H:i' },
                { type: 'bool', name: 'bloqueado' },
                { type: 'date', name: 'modified_at', dateFormat: 'd/m/Y H:i' },
                { type: 'date', name: 'data_fim_aquisicao', dateFormat: 'd/m/Y' },
                { type: 'int', name: 'created_by', useNull: true },
                { type: 'string', name: 'created_by_unicode' },
                { type: 'date', name: 'data_inicio_aquisicao', dateFormat: 'd/m/Y' },
                { type: 'int', name: 'servidor', useNull: true },
                { type: 'string', name: 'servidor_unicode' },
                { type: 'int', name: 'folha_evento_terco_constitucional', useNull: true },
                { type: 'string', name: 'folha_evento_terco_constitucional_unicode' },
                { type: 'int', name: 'content_type', useNull: true },
                { type: 'string', name: 'content_type_unicode' },
                { type: 'bool', name: 'pago_sem_folha' },
                { type: 'date', name: 'data_referencia', dateFormat: 'd/m/Y' },
                { type: 'int', name: 'periodo_aquisitivo', useNull: true },
                { type: 'string', name: 'periodo_aquisitivo_unicode' },
                { type: 'int', name: 'estado', useNull: true },
                { type: 'string', name: 'estado_display' },
                { type: 'int', name: 'paid_days', useNull: true },
                { name: 'status' },
                { type: 'int', name: 'dias_marcados', useNull: true },
                { type: 'int', name: 'dias_agendados', useNull: true },
                { type: 'int', name: 'dias_usufruidos', useNull: true },
                { type: 'int', name: 'dias_ausufruir', useNull: true },
                { type: 'int', name: 'dias_nao_marcados', useNull: true },
                { type: 'date', name: 'usufruto_ini', dateFormat: 'd/m/Y' },
                { type: 'date', name: 'usufruto_fim', dateFormat: 'd/m/Y' },
                { type: '', name: 'situacao' },
            ]);

        return this._fields;
    },

    createAutomaticBookVacation: function (pa, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                var msg = 'Acompanhe pelas tarefas do sistema com o texto: Férias - Marcando férias de períodos pendentes.';
                if (message)
                    message += msg;
                else
                    message = msg;
                Ext.Msg.show({
                    width: 400,
                    title: 'Marcando em lote!',
                    icon: Ext.Msg.INFO,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            'create_automatic_book_vacation',
            null,
            'PUT',
            {
                params: { pa: pa },
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if (rst.success)
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },
});

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.officehoursworkplace.Grid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.officehoursworkplace.Window',

    configOrderToolBar: ['add', 'edit', 'remove', '-', 'applyAll', '-', 'search', '->', 'download'],


    getColumnModel: function () {
        if (!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    { header: 'Descrição', dataIndex: 'description', id: 'autoExpandColumn', sortable: true, hidden: false },
                    { header: 'Criado por', dataIndex: 'created_by_unicode', width: 120, sortable: true, hidden: true },
                    { header: 'Criado em', dataIndex: 'created_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i'), sortable: true, hidden: true },
                    { header: 'Modificado por', dataIndex: 'modified_by_unicode', width: 120, sortable: true, hidden: true },
                    { header: 'Modificado em', dataIndex: 'modified_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i'), sortable: true, hidden: true }
                ]
            );

        return this._columnModel;
    },

    getApplyAllAction: function (cfg) {
        if (!this._appyAllButton)
            this._appyAllButton = Ext._create('Ext.Button', {
                text: 'Aplicar para todas Lotações',
                scope: this,
                iconCls: 'icon-core icon-core-add-all',
                handler: this.execApplyForAll
            });

        return this._appyAllButton;
    },

    execApplyForAll: function () {
        var selected = this.getSelectionModel().getSelected();

        if (selected) {
            Ext.Msg.show({
                title: 'Aplicação de horários de expediente',
                msg: 'Deseja aplicar este horário de expediente a todas as lotações?',
                icon: Ext.Msg.QUESTION,
                buttons: Ext.Msg.OKCANCEL,
                scope: this,
                fn: function (btn) {
                    if (btn == 'cancel')
                        return;
                    else {
                        var rest = this.factoryRestful();
                        var mask = new Ext.LoadMask(this.getEl(), { msg: 'Processando...' });
                        mask.show();

                        rest.doRequest(
                            rest.getRoute(
                                'apply_all',
                                false,
                                'POST', {
                                scope: this,
                                params: {
                                    office_hours_pk: selected.id
                                },
                                callback: function () {
                                    mask.hide();
                                    mask = undefined;
                                },
                                success: function (xhr) {
                                    var rst = Ext.decode(xhr.responseText);

                                    if (rst.success) {
                                        Ext.Msg.show({
                                            title: 'Atenção',
                                            icon: Ext.Msg.INFO,
                                            buttons: Ext.Msg.OK,
                                            msg: rst.message
                                        });
                                    }
                                    else
                                        Ext.Msg.show({
                                            title: 'Atenção',
                                            icon: Ext.Msg.ERROR,
                                            buttons: Ext.Msg.OK,
                                            msg: rst.message
                                        });
                                },
                                failure: function (xhr) {
                                    Ext.Msg.show({
                                        title: 'Atenção',
                                        icon: Ext.Msg.ERROR,
                                        buttons: Ext.Msg.OK,
                                        msg: 'Recurso indisponível no momento.'
                                    });
                                },

                            }
                            )
                        );
                    }
                }
            });
        } else {
            Ext.Msg.show({
                title: 'Aplicação de horários de expediente',
                icon: Ext.Msg.WARNING,
                buttons: Ext.Msg.OK,
                msg: 'Selecione ao menos um registro para realizar a atualização'
            });
        }
    },
});

core.RestfulGrid.register(
    'rh.officehoursworkplace.Restful',
    'rh.officehoursworkplace.Grid'
);


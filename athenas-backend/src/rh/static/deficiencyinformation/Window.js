/** autogenerated **/
/**
 *
 **/
Ext._define('rh.deficiencyinformation.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.deficiencyinformation.Restful',
    width: 535,

    constructor: function(cfg) {
        cfg = cfg || {};
        rh.deficiencyinformation.Window.superclass.constructor.call(this, cfg);
        this._observe();
    },

    _observe: function() {
        var values = this.getParams();
        if(values.naturalperson)
            this.getNaturalPersonField().setReadOnly(true);
        else
            this.getNaturalPersonField().setReadOnly(false);
    },

    getNaturalPersonField: function(cfg){
        if(!this._naturalPersonField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Pessoa Física',
                    allowBlank: true,
                    rest: 'rh.person.naturalperson.Restful',
                    name: 'naturalperson',
                }
            );
            this._naturalPersonField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._naturalPersonField;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.getNaturalPersonField(),
                    {
                        xtype: "checkbox",
                        boxLabel: "Reabilitado",
                        allowBlank: true,
                        hideLabel: true,
                        name: "rehabilitation",
                        checked: false,
                    },
                    {
                        xtype: "checkbox",
                        boxLabel: "Preenche cota",
                        allowBlank: true,
                        hideLabel: true,
                        name: "quota",
                        checked: false,
                    },
                    {
                        maxLength: 2000,
                        allowBlank: true,
                        fieldLabel: "Observação",
                        name: "note",
                        xtype: "ckeditor",
                        width: 400
                    }
            ]
            });

        return this._formPanel;
    }
});


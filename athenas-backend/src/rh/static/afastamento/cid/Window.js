/** autogenerated **/
/**
 *
 **/
Ext._define('rh.afastamento.cid.Window', {
    extend: 'core.RestfulWindow',
    rest: 'rh.afastamento.cid.Restful',

    width: 550,

    constructor: function(cfg) {
        rh.afastamento.cid.Window.superclass.constructor.call(this, cfg);
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                height: 500,
                border: false,
                activeTab: 0,
                deferredRender: false,
                items: [
                    this.getPublicConcurrencePanel(cfg),
                    this.getCidCodes(cfg)
                ]
            });

        return this._tabPanel;
    },

    getCidCodes: function (cfg) {
        if (!this._jobPositionsPanel)
            this._jobPositionsPanel = Ext._create('core.fields.RelatedRestfulField', {
                title: 'Códigos de Cid-10',
                name: 'cid_code',
                relatedname: 'public_concurrence',
                displayField: 'unicode',
                width: 540,
                height: 500,
                rest: this.rest,
                disabled: false,
                sourceRest: 'rh.afastamento.cid.code.Restful',
                oId: cfg.oId,
                gridConfig: {
                    columnAction: false,
                    configOrderToolBar: ['search', '->'],
                }
            });

        return this._jobPositionsPanel;
    },

    
    getPublicConcurrencePanel: function(cfg) {
        if(!this._publicPanel)
            this._publicPanel = Ext._create("Ext.Panel", {
                frame: true,
                border: false,
                defaults: {
                    width: 400,
                },
                title: "Geral",
                layout: "form",
                items: [
                    {
                        maxLength: 50,
                        allowBlank: false,
                        fieldLabel: "Capítulo",
                        name: "chapter",
                        xtype: "textfield"
                    },
                    {
                        maxLength: 50,
                        allowBlank: false,
                        fieldLabel: "Código",
                        name: "code",
                        xtype: "textfield"
                    },
                    {
                        maxLength: 244,
                        allowBlank: false,
                        fieldLabel: "Descrição",
                        name: "description",
                        xtype: "textfield"
                    }
                ]
            });

        return this._publicPanel;
    },


    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: this.getTabPanel(cfg),
            });

        return this._formPanel;
    },

});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.afastamento.ManagerGrid', {
    extend: 'rh.afastamento.baselicencaafastamento.Grid',

    restWindow: 'rh.afastamento.ManagerWindow',

    statics: {
        types: [],
        menu_structure: [],
        filter_structure: [],

        register: function (name, label, iconCls, Class, groupMenu, checked, value) {
            rh.afastamento.ManagerGrid.types.push({
                name: name,
                label: label,
                iconCls: iconCls,
                Class: Class,
                groupMenu: groupMenu,
                checked: checked,
                value: value
            });
        },

        getClassByName: function (name) {
            var Class = false;
            rh.afastamento.ManagerGrid.types.forEach(
                function (item) {
                    if (item.name == name) {
                        Class = item.Class;
                        return false;
                    }
                }
            );
            return Class;
        },

        getGroupMenu: function (menu, name) {
            var groupMenu;
            menu.forEach(
                function (item) {
                    if (item.name == name) {
                        groupMenu = item;
                        return false;
                    }
                }
            );
            return groupMenu;
        },

        getMenuFilterStructure: function (scope, department) {
            if (!scope._menuFilterStructure) {
                rh.afastamento.ManagerGrid.types.forEach(
                    function (item) {
                        if (item.Class === '') {
                            rh.afastamento.ManagerGrid.filter_structure.push({
                                name: item.name,
                                groupMenu: item.groupMenu,
                                text: item.label,
                                scope: scope,
                                iconCls: item.iconCls,
                                menu: []
                            });
                        } else {
                            rh.afastamento.ManagerGrid.filter_structure.push(
                                new Ext.menu.CheckItem({
                                    groupMenu: item.groupMenu,
                                    inherited: true,
                                    name: item.name,
                                    text: item.label,
                                    checked: item.checked,
                                    value: item.value,
                                    scope: scope,
                                    hideOnClick: false,
                                })
                            );
                        }
                    }
                );

                rh.afastamento.ManagerGrid.filter_structure.push(
                    new Ext.menu.CheckItem({
                        groupMenu: 'licenca',
                        inherited: true,
                        name: 'licencadoencapessoafamiliajuntamedica',
                        text: "Doença em pessoa da família junta médica",
                        checked: true,
                        value: 64,
                        scope: scope,
                        hideOnClick: false,
                    })
                );

                scope._menuFilterStructure = [
                    {
                        name: 'selection',
                        groupMenu: '',
                        text: 'Selecionar',
                        scope: scope,
                        menu: [
                            {
                                name: 'all',
                                groupMenu: 'selection',
                                text: 'Todos',
                                scope: scope,
                                hideOnClick: false,
                                handler: function () {
                                    this.markFilter(rh.afastamento.ManagerGrid.getMenuFilterStructure(this, department), 'all');
                                },
                            },
                            {
                                name: 'none',
                                groupMenu: 'selection',
                                text: 'Nenhum',
                                scope: scope,
                                hideOnClick: false,
                                handler: function () {
                                    this.markFilter(rh.afastamento.ManagerGrid.getMenuFilterStructure(this, department), 'none');
                                },
                            }
                        ]
                    },
                    {
                        name: 'servidor__tipo',
                        groupMenu: '',
                        text: 'Tipos de Servidores',
                        scope: scope,
                        menu: [
                            new Ext.menu.CheckItem({
                                name: 'administrative',
                                groupMenu: 'servidor__tipo',
                                text: 'Servidores',
                                checked: department == 'rh' ? true : false,
                                value: 'S',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'member',
                                groupMenu: 'servidor__tipo',
                                text: 'Membros',
                                checked: department == 'expediente' || department == 'rh' ? true : false,
                                value: 'M',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'trainee',
                                groupMenu: 'servidor__tipo',
                                text: 'Estagiários',
                                checked: false,
                                value: 'E',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'voluntary',
                                groupMenu: 'servidor__tipo',
                                text: 'Voluntário',
                                checked: false,
                                value: 'V',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'outsourced',
                                groupMenu: 'servidor__tipo',
                                text: 'Terceirizado',
                                checked: false,
                                value: 'T',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'residente',
                                groupMenu: 'servidor__tipo',
                                text: 'Residente',
                                checked: false,
                                value: 'R',
                                hideOnClick: false,
                                scope: scope
                            }),
                        ]
                    },
                    {
                        name: 'estado',
                        groupMenu: '',
                        text: 'Situação',
                        scope: scope,
                        menu: [
                            new Ext.menu.CheckItem({
                                name: 'active',
                                groupMenu: 'estado',
                                text: 'EM FRUIÇÃO',
                                checked: true,
                                value: 2,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'scheduled',
                                groupMenu: 'estado',
                                text: 'AGENDADO',
                                checked: true,
                                value: 1,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'canceled',
                                groupMenu: 'estado',
                                text: 'CANCELADO',
                                checked: false,
                                value: 4,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'finished',
                                groupMenu: 'estado',
                                text: 'USUFRUÍDO',
                                checked: true,
                                value: 3,
                                hideOnClick: false,
                                scope: scope
                            })
                        ]
                    },
                    {
                        name: 'alteracao',
                        groupMenu: '',
                        text: 'Tipo de alteração',
                        scope: scope,
                        menu: [
                            new Ext.menu.CheckItem({
                                name: 'interruption',
                                groupMenu: 'alteracao',
                                text: 'Interrupção',
                                checked: false,
                                value: 5,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'request',
                                groupMenu: 'alteracao',
                                text: 'Alteração a pedido',
                                checked: false,
                                value: 2,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'revocation',
                                groupMenu: 'alteracao',
                                text: 'Revogação',
                                checked: false,
                                value: 1,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'suspension',
                                groupMenu: 'alteracao',
                                text: 'Suspensão',
                                checked: false,
                                value: 3,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'canceled',
                                groupMenu: 'alteracao',
                                text: 'Cancelado',
                                checked: false,
                                value: 4,
                                hideOnClick: false,
                                scope: scope
                            })
                        ]
                    }
                ];
                if (scope._menuFilterStructure && rh.afastamento.ManagerGrid.filter_structure) {
                    rh.afastamento.ManagerGrid.filter_structure.forEach(
                        function (item) {
                            if (item.groupMenu !== '' && item.groupMenu !== undefined) {
                                var groupMenu = rh.afastamento.ManagerGrid.getGroupMenu(
                                    rh.afastamento.ManagerGrid.filter_structure, item.groupMenu);
                                if (groupMenu !== undefined) {
                                    groupMenu.menu.push(item);
                                }
                            }
                        }
                    );
                    var newMenu = rh.afastamento.ManagerGrid.getGroupMenu(rh.afastamento.ManagerGrid.filter_structure, 'new');
                    if (newMenu) {
                        scope._menuFilterStructure = scope._menuFilterStructure.concat(newMenu.menu);
                    }
                }
            }
            return scope._menuFilterStructure;
        },

        getMenuNew: function (scope) {
            if (!this._menuNew) {
                rh.afastamento.ManagerGrid.types.forEach(
                    function (item) {
                        if (item.Class === '') {
                            rh.afastamento.ManagerGrid.menu_structure.push({
                                name: item.name,
                                groupMenu: item.groupMenu,
                                text: item.label,
                                scope: scope,
                                iconCls: item.iconCls,
                                menu: []
                            });
                        } else {
                            rh.afastamento.ManagerGrid.menu_structure.push({
                                name: item.name,
                                groupMenu: item.groupMenu,
                                text: item.label,
                                scope: scope,
                                iconCls: item.iconCls,
                                handler: function () {
                                    this.createItem(this.values || {}, item.Class);
                                }
                            });
                        }
                    }
                );

                rh.afastamento.ManagerGrid.menu_structure.forEach(
                    function (item) {
                        if (item.groupMenu !== '' && item.groupMenu !== undefined) {
                            var groupMenu = rh.afastamento.ManagerGrid.getGroupMenu(
                                rh.afastamento.ManagerGrid.menu_structure, item.groupMenu);
                            if (groupMenu !== undefined)
                                groupMenu.menu.push(item);
                        }
                    }
                );
                this._menuNew = rh.afastamento.ManagerGrid.getGroupMenu(
                    rh.afastamento.ManagerGrid.menu_structure, 'new');
            }
            return this._menuNew;
        },

        getMenuAnnotation: function (scope) {
            return {
                text: 'Anotação',
                iconCls: true,
                icon: "/" + global.Context + "/static/engine/images/icons/athenas-0333.png",
                handler: function () {
                    var selected = scope.getSelectionModel().getSelected();
                    if (selected.get("anotacao_geral")) {
                        var rest = Ext._create(selected.get("annotation_class"), {
                            action: 'update',
                            oId: selected.get('anotacao_geral'),
                            values: 'remote',
                            callback: {
                                success: {
                                    scope: scope,
                                    fn: function () {
                                        this.getStore().reload();
                                    }
                                }
                            }
                        });
                        rest.show();
                    } else { alert('Este afastamento não possui anotação!'); }
                },
                scope: scope
            };
        },

        getMenuEdit: function (scope) {
            if (!this._menuEdit) {
                this._menuEdit = {
                    text: 'Editar',
                    iconCls: 'icon-core icon-core-edit',
                    scope: scope,
                    handler: function () {
                        this.updateItem();
                    }
                };
            }
            return this._menuEdit;
        },

        getMenuRemove: function (scope) {
            if (!this._menuRemove) {
                this._menuRemove = {
                    text: 'Remover',
                    iconCls: 'icon-core icon-core-delete',
                    scope: scope,
                    handler: function () {
                        this.removeItems();
                    }
                };
            }
            return this._menuRemove;
        },
    },

    getColumnModelItems: function () {
        return rh.afastamento.ManagerGrid.superclass.getColumnModelItems.call(this, {});
    },

    getToolbar: function (cfg) {
        var newComponent;
        var editComponent;
        var deleteComponent;
        var downloadComponent;

        if (!this._toolbar) {
            cfg = core.nullValue(cfg, {});
            Ext.apply(cfg, { gridAutoLoad: false, });

            this._toolbar = rh.afastamento.ManagerGrid.superclass.getToolbar.call(this, cfg);
            this._toolbar.findBy(
                function (item) {
                    if (item.text == 'Novo')
                        newComponent = item;
                    if (item.text == 'Editar')
                        editComponent = item;
                    if (item.text == 'Remover')
                        deleteComponent = item;
                    if (item.text == 'Download')
                        downloadComponent = item;
                }
            );
            this._toolbar.remove(newComponent);
            this._toolbar.remove(editComponent);
            this._toolbar.remove(deleteComponent);
            this._toolbar.remove(downloadComponent);

            this._toolbar.insert(0,
                {
                    text: 'Afastamentos',
                    iconCls: true,
                    icon: "/" + global.Context + "/static/engine/images/icons/athenas-0460.png",
                    menu:
                    {
                        id: 'firstMenu',
                        split: true,
                        defaultStyle: 'splitbutton',
                        style: { overflow: 'visible' },
                        scope: this,
                        items: [
                            rh.afastamento.ManagerGrid.getMenuNew(this),
                            rh.afastamento.ManagerGrid.getMenuEdit(this),
                            rh.afastamento.ManagerGrid.getMenuRemove(this),
                            rh.afastamento.ManagerGrid.getMenuAnnotation(this),
                        ]
                    }
                }
            );
            this._toolbar.insert(1,
                {
                    text: 'Substituições',
                    iconCls: true,
                    icon: "/" + global.Context + "/static/engine/images/icons/athenas-0246.png",
                    handler: function () {
                        new rh.afastamento.ManagerSubstitutionWindow({
                            title: (
                                this.getSelectionModel().getSelected().get("servidor_unicode") +
                                ' | ' + this.getSelectionModel().getSelected().get("situation_unicode") +
                                ': ' + this.getSelectionModel().getSelected().get("estado_display") +
                                ' ' + Ext.util.Format.date(this.getSelectionModel().getSelected().get("data_inicio"), 'd/m/Y') +
                                ' -> ' + (this.getSelectionModel().getSelected().get("data_fim") !== '' ? Ext.util.Format.date(this.getSelectionModel().getSelected().get("data_fim"), 'd/m/Y') : '--------')),

                        },
                            {
                                departure: this.getSelectionModel().getSelected().get("pk"),
                                employee: this.getSelectionModel().getSelected().get("servidor"),
                                employee_type: this.getSelectionModel().getSelected().get("employee_type"),
                                employee_registry: this.getSelectionModel().getSelected().get("employee_registry"),
                                employee_pk: this.getSelectionModel().getSelected().get("employee_pk"),
                                departament: this.department,
                                callback: this.getStore()
                            }).show();
                    },
                    scope: this
                }
            );

            this._toolbar.insert(this._toolbar.items.length,
                {
                    xtype: 'button',
                    text: 'Localizar',
                    iconCls: true,
                    icon: '/' + global.Context + '/static/images/find.png',
                    handler: this.setFilterParams,
                    scope: this
                }
            );

            this._dateStartField = new Ext.form.DateField({
                emptyText: 'Início',
                format: 'd/m/Y',
                width: 90,
                enableKeyEvents: true,
                listeners: {
                    scope: this,
                    keypress: function (text, event) {
                        if (event.getCharCode() == event.RETURN || event.getCharCode() == event.TAB) {
                            this.setFilterParams();
                        }
                    }
                }
            });
            this._dateEndField = new Ext.form.DateField({
                emptyText: 'Fim',
                format: 'd/m/Y',
                width: 90,
                enableKeyEvents: true,
                listeners: {
                    scope: this,
                    keypress: function (text, event) {
                        if (event.getCharCode() == event.RETURN || event.getCharCode() == event.TAB) {
                            this.setFilterParams();
                        }
                    }
                }
            });
            this._checkChangeField = new Ext.form.Checkbox({
                boxLabel: 'Alterações',
                checked: false
            });
            this._toolbar.insert(7,
                this._checkChangeField
            );
            this._toolbar.insert(8,
                '-'
            );
            this._toolbar.insert(9,
                this._dateStartField
            );
            this._toolbar.insert(11,
                this._dateEndField
            );
            this._toolbar.insert(14,
                '-'
            );
            this._toolbar.insert(16,
                '-'
            );
            this._toolbar.insert(17,
                downloadComponent
            );

            this.setFilterParams();
        }
        return this._toolbar;
    },

    getFilterMenu: function () {
        return rh.afastamento.ManagerGrid.getMenuFilterStructure(this, this.department);
    },

    setFilterParams: function () {
        var store = this.getStore();

        this.setKeyWord(this.getKeywordField().getValue());

        this._filterParams = { 'tipo': [], 'estado': [], 'alteracao': [], 'servidor__tipo': [] };
        this._filterSituation = [];
        this._filterChange = [];
        this.toFind(rh.afastamento.ManagerGrid.getMenuFilterStructure(this, this.department));

        var dateStart = Ext.util.Format.date(this._dateStartField.getValue(), 'Y-m-d');
        var dateEnd = Ext.util.Format.date(this._dateEndField.getValue(), 'Y-m-d');

        var params = [];
        params.push({ 'property': 'tipo__in', 'value': this._filterParams.tipo, 'stage': 0 });
        params.push({ 'property': 'estado__in', 'value': this._filterParams.estado, 'stage': 0 });
        params.push({ 'property': 'alteracao__in', 'value': this._filterParams.alteracao, 'stage': 0 });
        params.push({ 'property': 'servidor__tipo__in', 'value': this._filterParams.servidor__tipo, 'stage': 0 });

        if (dateStart !== '' && dateEnd === '') {
            if (this._checkChangeField.checked) {
                params.push({ 'property': 'modified_at__gte', 'value': dateStart, 'stage': 1 });
            }
            else {
                params.push({ 'property': 'data_inicio__gte', 'value': dateStart, 'stage': 1 });
            }
        } else if (dateEnd !== '' && dateStart === '') {
            if (this._checkChangeField.checked) {
                params.push({ 'property': 'modified_at__lte', 'value': dateEnd, 'stage': 1 });
            } else {
                params.push({ 'property': 'data_fim__lte', 'value': dateEnd, 'stage': 1 });
            }
        } else if (dateStart !== '' && dateEnd !== '') {
            if (this._checkChangeField.checked) {
                params.push({ 'property': 'modified_at__gte', 'value': dateStart, 'stage': 1 });
                params.push({ 'property': 'modified_at__lte', 'value': dateEnd, 'stage': 2 });
            } else {
                params.push({ 'property': 'data_inicio__gte', 'value': dateStart, 'stage': 1 });
                params.push({ 'property': 'data_fim__lte', 'value': dateEnd, 'stage': 2 });
            }
        }

        this.setFilter(params);
    },

    doKeywordFilter: function (keyword) {
        this.setKeyWord(keyword);
        this.setFilterParams();
    },

    setKeyWord: function (keyword) {
        var store = this.getStore();
        if (keyword !== '')
            store.baseParams.keyword = keyword;
        else {
            store.baseParams.keyword = null;
            delete store.baseParams.keyword;
        }
    },

    toFind: function (menu) {
        menu.forEach(
            function (item) {
                if (item.menu) {
                    this.toFind(item.menu);
                } else {
                    if (!item.handler) {
                        if (item.inherited && item.checked && item.value) {
                            this._filterParams.tipo.push(item.value);
                        }
                        if (item.groupMenu == 'estado' && item.checked) {
                            this._filterParams.estado.push(item.value);
                        }
                        if (item.groupMenu == 'alteracao' && item.checked) {
                            this._filterParams.alteracao.push(item.value);
                        }
                        if (item.groupMenu == 'servidor__tipo' && item.checked) {
                            this._filterParams.servidor__tipo.push(item.value);
                        }
                    }
                }
            },
            this
        );
    },

    markFilter: function (menu, option) {
        var value = option == 'all' ? true : false;
        this._dateStartField.setValue('');
        this._dateEndField.setValue('');
        this._checkChangeField.setValue(false);
        menu.forEach(
            function (item) {
                if (item.menu) {
                    this.markFilter(item.menu, option);
                } else {
                    if (!item.handler) {
                        if (item.inherited) {
                            item.setChecked(value);
                        }
                        if (value === true && item.groupMenu == 'estado' && (item.name == 'active' || item.name == 'scheduled')) {
                            item.setChecked(value);
                        }
                        if (value === true && item.groupMenu == 'alteracao') {
                            item.setChecked(!value);
                        }
                        if (item.groupMenu == 'servidor__tipo') {
                            item.setChecked(value);
                        }
                    }
                }
            },
            this
        );
    },

    createItem: function (values, _class) {
        _class = _class || 'rh.afastamento.baselicencaafastamento.Window';
        this.restWindow = _class;
        rh.afastamento.ManagerGrid.superclass.createItem.call(this, values);
    },

    updateItem: function (record) {
        var selected = core.nullValue(record, this.getSelectionModel().getSelected());
        if (selected) {
            var _class = rh.afastamento.ManagerGrid.getClassByName(selected.get('type'));
            if (selected.get('situation_unicode') == 'Recesso Forense - Membros'){
                _class = 'rh.afastamento.afastamentorecessoforense.Window';
            }
            this.restWindow = _class;
        }
        rh.afastamento.ManagerGrid.superclass.updateItem.call(this, record);
    },

    removeItems: function (record, cfg) {
        Ext.Msg.show({
            title: 'Removendo',
            icon: Ext.Msg.ERROR,
            buttons: Ext.Msg.OK,
            msg: 'Não é permitido remover. Clique em Editar e modifique o campo alteração para CANCELADO.'
        });
    },

    constructor: function (cfg) {
        this.department = cfg.department;
        rh.afastamento.ManagerGrid.superclass.constructor.call(this, cfg);
    },

});

core.RestfulGrid.register(
    'rh.afastamento.ManagerRestful',
    'rh.afastamento.ManagerGrid'
);

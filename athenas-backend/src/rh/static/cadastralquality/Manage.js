/** autogenerated **/
Ext._define('rh.cadastralquality.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.cadastralquality.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Qualidade Cadastral'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getGrid(),
                ]
            }
        );

        rh.cadastralquality.Manage.superclass.constructor.call(this, cfg);
    }
});


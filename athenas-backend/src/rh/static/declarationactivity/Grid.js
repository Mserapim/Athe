/** autogenerated **/
/**
 *
 **/
Ext._define('rh.declarationactivity.Grid', {
    extend: 'rh.movimentacao.pessoal.Grid',

    restWindow: 'rh.declarationactivity.Window',

    configOrderToolBar: ['add', 'edit', 'remove', 'setMain', '-', 'search', '->', 'download'],

    constructor: function(cfg) {
        rh.declarationactivity.Grid.superclass.constructor.call(this, cfg);
    },

    getSetMainAction: function (cfg) {
        if (!this._setMainAction) {
            this._setMainAction = Ext._create('Ext.Button', {
                text: 'Marcar como Principal...',
                iconCls: 'icon-core icon-core-reports',
                scope: this,
                handler: function () {
                    if (this.getSelectionModel().getSelected())
                        this._setMain(this.getSelectionModel().getSelected().id);
                    else
                        Ext.Msg.show({
                            title: 'Maracando como Principal',
                            msg: 'Primeiro selecione uma lotação.',
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK
                        });

                }
            });
        }
        return this._setMainAction;
    },

    _setMain: function (pk) {
        var rest = this.factoryRestful();
        var mask = new Ext.LoadMask(this.getEl(), { msg: 'Setando principal' });

        mask.show();
        rest.setMain(
            pk,
            true,
            {
                scope: this,
                fn: function () {
                    this.getStore().reload();
                }
            },
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: 'Principal',
                        msg: message,
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK
                    });
                }
            },
            {
                fn: function () {
                    mask.hide();
                }
            }
        );
    },

    getColumnModelItems: function(){
        if(!this._columnModelItems){
            this._columnModelItems = rh.declarationactivity.Grid.superclass.getColumnModelItems.call(this, {});
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'servidor_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'publicacao_movimentacao_unicode');
            this._columnModelItems = this._columnModelItems.concat([
                {header: 'Ícones', dataIndex: 'icons', width: 90, renderer: toolkit.util.rendererIconGrid},
                {header: 'Ativo', dataIndex: 'ativo', width: 70, renderer: toolkit.util.formatIconYesNo, hidden: true},
                {header: 'Servidor', dataIndex: 'servidor_unicode', width: 220, id: 'autoExpandColumn'},
                {header: 'Quadro', dataIndex: 'quadro_unicode', width: 200},
                {header: 'Lotação', dataIndex: 'lotacao_unicode', width: 220},
                {header: 'Turno', dataIndex: 'turno_display', width: 90},
                {header: 'Exercício', dataIndex: 'data_exercicio', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                {header: 'Encerramento', dataIndex: 'data_encerramento', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                {header: 'Principal', dataIndex: 'main', width: 120, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }, hidden: true},
                {header: 'Agendamento principal', dataIndex: 'main_schedule_date', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y'), sortable: true}
            ]);
        }
        return this._columnModelItems;
    }
});

core.RestfulGrid.register(
    'rh.declarationactivity.Restful',
    'rh.declarationactivity.Grid'
);

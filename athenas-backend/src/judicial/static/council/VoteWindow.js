/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.council.VoteWindow', {
    extend: 'core.RestfulWindow',

    rest: 'judicial.council.VoteRestful',

    width: 900,

    getAttachedGrid: function(cfg) {
        if(!this._attachedGrid)
            this._attachedGrid = Ext._create('judicial.council.VoteAttachedGrid', {
                title: 'Anexos do Voto',
                gridAutoLoad: true
            });

        return this._attachedGrid;
    },

    getMainPanel: function(cfg) {
        if(!this._mainPanel)
            this._mainPanel = Ext._create('Ext.Panel', {
                title: 'Voto',
                layout: 'form',
                border: false,
                frame: true,
                items: [
                    {
                        xtype: 'choicefield',
                        hiddenName: 'vote_type',
                        fieldLabel: 'Votar',
                        choiceId: 'council.VOTE_TYPE',
                        allowBlank: true,
                        withNone: true,
                        withNoneLabel: 'Ausente na sessão'
                    },
                    {
                        xtype: 'container',
                        items: [
                            {
                                xtype: 'ckeditor',
                                name: 'observation',
                                height: 350
                            }
                        ]
                    }
                ]
            });

        return this._mainPanel;
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                activeTab: 0,
                border: false,
                height: 545,
                items: [
                    this.getMainPanel(cfg),
                    this.getAttachedGrid(cfg)
                ]
            });

        return this._tabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    this.getTabPanel(cfg)
                ]
            });

        return this._formPanel;
    },

    readInstance: function(inst) {
        this.oId = inst.pk;
        this.vote(inst.pk);
        this.action = 'update';
    },

    vote: function(value, dispatch) {
        dispatch = (dispatch === undefined ? true : dispatch);

        if(value !== undefined) {
            this._vote = value;

            if(dispatch)
                this.observeVote();
        }

        return this._vote;
    },

    observeVote: function() {
        var value = this.vote();

        if(value) {
            this.getAttachedGrid().enable();
            this.getAttachedGrid().setParam('vote', value);
            this.getAttachedGrid().setFilterProperty('vote', value, 100);
        }
        else {
            this.getAttachedGrid().disable();
            this.getAttachedGrid().setParam('vote', value);
            this.getAttachedGrid().setFilterProperty('vote', value, 100, false);
            this.getAttachedGrid().getStore().removeAll();
        }
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            saveAndContinue: {
                scope: this,
                fn: this.readInstance
            }
        });

        judicial.council.VoteWindow.superclass.constructor.call(this, cfg);
        if(cfg.oId || this.oId) this.vote(cfg.oId || this.oId);
    }
});

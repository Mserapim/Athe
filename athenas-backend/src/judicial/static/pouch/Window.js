/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.pouch.Window', {
    extend: 'core.RestfulWindow',

    rest: 'judicial.pouch.Restful',

    width: 850,

    getLogPanel: function() {
        if (!this._logPanel) {
            this._logPanel = Ext._create('Ext.Panel', {
                title: 'Log',
                border: false,
                frame: true
            });
        }

        return this._logPanel;
    },

    getMainPanel: function(cfg) {
        if (!this._mainPanel) {
            this._mainPanel = Ext._create('Ext.Panel', {
                layout: 'form',
                title: 'Principal',
                border: false,
                frame: true,
                items: [
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Origem",
                        allowBlank: false,
                        rest: "rh.workplace.Restful",
                        name: "from_location"
                    },
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Destino",
                        allowBlank: false,
                        name: "to_location",
                        rest: "rh.workplace.Restful",
                        preFilter: [
                            { property: 'allow_lawsuit', value: true, stage: 1002 }
                        ]
                    },
                    {
                        xtype: 'container',
                        items: [
                            {
                                allowBlank: true,
                                name: "content",
                                xtype: "ckeditor",
                                height: 450
                            }
                        ]
                    }
                ]
            });
        }

        return this._mainPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    Ext._create('Ext.TabPanel', {
                        activeTab: 0,
                        height: 650,
                        border: false,
                        items: [
                            this.getMainPanel(cfg),
                            this.getLogPanel(cfg)
                        ]
                    })
                ]
            });

        return this._formPanel;
    }
});

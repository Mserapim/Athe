/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.TagWindow', {
    extend: 'core.RestfulWindow',

    rest: 'judicial.TagRestful',

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                {
                    maxLength: 100,
                    width: 200,
                    allowBlank: false,
                    fieldLabel: "T\u00edtulo",
                    name: "title",
                    xtype: "textfield"
                },
                {
                    xtype: "hidden",
                    name: "tag_type",
                },
                {
                    xtype: "hidden",
                    name: "work_place",
                }
            ]
            });

        return this._formPanel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.apply(
            cfg,
            {
                frame: true,
                border: false
            }
        );

        judicial.TagWindow.superclass.constructor.call(this, cfg);
    }
});

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.tac.ManagementTACGrid', {
    'extend': 'core.RestfulGrid',

    'restWindow': 'judicial.tac.ManagementTACWindow',

    'getColumnModel': function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: '', dataIndex: 'icons', width: 86, menuDisabled: true, renderer: core.rendererIconGrid},
                    {header: 'Procedimento', dataIndex: 'lawsuit_cache_number', 'width': 100},
                    {header: 'Descrição', dataIndex: 'description', id: 'autoExpandColumn', width: 400},
                    {header: 'Cláusula a vencer', dataIndex: 'next_date_expiration', width: 115, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                ]
            );

        return this._columnModel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.apply(
            cfg,
            {
                viewConfig: {
                    getRowClass: function(record, rowIndex, rp, ds) {
                        if(record.data.days_to_expiration !== null)
                            var days = record.data.days_to_expiration;
                            if(days > 10)
                                return '';
                            else if(days >= 0)
                                return 'x-grid3-yellow-simple';
                            else if(days < 0)
                                return 'x-grid3-red-simple';
                        else
                            return '';
                    }
                }
            }
        );

        judicial.OutCourtLawsuitGrid.superclass.constructor.call(this, cfg);
    }
});

core.RestfulGrid.register(
    'judicial.tac.ManagementTACRestful',
    'judicial.tac.ManagementTACGrid'
);

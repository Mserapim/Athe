/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.requestexternalaccess.Grid', {
    extend: 'core.RestfulGrid',

    restWindow: 'judicial.requestexternalaccess.Window',

    hideActions: ['copy', 'remove', 'edit'],

    openJustificationWindow: function(instance) {
        Ext._create('judicial.requestexternalaccess.JustificationWindow', {
            oId: instance.get('pk'),
            callback: {
                success: {
                    scope: this,
                    fn: function(xhr) {
                        core.invokeCallback((this.callback || {}).success);
                        this.getStore().reload();
                    }
                }
            }
        }).show();
    },

    getState: function() {
        if(!this._stateFilter) {
            var filter = this.getFilter();
            var values = [];
            
            filter.forEach(
                function(item) {
                    if(item.property == 'state__in' && item.stage == 100)
                        values = item.value;
                }
            );

            this._stateFilter = values;
        }

        return this._stateFilter;
    },

    getFilterMenu: function(cfg) {
        if(!this._filterMenu)
            this._filterMenu = [
                this.pendingFilter(),
                this.authorizedFilter(),
                this.revokedDeniedFilter()
            ];

        return this._filterMenu;
    },

    addFilterState: function(state, autoload) {
        autoload = core.nullValue(autoload, true);
        this.getState().push(state);
        this._applyState(autoload);
    },

    _applyState: function(autoload) {
        this.removeFilterProperty('state__in', 100, false);
        this.addFilterProperty('state__in', this.getState(), 100, autoload);
    },

    removeFilterState: function(state, autoload) {
        autoload = core.nullValue(autoload, true);
        var index = this.getState().indexOf(state);
        if(index !== -1)
            this.getState().splice(index, 1);
        this._applyState(autoload);
    },

    pendingFilter: function() {
        if(!this._pendingFilter) {
            this._pendingFilter = Ext._create('Ext.menu.CheckItem', {
                text: 'Todas solicitações pendentes',
                checked: true,
                listeners: {
                    scope: this,
                    checkchange: function(el, checked) {
                        if(checked)
                            this.addFilterState(1);
                        else
                            this.removeFilterState(1);
                    }
                }
            });
        }

        return this._pendingFilter;
    },

    revokedDeniedFilter: function() {
        if(!this._revokedDeniedFilter) {
            this._revokedDeniedFilter = Ext._create('Ext.menu.CheckItem', {
                text: 'Todas solicitações negadas ou revogados',
                checked: false,
                listeners: {
                    scope: this,
                    checkchange: function(el, checked) {
                        if(checked) {
                            this.addFilterState(3, false);
                            this.addFilterState(4);
                        } else {
                            this.removeFilterState(3, false);
                            this.removeFilterState(4);
                        }
                    }
                }
            });
        }

        return this._revokedDeniedFilter;
    },

    authorizedFilter: function() {
        if(!this._authorizedFilter) {
            this._authorizedFilter = Ext._create('Ext.menu.CheckItem', {
                text: 'Todas solicitações autorizadas',
                checked: false,
                listeners: {
                    scope: this,
                    checkchange: function(el, checked) {
                        if(checked)
                            this.addFilterState(2);
                        else
                            this.removeFilterState(2);
                    }
                }
            });
        }

        return this._authorizedFilter;
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Procedimento', dataIndex: 'lawsuit_cache_number', width: 80},
                    {header: 'Título', hidden: true, dataIndex: 'lawsuit_title', width: 120},
                    {header: 'Para', dataIndex: 'person_unicode', id: 'autoExpandColumn'},
                    {header: 'Representando', dataIndex: 'as_representative_of_unicode', width: 200},
                    {header: 'Autorizado por', dataIndex: 'authorized_by_unicode', width: 120},
                    {header: 'Autorizado', hidden: true, dataIndex: 'authorized_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Revogado por', dataIndex: 'revoked_by_unicode', width: 120},
                    {header: 'Revogado', hidden: true, dataIndex: 'revoked_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Negado por', dataIndex: 'denied_by_unicode', width: 120},
                    {header: 'Negado', hidden: true, dataIndex: 'denied_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Modificado por', hidden: true, dataIndex: 'modified_by_unicode', width: 120},
                    {header: 'Modificado', hidden: true, dataIndex: 'modified_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Criado por', hidden: true, dataIndex: 'created_by_unicode', width: 120},
                    {header: 'Criado', hidden: true, dataIndex: 'created_at', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                ]
            );

        return this._columnModel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.apply(
            cfg,
            {
                viewConfig: {
                    getRowClass: function(record, rowIndex, rp, ds) {
                        if(record.data.pending == true)
                            return 'x-grid3-red-simple';
                    }
                }
            }
        );

        judicial.requestexternalaccess.Grid.superclass.constructor.call(this, cfg);
    }

});

core.RestfulGrid.register(
    'judicial.requestexternalaccess.Restful',
    'judicial.requestexternalaccess.Grid'
);

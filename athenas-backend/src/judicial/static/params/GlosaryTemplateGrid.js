/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.params.GlosaryTemplateGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'judicial.params.GlosaryTemplateWindow',

    getToolbar: function(cfg) {
        if(!this._toolbar) {
            this._toolbar = judicial.params.GlosaryGrid.superclass.getToolbar.call(this, cfg);

            this._toolbar.items.each(function(item) {
                if(item.setText)
                    item.setText('');
            });


            this._toolbar.get(5).setWidth(205);
            this._toolbar.remove(4);
            this._toolbar.remove(5);
        }

        return this._toolbar;
    },

    getFooterbar: function(cfg) {
        if(!this._footerbar)
            this._footerbar = Ext._create('Ext.PagingToolbar', {
                style: cfg.footerStyle,
                store: this.getStore(),
                pageSize: 30,
                displayInfo: false
            });

        return this._footerbar;
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {
                        'header': '',
                        'dataIndex': 'icons',
                        'width': 30,
                        'menuDisabled': true,
                        'renderer': core.rendererIconGrid
                    },
                    {header: 'Descricao', dataIndex: 'title', id: 'autoExpandColumn'}
                ]
            );

        return this._columnModel;
    }
});

core.RestfulGrid.register(
    'judicial.params.GlosaryTemplateRestful',
    'judicial.params.GlosaryTemplateGrid'
);

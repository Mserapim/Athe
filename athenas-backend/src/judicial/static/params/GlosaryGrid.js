/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.params.GlosaryGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'judicial.params.GlosaryWindow',

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {
                        'header': '',
                        'dataIndex': 'icons',
                        'width': 30,
                        'menuDisabled': true,
                        'renderer': core.rendererIconGrid
                    },
                    {header: 'Documento', dataIndex: 'title', id: 'autoExpandColumn'},
                    {header: 'Tipo', dataIndex: 'meaning_type_display', with: 80}
                ]
            );

        return this._columnModel;
    },

    getToolbar: function(cfg) {
        if(!this._toolbar) {
            this._toolbar = judicial.params.GlosaryGrid.superclass.getToolbar.call(this, cfg);

            this._toolbar.remove(0);
            this._toolbar.remove(1);
            this._toolbar.remove(2);
            this._toolbar.remove(3);

            this._toolbar.get(0).setText('');
            this._toolbar.get(5).setText('');
            this._toolbar.get(2).setWidth(250);
        }

        return this._toolbar;
    },

    getFooterbar: function(cfg) {
        if(!this._footerbar)
            this._footerbar = Ext._create('Ext.PagingToolbar', {
                style: cfg.footerStyle,
                store: this.getStore(),
                pageSize: 30,
                displayInfo: false
            });

        return this._footerbar;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg,  {
            columnAction: false,
            allowCreate: false,
            allowRemove: false
        });

        judicial.params.GlosaryGrid.superclass.constructor.call(this, cfg);
    }
});

core.RestfulGrid.register(
    'judicial.params.GlosaryRestful',
    'judicial.params.GlosaryGrid'
);

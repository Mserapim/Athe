/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.DearchivingDispatchWindow', {
    extend: 'judicial.PartLawsuitActionWindow',

    rest: 'judicial.parts.DearchivingDispatchRestful',

    width: 750,
    autoCreate: true,

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    {
                        allowBlank: true,
                        width: 400,
                        fieldLabel: "Motivo",
                        hiddenName: "dearchiving_type",
                        xtype: "choicefield",
                        choiceId: 'judicial.DEARCHIVEDISPATCH_TYPE'
                    },
                    {
                        allowBlank: false,
                        fieldLabel: "Relato",
                        name: "feedback",
                        xtype: "ckeditor",
                        hideLabel: true,
                        height: 437,
                        submit: true,
                    }
                ]
            });

        return this._formPanel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            border: false,
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    
                    this.getFormPanel().getForm().setValues(instance);
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        judicial.parts.DearchivingDispatchWindow.superclass.constructor.call(this, cfg);

    }
});

judicial.PartLawsuitGrid.register('judicial.dearchivingdispatch', 'judicial.parts.DearchivingDispatchWindow');

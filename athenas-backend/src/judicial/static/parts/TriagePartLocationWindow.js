/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.TriagePartLocationWindow', {
    extend: 'core.RestfulWindow',

    rest: 'judicial.parts.TriagePartLocationRestful',

    width: 450,

    getSectionalField: function(cfg) {
        if(!this._sectionalField) {
            this._sectionalField = Ext._create('core.fields.AutocompleteField', {
                fieldLabel: "Regional",
                allowBlank: true,
                rest: "judicial.params.SectionalRestful",
                name: "sectional",
                preFilter: [
                    {
                        property: 'county__locations',
                        value: cfg.values.location
                    }
                ]
            });
        }

        return this._sectionalField;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    {
                        xtype: "rest-autocompletefield",
                        fieldLabel: "Localidade",
                        allowBlank: false,
                        rest: "rh.localidade.Restful",
                        name: "location",
                        comboListeners: {
                            scope: this,
                            select: function(combo, data) {
                                this.getSectionalField().setValue('');
                                this.getSectionalField().setPreFilter([
                                    {property: 'county__locations', value: data.get('pk')}
                                ]);
                            }
                        }
                    },
                    this.getSectionalField(cfg)
                ]
            });

        return this._formPanel;
    }
});


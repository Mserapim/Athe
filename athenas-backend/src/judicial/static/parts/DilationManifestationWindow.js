/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.DilationManifestationWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.parts.DilationManifestationRestful',

    width: 950,

    getAttachementPanel: function(cfg) {
        if(!this._attachmentPanel)
            this._attachmentPanel = Ext._create('judicial.parts.AttachedGrid', {
                title: 'Anexos',
                gridAutoLoad: false
            });

        return this._attachmentPanel;
    },

    getContentPanel: function(cfg) {
        if(!this._contentTabPanel)
            this._contentTabPanel = Ext._create('Ext.Panel', {
                title: 'Conteúdo',
                border: false,
                items: [
                    {
                        layout: 'form',
                        border: false,
                        frame: true,
                        labelWidth: 120,
                        items: [
                            {
                                xtype: 'rest-autocompletefield',
                                name: 'manifestation',
                                fieldLabel: 'Diligência',
                                rest: 'judicial.manifestation.Restful',
                                preFilter: [
                                    {
                                        property: 'reference__lawsuit',
                                        value: ((cfg.params || {}).lawsuit || 0),
                                        stage: 1
                                    },
                                    {
                                        property: 'deadline',
                                        value: null,
                                        stage: -1
                                    }
                                ]
                            },
                            {
                                xtype: 'numberfield',
                                fieldLabel: 'Dias de dilação',
                                name: 'dilation_days',
                                width: 80
                            }
                        ]
                    },
                    {
                        xtype: 'container',
                        border: false,
                        items: [
                            {
                                allowBlank: false,
                                height: 407,
                                name: "content",
                                xtype: "ckeditor"
                            }
                        ]
                    }
                ]
            });

        return this._contentTabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    Ext._create('Ext.TabPanel', {
                        activeTab: 0,
                        height: 610,
                        items: [
                            this.getContentPanel(cfg),
                            this.getAttachementPanel(cfg)
                        ]
                    })
                ]
            });

        return this._formPanel;
    },

    dilationManifestation: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._dilationManifestation = value;

            if(!prevent) this.observeDilationManifestation();
        }

        return this._dilationManifestation;
    },

    observeDilationManifestation: function() {
        var value = this.dilationManifestation();

        if(value) {
            this.getAttachementPanel().enable();
            this.getAttachementPanel().setParam('attached_document', value);
            this.getAttachementPanel().setFilterProperty('attached_document_id', value, 100);
        }
        else {
            this.getAttachementPanel().disable();
            this.getAttachementPanel().setParam('attached_document', 0);
            this.getAttachementPanel().setFilterProperty('attached_document_id', 0, 100, false);
            this.getAttachementPanel().getStore().removeAll();
        }
    },

    readDataCallback: function(instance) {
        this.dilationManifestation(instance.pk);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            border: false,
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.dilationManifestation(instance.pk);

                    this.getFormPanel().getForm().setValues(instance);
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        judicial.parts.DilationManifestationWindow.superclass.constructor.call(this, cfg);
        this.observeDilationManifestation();
    }
});

judicial.PartLawsuitGrid.register('judicial.dilationmanifestation', 'judicial.parts.DilationManifestationWindow');

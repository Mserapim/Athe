/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.JudicializationWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.parts.JudicializationRestful',

    width: 950,

    getMainPanel: function(cfg) {
        if(!this._mainPainel)
            this._mainPainel = Ext._create('Ext.Panel', {
                title: 'Principal',
                layout: 'form',
                frame: true,
                items: [
                    {
                        maxLength: 100,
                        allowBlank: false,
                        fieldLabel: "Número",
                        name: "code",
                        xtype: "textfield",
                        width: 250
                    },
                    {
                        maxLength: 200,
                        allowBlank: false,
                        fieldLabel: "Vara",
                        name: "court",
                        xtype: "textfield",
                        width: 550
                    },
                    {
                        xtype: 'container',
                        items: [
                            {
                                allowBlank: false,
                                name: "observation",
                                xtype: "ckeditor",
                                height: 350
                            }
                        ]
                    }
                ]
            });

        return this._mainPainel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    {
                        xtype: 'tabpanel',
                        activeTab: 0,
                        height: 550,
                        border: false,
                        items: [
                            this.getMainPanel(cfg),
                            this.getAttachementPanel(cfg)
                        ]
                    }
                ]
            });

        return this._formPanel;
    },

    getAttachementPanel: function(cfg) {
        if(!this._attachmentPanel)
            this._attachmentPanel = Ext._create('judicial.parts.AttachedGrid', {
                title: 'Anexos',
                gridAutoLoad: false,
                flex: 1
            });

        return this._attachmentPanel;
    },

    judicialization: function(value, dispatch) {
        dispatch = (dispatch === undefined ? true : dispatch);

        if(value !== undefined) {
            this._judicialization = value;

            if(dispatch)
                this.observeJudicialization();
        }

        return this._judicialization;
    },

    observeJudicialization: function() {
        var value = this.judicialization();

        if(value) {
            this.getAttachementPanel().enable();
            this.getAttachementPanel().setParam('attached_document', value);
            this.getAttachementPanel().setFilterProperty('attached_document', value, 100);
        }
        else {
            this.getAttachementPanel().disable();
            this.getAttachementPanel().setParam('attached_document', 0);
            this.getAttachementPanel().setFilterProperty('attached_document', 0, 100);
            this.getAttachementPanel().getStore().removeAll();
        }
    },

    readDataCallback: function(instance) {
        this.judicialization(instance.pk);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.oId = instance.pk;
                    this.action = 'update';
                    this.judicialization(instance.pk);
                }
            }
        });

        judicial.parts.JudicializationWindow.superclass.constructor.call(this, cfg);
        this.on({
            scope: this,
            render: function() {
                this.judicialization(cfg.oId || null);
            }
        });
    }
});

judicial.PartLawsuitGrid.register('judicial.judicialization', 'judicial.parts.JudicializationWindow');

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.UnfoldWindow', {
    extend: 'judicial.PartLawsuitActionWindow',

    rest: 'judicial.parts.UnfoldRestful',

    width: 750,

    getContentPanel: function(cfg) {
        console.log(cfg);

        if(!this._contentTabPanel)
            this._contentTabPanel = Ext._create('Ext.Panel', {
                title: 'Conteúdo',
                border: false,
                items: [
                    {
                        layout: 'form',
                        border: false,
                        frame: true,
                        defaults: { width: 595 },
                        labelWidth: 120,
                        items: [
                            {
                                allowBlank: false,
                                fieldLabel: "Documento",
                                name: "unfold_document",
                                rest: 'judicial.PartLawsuitRestful',
                                xtype: 'rest-autocompletefield',
                                displayField: 'unicode_with_signed_at',
                                gridConfig: {
                                    params: cfg.params,
                                    gridAutoLoad: true,
                                    columnAction: false,
                                    configOrderToolBar: ['search', '->'],
                                },
                                preFilter: [
                                    {property: 'type_part__in', value: ['generalmotion', 'attacheddocument', 'archivement', 'rapporteurdocument', 'recomendation'], stage: 3},
                                    {property: 'lawsuit', value: cfg.params.lawsuit, stage: 1},
                                    {property: 'unfolded_by', value: null, stage: 2},
                                    {property: 'signed_by', value: null, stage: -1}
                                ]
                            }
                        ]
                    }
                ]
            });

        return this._contentTabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    Ext._create('Ext.TabPanel', {
                        activeTab: 0,
                        items: [
                            this.getContentPanel(cfg)
                        ]
                    })
                ]
            });

        return this._formPanel;
    },

    unfold: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._unfold = value;

            if(!prevent) this.observeUnfold();
        }

        return this._unfold;
    },

    observeUnfold: function() {
        var value = this.unfold();
    },

    readDataCallback: function(instance) {
        this.unfold(instance.pk);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            border: false,
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.unfold(instance.pk);

                    this.getFormPanel().getForm().setValues(instance);
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        judicial.parts.UnfoldWindow.superclass.constructor.call(this, cfg);
        this.observeUnfold();
    }
});

judicial.PartLawsuitGrid.register('judicial.unfold', 'judicial.parts.UnfoldWindow');

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.PreInvestigationFactWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.parts.PreInvestigationFactRestful',

    width: 800,

    getAttachementPanel: function(cfg) {
        if(!this._attachmentPanel)
            this._attachmentPanel = Ext._create('judicial.parts.AttachedGrid', {
                title: 'Anexos',
                gridAutoLoad: false,
                flex: 1
            });

        return this._attachmentPanel;
    },

    readDataCallback: function(instance) {
        this.preInvestigationFact(instance.pk);
    },

    preInvestigationFact: function(value, dispatch) {
        dispatch = (dispatch === undefined ? true : dispatch);

        if(value !== undefined) {
            this._preInvestigationFact = value;

            if(dispatch)
                this.observePreInvestigationFact();
        }

        return this._preInvestigationFact;
    },

    observePreInvestigationFact: function() {
        var value = this.preInvestigationFact();

        if(value) {
            this.getAttachementPanel().enable();
            this.getAttachementPanel().setParam('attached_document', value);
            this.getAttachementPanel().setFilterProperty('attached_document', value, 100);
        }
        else {
            this.getAttachementPanel().disable();
            this.getAttachementPanel().setParam('attached_document', 0);
            this.getAttachementPanel().setFilterProperty('attached_document', 0, 100);
            this.getAttachementPanel().getStore().removeAll();
        }
    },

    getMainPanel: function(cfg) {
        if(!this._mainPanel)
            this._mainPanel = Ext._create('Ext.Panel', {
                title: 'Despacho',
                items: [
                    {
                        xtype: 'ckeditor',
                        name: 'justify',
                        height: 420
                    }
                ]
            });

        return this._mainPanel;
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                activeTab: 0,
                height: 550,
                border: false,
                items: [
                    this.getMainPanel(cfg),
                    this.getAttachementPanel(cfg)
                ]
            });

        return this._tabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: false,
                items: [
                    this.getTabPanel(cfg)
                ]
            });

        return this._formPanel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                buttonAlign: 'left',
                disableSaveAndNew: true,
                saveAndContinue: {
                    scope: this,
                    fn: function(instance) {
                        this.oId = instance.pk;
                        this.preInvestigationFact(instance.pk);
                        this.action = 'update';
                    }
                },
                border: true,
                title: 'Investigação Preliminar dos Fatos'
            }
        );

        judicial.parts.PreInvestigationFactWindow.superclass.constructor.call(this, cfg);
        this.observePreInvestigationFact();
    }
});

judicial.PartLawsuitGrid.register('judicial.preinvestigationfact', 'judicial.parts.PreInvestigationFactWindow');

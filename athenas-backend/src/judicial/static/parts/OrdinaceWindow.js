/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.OrdinaceWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.parts.OrdinaceRestful',

    width: 800,

    getInformationPanel: function(cfg) {
        if(!this._informationPanel)
            this._informationPanel = Ext._create('Ext.Panel', {
                layout: {
                    type: 'vbox',
                    align: 'stretch'
                },
                title: 'Informações',
                items: [
                    {
                        layout: 'form',
                        frame: true,
                        padding: '0 0 5 0',
                        labelWidth: 180,
                        items: [
                            {
                                xtype: 'displayfield',
                                fieldLabel: 'Número',
                                name: 'cache_formated_number'
                            },
                            {
                                xtype: 'textfield',
                                fieldLabel: 'Mudar título do Procedimento',
                                name: 'change_title',
                                allowBlank: true,
                                width: 587
                            },
                            {
                                xtype: "rest-autocompletefield",
                                fieldLabel: "Mudar assunto",
                                allowBlank: true,
                                rest: "judicial.taxonomy.LegalMatterRestful",
                                name: "matter"
                            },
                            {
                                xtype: 'choicefield',
                                fieldLabel: 'Tipo',
                                name: 'type_ordinace',
                                hiddenName: 'type_ordinace',
                                choiceId: 'judicial.TYPE_ORDINACE',
                                width: 350
                            }
                        ]
                    },
                    {
                        title: 'Objeto do Procedimento',
                        flex: 1,
                        border: false,
                        items: [
                            {
                                xtype: 'ckeditor',
                                name: 'object_of_proccess',
                                height: 370
                            }
                        ]
                    }
                ]
            });

        return this._informationPanel;
    },

    getLegalGroundPanel: function(cfg) {
        if(!this._legalGroundPanel)
            this._legalGroundPanel = Ext._create('Ext.Panel', {
                title: 'Fundamentação Legal',
                border: false,
                items: [
                    this.getLegalGroundField(cfg)
                ]
            });

        return this._legalGroundPanel;
    },

    getLegalGroundField: function(cfg) {
        if(!this._legalGroundField)
            this._legalGroundField = Ext._create('core.fields.RelatedRestfulField', {
                width: 790,
                height: 574,
                border: false,
                name: 'legalgrounds',
                relatedname: 'in_ordinaces',
                rest: this.rest,
                sourceRest: 'judicial.params.LegalGroundRestful',
                oId: cfg.oId
            });

        return this._legalGroundField;
    },

    getBlokeGrid: function() {
        if(!this._blokeGrid)
            this._blokeGrid = Ext._create('judicial.bloke.Grid', {
                title: 'Investigados',
                gridAutoLoad: false
            });

        return this._blokeGrid;
    },

    getInvestigationGrid: function() {
        if(!this._investigationGrid)
            this._investigationGrid = Ext._create('judicial.diligences.JudicialDiligenceGrid', {
                title: 'Diligências',
                gridAutoLoad: false
            });

        return this._investigationGrid;
    },

    getConsiderationPanel: function(cfg) {
        if(!this._considerationPanel)
            this._considerationPanel = Ext._create('Ext.Panel', {
                title: 'Considerando',
                items: [
                    {
                        xtype: 'ckeditor',
                        name: 'consideration',
                        height: 475
                    }
                ]
            });

        return this._considerationPanel;
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                activeTab: 0,
                border: false,
                height: 600,
                items: [
                    this.getInformationPanel(cfg),
                    this.getConsiderationPanel(cfg),
                    this.getBlokeGrid(cfg),
                    this.getLegalGroundPanel(cfg),
                    this.getInvestigationGrid(cfg),
                    this.getScientifyWorkplaceGrid(cfg)
                ]
            });

        return this._tabPanel;
    },

    getScientifyWorkplaceGrid: function(cfg) {
        if(!this._scientifyWorkplaceGrid)
            this._scientifyWorkplaceGrid = Ext._create('judicial.ScientifyWorkplaceGrid', {
                title: 'Comunicações',
                gridAutoLoad: false,
                columnAction: false
            });

        return this._scientifyWorkplaceGrid;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: false,
                items: [
                    this.getTabPanel(cfg)
                ]
            });

        return this._formPanel;
    },

    lawsuit: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._lawsuit = value;

            if(!prevent) this.observeLawsuit();
        }

        return this._lawsuit;
    },

    observeLawsuit: function() {
        var value = this.lawsuit();

        if(value) {
            this.getBlokeGrid().enable();
            this.getBlokeGrid().setParam('lawsuit', value);
            this.getBlokeGrid().setFilterProperty('lawsuit', value, 1);
        }
        else {
            this.getBlokeGrid().disable();
            this.getBlokeGrid().setParam('lawsuit', 0);
            this.getBlokeGrid().setFilterProperty('lawsuit', 0, 1, false);
            this.getBlokeGrid().getStore().removelAll();
        }
    },

    ordinace: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._ordinace = value;

            if(!prevent) this.observeOrdinace();
        }

        return this._ordinace;
    },

    observeOrdinace: function() {
        var value = this.ordinace();

        if(value) {
            this.getLegalGroundField().objectId(value);

            this.getInvestigationGrid().enable();
            this.getInvestigationGrid().setParam('part', value);
            this.getInvestigationGrid().setFilterProperty('part', value, 1);

            this.getSupplementButton().enable();

            this.getScientifyWorkplaceGrid().enable();
            this.getScientifyWorkplaceGrid().setParam('part', value);
            this.getScientifyWorkplaceGrid().setFilterProperty('part', value, 1);
        }
        else {
            this.getLegalGroundField().objectId(null);

            this.getInvestigationGrid().disable();
            this.getInvestigationGrid().setParam('part', 0);
            this.getInvestigationGrid().setFilterProperty('part', 0, 1, false);
            this.getInvestigationGrid().getStore().removeAll();

            this.getScientifyWorkplaceGrid().disable();
            this.getScientifyWorkplaceGrid().setParam('part', 0);
            this.getScientifyWorkplaceGrid().setFilterProperty('part', 0, 1, false);
            this.getScientifyWorkplaceGrid().getStore().removeAll();

            this.getSupplementButton().disable();

        }
    },

    readDataCallback: function(instance) {
        this.lawsuit(instance.lawsuit);
        this.ordinace(instance.pk);
    },

    getSupplementButton: function(cfg) {
        if(!this._supplementButton)
            this._supplementButton = Ext._create('Ext.Button', {
                text: 'Aditamento',
                scope: this,
                handler: this.createSupplement
            });

        return this._supplementButton;
    },

    createSupplement: function() {
        var rest = this.factoryRestful();
        var mask = new Ext.LoadMask(this.getEl(), {msg: 'Solicitando...'});

        mask.show();
        rest.createSupplement(
            {
                lawsuit: this.lawsuit(),
                ordinace: this.ordinace()
            },
            {
                scope: this,
                fn: function(instance) {
                    Ext._create('judicial.parts.SupplementOrdinaceWindow', {
                        action: 'update',
                        values: 'remote',
                        oId: instance.pk,
                        modal: true
                    }).show();
                }
            },
            {
                scope: this,
                fn: function(message) {
                    Ext.Msg.show({
                        title: 'Solicitando suplementar',
                        msg: message,
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK
                    });
                }
            },
            {
                scope: this,
                fn: function() {
                    mask.hide();
                }
            }
        );
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.ordinace(instance.pk);
                    this.lawsuit(instance.lawsuit);

                    this.getFormPanel().getForm().setValues(instance);
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        judicial.parts.OrdinaceWindow.superclass.constructor.call(this, cfg);

        if((this.params || {}).lawsuit)
            this.lawsuit(this.params.lawsuit);

        this.observeOrdinace();
    }
});

judicial.PartLawsuitGrid.register('judicial.ordinace', 'judicial.parts.OrdinaceWindow');

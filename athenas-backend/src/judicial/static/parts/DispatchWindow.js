/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.DispatchWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.parts.DispatchRestful',

    width: 950,

    getAttachementPanel: function(cfg) {
        if(!this._attachmentPanel)
            this._attachmentPanel = Ext._create('judicial.parts.AttachedGrid', {
                title: 'Anexos',
                gridAutoLoad: false
            });

        return this._attachmentPanel;
    },

    getContentPanel: function(cfg) {
        if(!this._contentTabPanel)
            this._contentTabPanel = Ext._create('Ext.Panel', {
                title: 'Conteúdo',
                border: false,
                items: [
                    {
                        layout: 'form',
                        border: false,
                        frame: true,
                        defaults: { width: 595 },
                        labelWidth: 120,
                        items: [
                            {
                                allowBlank: false,
                                fieldLabel: "Título",
                                name: "dispatch_title",
                                xtype: "textfield"
                            }
                        ]
                    },
                    {
                        xtype: 'container',
                        border: false,
                        items: [
                            {
                                allowBlank: false,
                                height: 465,
                                name: "content",
                                xtype: "ckeditor"
                            }
                        ]
                    }
                ]
            });

        return this._contentTabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    Ext._create('Ext.TabPanel', {
                        activeTab: 0,
                        height: 610,
                        items: [
                            this.getContentPanel(cfg),
                            this.getAttachementPanel(cfg)
                        ]
                    })
                ]
            });

        return this._formPanel;
    },

    dispatch: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._dispatch = value;

            if(!prevent) this.observeDispatch();
        }

        return this._dispatch;
    },

    observeDispatch: function() {
        var value = this.dispatch();

        if(value) {
            this.getAttachementPanel().enable();
            this.getAttachementPanel().setParam('attached_document', value);
            this.getAttachementPanel().setFilterProperty('attached_document_id', value, 100);
        }
        else {
            this.getAttachementPanel().disable();
            this.getAttachementPanel().setParam('attached_document', 0);
            this.getAttachementPanel().setFilterProperty('attached_document_id', 0, 100, false);
            this.getAttachementPanel().getStore().removeAll();
        }
    },

    readDataCallback: function(instance) {
        this.dispatch(instance.pk);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            border: false,
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.dispatch(instance.pk);

                    this.getFormPanel().getForm().setValues(instance);
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        judicial.parts.DispatchWindow.superclass.constructor.call(this, cfg);
        this.observeDispatch();
    }
});

judicial.PartLawsuitGrid.register('judicial.dispatch', 'judicial.parts.DispatchWindow');

/** autogenerated **/
Ext._define('adm.accounting.product.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getProdutoGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('adm.accounting.product.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Produto'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getProdutoGrid(),
                ]
            }
        );

        adm.accounting.product.Manage.superclass.constructor.call(this, cfg);
    }
});


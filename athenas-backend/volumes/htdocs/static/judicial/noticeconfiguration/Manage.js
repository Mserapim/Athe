/** autogenerated **/
Ext._define('judicial.noticeconfiguration.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getAuthorGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('judicial.noticeconfiguration.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Confguração de Notificação'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getAuthorGrid(),
                ]
            }
        );

        judicial.noticeconfiguration.Manage.superclass.constructor.call(this, cfg);
    }
});

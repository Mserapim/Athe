/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.manifestation.Window', {
    extend: 'core.RestfulWindow',

    rest: 'judicial.manifestation.Restful',

    width: 900,

    getManifestationPanel: function(cfg) {
        if(!this._manifestationPanel)
            this._manifestationPanel = Ext._create('Ext.Panel', {
                title: 'Manifestação',
                border: false,
                frame: true,
                layout: 'form',
                items: [
                    {
                        fieldLabel: 'Diligência',
                        xtype: 'displayfield',
                        name: 'diligence_unicode'
                    },
                    {
                        fieldLabel: 'Pessoa',
                        xtype: 'displayfield',
                        name: 'who_unicode'
                    },
                    {
                        fieldLabel: 'Envolvimento',
                        xtype: 'displayfield',
                        name: 'who_type_display'
                    },
                    {
                        xtype: 'ckeditor',
                        name: 'content',
                        hideLabel: true,
                        height: 315
                    }
                ]
            });

        return this._manifestationPanel;
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                activeTab: 0,
                height: 550,
                // border: false,
                items: [
                    this.getManifestationPanel(),
                    this.getAttachedGrid()
                ]
            });

        return this._tabPanel;
    },

    getAttachedGrid: function() {
        if(!this._attachedGrid)
            this._attachedGrid = Ext._create('judicial.parts.AttachedGrid', {
                title: 'Anexos',
                gridAutoLoad: false
            });

        return this._attachedGrid;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    this.getTabPanel()
                ]
            });

        return this._formPanel;
    },

    sign: function() {
        var rest = this.factoryRestful();

        rest.doRequest(
            rest.getRoute('sign', this.oId, 'PUT', {
                scope: this,
                success: function(xhr) {
                    rst = Ext.decode(xhr.responseText);

                    if(rst.success)
                        Ext.Msg.show({
                            title: 'Assinando documento',
                            icon: Ext.Msg.INFO,
                            buttons: Ext.Msg.OK,
                            msg: 'Documento assinado com sucesso.'
                        });
                    else
                        Ext.Msg.show({
                            title: 'Assinando documento',
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK,
                            msg: rst.message
                        });
                },
                failure: function() {
                    Ext.Msg.show({
                        title: 'Assinando o documento',
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: 'O sistema esta indisponível neste momento.'
                    });
                }
            })
        );
    },

    getButtons: function(cfg) {
        if(!this._buttons)
            this._buttons = [
                {
                    text: 'Assinar',
                    scope: this,
                    handler: this.sign,
                    handle: this.sign
                },
                '->'
            ].concat(judicial.manifestation.Window.superclass.getButtons.call(this, cfg));

        return this._buttons;
    },

    manifestation: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._manifestation = value;

            if(!prevent) this.observeManifestation();
        }

        return this._manifestation;
    },

    observeManifestation: function() {
        var value = this.manifestation();

        if(value) {
            this.getAttachedGrid().enable();
            this.getAttachedGrid().setParam('attached_manifestation', value);
            this.getAttachedGrid().setFilterProperty('attached_manifestation', value, 101);
        }
        else {
            this.getAttachedGrid().disable();
            this.getAttachedGrid().setParam('attached_manifestation', 0);
            this.getAttachedGrid().setFilterProperty('attached_manifestation', 0, 101);
        }
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            border: false,
            buttonAlign: 'left',
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.oId = instance.pk;
                    this.action = 'update';
                    this.manifestation(instance.pk);
                }
            }
        });

        judicial.manifestation.Window.superclass.constructor.call(this, cfg);
        this.manifestation(this.oId || null);
    }
});

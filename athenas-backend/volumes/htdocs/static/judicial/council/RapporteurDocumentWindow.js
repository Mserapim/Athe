/** autogenerated **/
/**
 *
 **/

// Inicializa o ckeditor com o texto justificado e com paragrafo identado
CKEDITOR.on('instanceReady',function(e){
    CKEDITOR.instances[e.editor.name].getCommand('justifyblock').exec();
    CKEDITOR.instances[e.editor.name].getCommand('ident-paragraph').exec();
});

Ext._define('judicial.council.RapporteurDocumentWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.council.RapporteurDocumentRestful',

    width: 950,

    reconsiderate: function() {
        Ext.Msg.show({
            title: 'Reconsiderar voto',
            msg: 'Tem certeza que deseja reconsiderar o voto apresentado como relator?',
            icon: Ext.Msg.QUESTION,
            buttons: Ext.Msg.YESNO,
            scope: this,
            fn: function(btn) {
                if(btn == 'no') return;

                var mask = new Ext.LoadMask(this.getEl(), {msg: 'Reconsiderando...'});

                mask.show();
                this.factoryRestful().reconsiderate(
                    this.oId,
                    {
                        scope: this,
                        fn: function(rst) {
                            if(rst.success) {
                                this.oId = rst.instance.pk;
                                Ext.Msg.show({
                                    title: 'Reconsiderado',
                                    msg: 'Seu voto foi aberto para reconsideração.',
                                    icon: Ext.Msg.INFO,
                                    buttons: Ext.Msg.OK
                                });

                                core.invokeCallback(((this.callback || {}).success || {fn: Ext.emptyFn}), rst);
                            }
                            else
                                Ext.Msg.show({
                                    title: 'Reconsiderando',
                                    msg: rst.message,
                                    icon: Ext.Msg.ERROR,
                                    buttons: Ext.Msg.OK
                                });
                        }
                    },
                    {
                        fn: function(message) {
                            Ext.Msg.show({
                                title: 'Reconsiderando voto',
                                msg: message,
                                icon: Ext.Msg.ERROR,
                                buttons: Ext.Msg.OK
                            });
                        }},
                    {
                        fn: function() { mask.hide(); }
                    }
                );
            }
        });
    },

    getLeftButtons: function(cfg) {
        if(!this._leftButtons)
            this._leftButtons = judicial.council.RapporteurDocumentWindow.superclass.getLeftButtons.call(this, cfg).concat([
                {
                    text: 'Reconsiderar',
                    scope: this,
                    handler: this.reconsiderate
                },
            ]);

        return this._leftButtons;
    },

    readDataCallback: function(instance) {
        this.repporteurDocument(instance.pk);
    },

    repporteurDocument: function(value, dispatch) {
        dispatch = (dispatch === undefined ? true : dispatch);

        if(value !== undefined) {
            this._repporteurDocument = value;

            if(dispatch)
                this.observeRepporteurDocument();
        }

        return this._repporteurDocument;
    },

    observeRepporteurDocument: function() {
        var value = this.repporteurDocument();

        if(value) {
            this.getAttachementPanel().enable();
            this.getAttachementPanel().setParam('attached_document', value);
            this.getAttachementPanel().setFilterProperty('attached_document', value, 100);
        }
        else {
            this.getAttachementPanel().disable();
            this.getAttachementPanel().setParam('attached_document', value);
            this.getAttachementPanel().setFilterProperty('attached_document', value, 100, false);
            this.getAttachementPanel().getStore().removeAll();
        }
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                activeTab: 0,
                height: 600,
                border: false,
                items: [
                    this.getMainPanel(cfg),
                    this.getAttachementPanel(cfg)
                ]
            });

        return this._tabPanel;
    },

    getAttachementPanel: function(cfg) {
        if(!this._attachmentPanel)
            this._attachmentPanel = Ext._create('judicial.parts.AttachedGrid', {
                title: 'Anexos',
                gridAutoLoad: false
            });

        return this._attachmentPanel;
    },

    getMainPanel: function(cfg) {
        if(!this._mainPAnel)
            this._mainPAnel = Ext._create('Ext.Panel', {
                title: 'Voto',
                layout: 'form',
                border: false,
                items: [
                    {
                        xtype: 'panel',
                        frame: true,
                        layout: 'form',
                        items: [
                            {
                                xtype: 'choicefield',
                                fieldLabel: 'Decisão',
                                editable: false,
                                hiddenName: 'rapporteur_vote_type',
                                width: 300,
                                choiceId: 'council.RAPPORTEUR_VOTE_TYPE'
                            }
                        ]
                    },
                    {
                        xtype: 'panel',
                        title: 'Fundamentação do Voto',
                        items: [
                            {
                                xtype: 'ckeditor',
                                name: 'content',
                                height: 450,
                            }
                        ]
                    }
                ]
            });
        return this._mainPAnel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    this.getTabPanel(cfg)
                ]
            });

        return this._formPanel;
    },

    readInstance: function(instance) {
        this.oId = instance.pk;
        this.action = 'update';
        this.repporteurDocument(instance.pk);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: this.readInstance
            }
        });

        judicial.council.RapporteurDocumentWindow.superclass.constructor.call(this, cfg);
        this.repporteurDocument(cfg.oId || null);
    }
});

judicial.PartLawsuitGrid.register('council.rapporteurdocument', 'judicial.council.RapporteurDocumentWindow');

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.taxonomy.LegalClassificationWindow', {
    extend: 'core.RestfulWindow',

    rest: 'judicial.taxonomy.LegalClassificationRestful',

    width: 600,

    getGlossaryField: function() {
        if(!this._glossaryField)
            this._glossaryField = Ext._create('Ext.Panel',{
                title: 'Glossário',
                border: true,
                frame: false,
                autoHeight: true,
                items: [
                    {
                        xtype: "ckeditor",
                        allowBlank: true,
                        fieldLabel: "Glossário",
                        name: "glossary",
                        hideLabel: true,
                        height: 180,
                        submit: true,
                    }
                ]
            });
        return this._glossaryField;
    },

    getMainPanel: function(cfg) {
        if(!this._mainPanel)
            this._mainPanel = Ext._create('Ext.Panel', {
                title: 'Principal',
                border: false,
                frame: true,
                layout: 'form',
                items: [
                    {
                        maxLength: 120,
                        allowBlank: false,
                        fieldLabel: "Título",
                        name: "title",
                        xtype: "textfield",
                        width: 465
                    },
                    {
                        xtype: "numberfield",
                        fieldLabel: "Código CNMP",
                        allowBlank: false,
                        allowDecimals: false,
                        name: "cnmp_code",
                        width: 465
                    },
                    this.getGlossaryField()
                ]
            });

        return this._mainPanel;
    },

    _allTabs: function(cfg) {
        return [
            this.getMainPanel(cfg)
        ]
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                items: [
                    {
                        xtype: 'tabpanel',
                        border: false,
                        activeTab: 0,
                        height: 445,
                        items: this._allTabs(cfg)
                    }
                ]
            });

        return this._formPanel;
    }
});

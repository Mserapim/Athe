/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.SupplementOrdinaceWindow', {
    extend: 'judicial.PartLawsuitWindow',

    rest: 'judicial.parts.SupplementOrdinaceRestful',

    width: 800,

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                activeTab: 0,
                height: 550,
                border: false,
                items: [
                    this.getMainPanel(),
                    this.getBlokeGrid(),
                    this.getInvestigationGrid()
                ]
            });

        return this._tabPanel;
    },

    getBlokeGrid: function() {
        if(!this._blokeGrid)
            this._blokeGrid = Ext._create('judicial.bloke.Grid', {
                title: 'Investigados',
                gridAutoLoad: false
            });

        return this._blokeGrid;
    },

    getInvestigationGrid: function() {
        if(!this._investigationGrid)
            this._investigationGrid = Ext._create('judicial.diligences.JudicialDiligenceGrid', {
                title: 'Diligências',
                gridAutoLoad: false
            });

        return this._investigationGrid;
    },

    getMainPanel: function(cfg) {
        if(!this._mainPanel)
            this._mainPanel = Ext._create('Ext.Panel', {
                title: 'Informações',
                items: [
                    {
                        xtype: 'container',
                        items: [
                            {
                                xtype: 'ckeditor',
                                name: 'justification',
                                height: 420
                            }
                        ]
                    }
                ]
            });

        return this._mainPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    this.getTabPanel()
                ]
            });

        return this._formPanel;
    },

    supplement: function(value, dispatch) {
        dispatch = (dispatch === undefined ? true : dispatch);

        if(value !== undefined) {
            this._supplement = value;

            if(dispatch)
                this.observeSupplement();
        }

        return this._supplement;
    },

    observeSupplement: function() {
        var value = this.supplement();
        var grid;

        if(value) {
            grid = this.getInvestigationGrid();
            grid.enable();
            grid.setParam('part', value);
            grid.setFilterProperty('part', value, 1001);
        }
        else {
            grid = this.getInvestigationGrid();
            grid.disable();
            grid.setParam('part', 0);
            grid.setFilterProperty('part', 0, 1001, false);
        }
    },

    lawsuit: function(value, dispatch) {
        dispatch = (dispatch === undefined ? true : dispatch);

        if(value !== undefined) {
            this._lawsuit = value;

            if(dispatch)
                this.observeLawsuit();
        }

        return this._lawsuit;
    },

    observeLawsuit: function() {
        var value = this.lawsuit();

        if(value) {
            this.getBlokeGrid().enable();
            this.getBlokeGrid().setParam('lawsuit', value);
            this.getBlokeGrid().setFilterProperty('lawsuit', value, 100);
        }
        else {
            this.getBlokeGrid().disable();
            this.getBlokeGrid().setParam('lawsuit', 0);
            this.getBlokeGrid().setFilterProperty('lawsuit', value, 0, false);
            this.getBlokeGrid().getStore().removelAll();
        }
    },

    readDataCallback: function(instance) {
        this.supplement(instance.pk);
        this.lawsuit(instance.lawsuit);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.readDataCallback(instance);
                    this.getFormPanel().getForm().setValues(instance);
                    this.action = 'update';
                }
            }
        });

        judicial.parts.SupplementOrdinaceWindow.superclass.constructor.call(this, cfg);
    }
});

judicial.PartLawsuitGrid.register('judicial.supplementordinace', 'judicial.parts.SupplementOrdinaceWindow');

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.RejectionFactRestful', {
    extend: 'judicial.PartLawsuitRestful',

    resource: 'EJudRejectionFact',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = judicial.parts.RejectionFactRestful.superclass.getFields.call(this, cfg).concat([
                {type: 'string', name: 'despatch'},
                {type: 'string', name: 'stage'},
                {type: 'string', name: 'decision'},
                {type: 'string', name: 'decision_formated'},
                {type: 'date', name: 'decided_at', dateFormat: 'd/m/Y H:i'},
                {type: 'int', name: 'decided_by', useNull: true},
                {type: 'string', name: 'decided_by_unicode'},
                {type: 'int', name: 'type_ordinace', useNull: true},
                {type: 'string', name: 'type_ordinace_display'},
                {type: 'int', name: 'decision_type', useNull: true},
                {type: 'string', name: 'decision_type_display'},
                {type: 'int', name: 'reject_fact_type', useNull: true},
                {type: 'string', name: 'reject_fact_type_display'},
                {type: 'string', name: 'appeal_formated'}
            ]);

        return this._fields;
    },

    rendererDecisionFormated: function(pk, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function(message) {
                Ext.Msg.show({
                    title: 'Buscando documento',
                    msg: message,
                    icon: Ext.Msg.ERROR,
                    buttons: Ext.Msg.OK
                });
            }
        };

        this.doRequest(this.getRoute(
            'renderer_decision_formated',
            pk,
            'GET',
            {
                success: function(xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if(rst.success)
                        core.invokeCallback((cbSuccess || {fn: Ext.emptyFn}), rst.document);
                    else
                        core.invokeCallback((emptyFailure || {fn: Ext.emptyFn}), rst.message);

                },
                failure: function(xhr) {
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function() {
                    core.invokeCallback((cbCallback || {fn: Ext.emptyFn}));
                }
            }
        ));
    }
});

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.RecomendationRestful', {
    extend: 'judicial.PartLawsuitRestful',

    resource: 'EJudRecomendation',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = judicial.parts.RecomendationRestful.superclass.getFields.call(this, cfg).concat([
                {
                    type: "string",
                    name: "content"
                },
                {
                    type: "int",
                    name: "deadline"
                },
                {
                    type: "string",
                    name: "deadline_display"
                },
                {
                    type: "int",
                    name: "deadline_type"
                },
                {
                    type: "string",
                    name: "deadline_type_display"
                },
                {
                    type: "int",
                    name: "finished_by"
                },
                {
                    type: "string",
                    name: "remaining_days"
                },
                {
                    type: "string",
                    name: "finished_by_unicode"
                },
                {
                    type: "date",
                    name: "finished_at",
                    dateFormat: "d/m/Y H:i"
                },
                {
                    type: "string",
                    name: "lawsuit_number"
                },
                {
                    type: "int",
                    name: "out_court_lawsuit_pk"
                },
            ]);

        return this._fields;
    },

    fulfilled: function(recommendationId, cbSuccess, cbFailure, cbCallback) {
        var fallback = {
            scope: window,
            fn: function(){}
        };

        this.doRequest(
            this.getRoute('fulfilled', recommendationId, 'PUT',
                {
                    params: {
                        recommendationId: recommendationId
                    },
                    callback: function() {
                        core.invokeCallback((cbCallback || fallback));
                    },
                    success: function(xhr) {
                        rst = Ext.decode(xhr.responseText);
    
                        if(rst.success)
                            core.invokeCallback((cbSuccess || fallback), rst);
                        else
                            core.invokeCallback((cbFailure || fallback), rst);
                    },
                    failure: function() {
                        core.invokeCallback((cbFailure || fallback), 'O sistema esta indisponível neste momento.');
                    }
                }
            )
        );
    },

    rendered: function(pk, cbSuccess, cbFailure, cbCallback) {
        this.doRequest(
            this.getRoute('render', pk,'GET', {
                    scope: this,
                    callback: function() {
                        core.invokeCallback((cbCallback || {fn: Ext.emptyFn}));
                    },
                    success: function(xhr) {
                        var rst = Ext.decode(xhr.responseText);

                        if(rst.success)
                        core.invokeCallback((cbSuccess || {fn: Ext.emptyFn}), rst);
                        else
                        core.invokeCallback((cbFailure || {fn: Ext.emptyFn}), rst.message);
                    },
                    failure: function() {
                        core.invokeCallback((cbFailure || {fn: Ext.emptyFn}), 'Recurso indisponivel no momento.');
                    }
                }
            )
        );
    },
});

/** autogenerated **/
/**
 *
 **/
Ext._define('judicial.parts.UnConnectWindow', {
    extend: 'judicial.PartLawsuitActionWindow',

    rest: 'judicial.parts.UnConnectRestful',

    width: 750,

    getContentPanel: function(cfg) {
        console.log(cfg);

        if(!this._contentTabPanel)
            this._contentTabPanel = Ext._create('Ext.Panel', {
                title: 'Conteúdo',
                border: false,
                items: [
                    {
                        layout: 'form',
                        border: false,
                        frame: true,
                        defaults: { width: 595 },
                        labelWidth: 120,
                        items: [
                            {
                                allowBlank: false,
                                fieldLabel: "Conexão",
                                name: "unconnect_lawsuit",
                                rest: 'judicial.OutCourtLawsuitRestful',
                                xtype: 'rest-autocompletefield',
                                preFilter: [
                                    {property: 'connections__lawsuit', value: cfg.params.lawsuit, stage: 1},
                                    {property: 'connections__unconnected_by', value: null, stage: 2}
                                ]
                            }
                        ]
                    }
                ]
            });

        return this._contentTabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: [
                    Ext._create('Ext.TabPanel', {
                        activeTab: 0,
                        items: [
                            this.getContentPanel(cfg)
                        ]
                    })
                ]
            });

        return this._formPanel;
    },

    unconnect: function(value, prevent) {
        prevent = core.nullValue(prevent, false);

        if(value !== undefined) {
            this._unconnect = value;

            if(!prevent) this.observeUnConnect();
        }

        return this._unconnect;
    },

    observeUnConnect: function() {
        var value = this.unconnect();
    },

    readDataCallback: function(instance) {
        this.unconnect(instance.pk);
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            buttonAlign: 'left',
            border: false,
            disableSaveAndNew: true,
            saveAndContinue: {
                scope: this,
                fn: function(instance) {
                    this.unconnect(instance.pk);

                    this.getFormPanel().getForm().setValues(instance);
                    this.oId = instance.pk;
                    this.action = 'update';
                }
            }
        });

        judicial.parts.UnConnectWindow.superclass.constructor.call(this, cfg);
        this.observeUnConnect();
    }
});

judicial.PartLawsuitGrid.register('judicial.unconnect', 'judicial.parts.UnConnectWindow');

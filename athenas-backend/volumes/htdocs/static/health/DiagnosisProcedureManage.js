/** autogenerated **/
Ext._define('health.DiagnosisProcedureManage', {
    extend: 'toolkit.widget.TabPanel',

    getDiagnosisProcedureGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.DiagnosisProcedureGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Procedimento Diagnóstico'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getDiagnosisProcedureGrid(),
                ]
            }
        );

        health.DiagnosisProcedureManage.superclass.constructor.call(this, cfg);
    }
});


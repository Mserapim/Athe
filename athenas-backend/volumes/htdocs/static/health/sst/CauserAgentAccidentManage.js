/** autogenerated **/
Ext._define('health.sst.CauserAgentAccidentManage', {
    extend: 'toolkit.widget.TabPanel',

    getCauserAgentAccidentGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.sst.CauserAgentAccidentGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Agente Causador ou Situação do Acidente de Trabalho'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getCauserAgentAccidentGrid(),
                ]
            }
        );

        health.sst.CauserAgentAccidentManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('health.sst.EpiManage', {
    extend: 'toolkit.widget.TabPanel',

    getEpiGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.sst.EpiGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Epi'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getEpiGrid(),
                ]
            }
        );

        health.sst.EpiManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('health.sst.ExamSstManage', {
    extend: 'toolkit.widget.TabPanel',

    getExamSstGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('health.sst.ExamSstGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Exame'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getExamSstGrid(),
                ]
            }
        );

        health.sst.ExamSstManage.superclass.constructor.call(this, cfg);
    }
});


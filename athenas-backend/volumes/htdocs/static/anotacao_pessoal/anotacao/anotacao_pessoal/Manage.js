/** autogenerated **/
Ext._define('anotacao_pessoal.anotacao.anotacao_pessoal.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getAnotacaoPessoalGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('anotacao_pessoal.anotacao.anotacao_pessoal.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'AnotacaoPessoal'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getAnotacaoPessoalGrid(),
                ]
            }
        );

        anotacao_pessoal.anotacao.anotacao_pessoal.Manage.superclass.constructor.call(this, cfg);
    }
});


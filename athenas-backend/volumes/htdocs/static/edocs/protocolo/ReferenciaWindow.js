/** autogenerated **/
/**
 *
 **/
Ext._define('edocs.protocolo.ReferenciaWindow', {
    extend: 'core.RestfulWindow',

    rest: 'edocs.protocolo.ReferenciaRestful',

    width: 850,

    extractDataForm: function() { 
        var values = edocs.protocolo.ReferenciaWindow.superclass.extractDataForm.call(this);
        var protocolCombo = this.getProtocolField().getComboField();
        var protocolId = protocolCombo.getValue();
        var protocolRecord;

        if(protocolId) {
            protocolRecord = protocolCombo.getStore().getAt(
                protocolCombo.getStore().find('pk', protocolId)
            );

            Ext.apply(
                values,
                {
                    protocolo_codigo: protocolRecord.get('codigo'),
                    protocolo_assunto: protocolRecord.get('assunto'),
                }
            );
        }

        return values;
    },

    getProtocolField: function(cfg) {
        if(!this._protocolField)
            this._protocolField = Ext._create('core.fields.AutocompleteField', {
                fieldLabel: "Protocolo",
                allowBlank: true,
                rest: "edocs.protocolo.ProtocoloRestful",
                name: "protocolo"
            });

        return this._protocolField;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.getProtocolField(),
                    {
                        xtype: 'ckeditor',
                        name: 'observation',
                        hideLabel: true,
                        height: 300
                    }
                ]
            });

        return this._formPanel;
    }
});

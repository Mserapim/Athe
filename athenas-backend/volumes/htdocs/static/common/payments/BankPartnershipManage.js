/** autogenerated **/
Ext._define('common.payments.BankPartnershipManage', {
    extend: 'toolkit.widget.TabPanel',

    getBankPartnershipGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('common.payments.BankPartnershipGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Boletos Gerados'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getBankPartnershipGrid(),
                ]
            }
        );

        common.payments.BankPartnershipManage.superclass.constructor.call(this, cfg);
    }
});


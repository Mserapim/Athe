/** autogenerated **/
/**
 *
 **/
Ext._define('common.internalSecurity.incidentReport.Window', {
    extend: 'core.RestfulWindow',

    rest: 'common.internalSecurity.incidentReport.Restful',

    width:850,
    buttonAlign: 'left',
    saveAndContinue: true,

    statics: {
        registerOptions: function(title, value, enable) {
            var Klass = common.internalSecurity.incidentReport.Window;
            var options = (Klass._options || []);

            options.push({ title: title, value: value, enable: enable });

            Klass._options = options;
        },

        options: function(scope) {
            var factoryHandler = function(value) {
                return function() {
                    scope.finish(value);
                }
            };

            var Klass = common.internalSecurity.incidentReport.Window;
            var options = (Klass._options || []);

            return options.map(
                function(option) {
                    return {
                        text: option.title,
                        enable: option.enable,
                        handler: factoryHandler(option.value)
                    }
                }
            );
        }
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                  {
                      xtype: 'displayfield',
                      fieldLabel: 'Reportado por',
                      name: 'employee_unicode',
                      value: 'undefined'
                  },
                  {
                      fieldLabel: "Reportado em",
                      name: "reported_at_formated",
                      xtype: "displayfield",
                  },
                  {
                      xtype: 'container',
                      items: [
                          {
                              allowBlank: false,
                              name: "content",
                              xtype: "ckeditor"
                          }
                      ]
                  }
                ]
            });

        return this._formPanel;
    },

    finish: function(value) {
        var mask = new Ext.LoadMask(this.getEl(), {msg: 'Finalizando...'});
        mask.show();
        this.factoryRestful().finish(
            {
                pk: this.oId,
                type_finish: value
            },
            {
                scope: this,
                fn: function(obj) {
                    Ext.Msg.show({
                        title: this.title,
                        icon: obj.success ? Ext.Msg.INFO : Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: obj.message
                    });
                    this.destroy();
                }
            },
            {
                scope: this,
                fn: function(message) {
                    Ext.Msg.show({
                        title: this.title,
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: message
                    });
                }
            },
            {
                fn: function() { mask.hide(); }
            }
        );
    },

    getButtons: function(cfg) {
        if(!this._buttons) {
            this._buttons = [
                {
                    text: 'Finalizar',
                    menu: common.internalSecurity.incidentReport.Window.options(this)
                },
                '->'
            ].concat(common.internalSecurity.incidentReport.Window.superclass.getButtons.call(this, cfg));
        }

        return this._buttons;
    },
});

var rest = Ext._create('standard.ChoiceRestful');
rest.doRequest(
    rest.getRoute('read', undefined, 'GET', {
        params: {
            filter: Ext.encode([
                {property: 'cache_path', value: 'internal_security.CLOSE_OPTIONS'}
            ])
        },
        success: function(xhr) {
            var rst = Ext.decode(xhr.responseText);

            if (rst.success) {
                rst.collection.forEach(
                    function(choice) {
                        common.internalSecurity.incidentReport.Window.registerOptions(
                            choice.unicode,
                            choice.value,
                            true
                        );
                    }
                )
            } else {
                common.internalSecurity.incidentReport.Window.registerOptions('Erro carregando opções', 1000, false);
            }
        }
    })
);

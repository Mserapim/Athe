/** autogenerated **/
Ext._define('standard.emailtemplate.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('standard.emailtemplate.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Modelos de Email'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getGrid(),
                ]
            }
        );

        standard.emailtemplate.Manage.superclass.constructor.call(this, cfg);
    }
});


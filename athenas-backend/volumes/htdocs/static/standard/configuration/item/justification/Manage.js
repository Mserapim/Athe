/** autogenerated **/
Ext._define('standard.configuration.item.justification.Manage', {
    extend: 'toolkit.widget.TabPanel',

    ItemGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('standard.configuration.item.justification.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Configuração de Parâmetros'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.ItemGrid(),
                ]
            }
        );

        standard.configuration.item.justification.Manage.superclass.constructor.call(this, cfg);
    }
});


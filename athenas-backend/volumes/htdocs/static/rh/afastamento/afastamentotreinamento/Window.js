/** autogenerated **/
/**
 *
 **/
Ext._define('rh.afastamento.afastamentotreinamento.Window', {
    extend: 'rh.afastamento.afastamento.Window',
    rest: 'rh.afastamento.afastamentotreinamento.Restful',

    constructor: function(cfg) {
        rh.afastamento.afastamentotreinamento.Window.superclass.constructor.call(this, cfg);
    },

    _observe: function() {
        rh.afastamento.afastamentotreinamento.Window.superclass._observe.call(this, {});
        if(this.oId) {
            this.getInstitutionField().enable();
        }
        else {
            this.getInstitutionField().disable();
        }
    },

    getTabPanel: function(cfg_window, cfg) {
        if(!this._tabPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {height: 550});
            this._tabPanel = rh.afastamento.afastamentotreinamento.Window.superclass.getTabPanel.call(this, cfg_window, cfg);
        }
        return this._tabPanel;
    },

    getPanelInformationItems: function(cfg_window){
        var items = rh.afastamento.afastamentotreinamento.Window.superclass.getPanelInformationItems.call(this, cfg_window);
        items.push(Ext._create('core.fields.AutocompleteField', {
            fieldLabel: "Curso",
            allowBlank: true,
            rest: "rh.curso.Restful",
            name: "curso"
        }));
        items.push({
            xtype: "numberfield",
            fieldLabel: "Carga horária",
            allowBlank: false,
            allowDecimals: false,
            name: "carga_horaria"
        });
        items.push(this.getInstitutionField(cfg_window, {}));
        return items;
    },

    getInstitutionField: function(cfg_window, cfg) {
        if(!this._institutionField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, 
                {
                    fieldLabel: 'Instituição',
                    width: 400,
                    height: 270,
                    sourceRest: 'rh.generalorgan.Restful',
                }
            );
            Ext.apply(cfg, 
                {
                    name: 'instituicao',
                    relatedname: 'afatreinamento_instituicao',
                    rest: (cfg_window.rest || this.rest),
                    oId: cfg_window.oId
                }
            );
            this._institutionField = Ext._create('core.fields.RelatedRestfulField', cfg);
        }
        return this._institutionField;
    },
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.afastamento.ManagerEmployeeGrid', {
    extend: 'rh.afastamento.baselicencaafastamento.Grid',

    restWindow: 'rh.afastamento.ManagerEmployeeWindow',

    statics: {
        my_types: [],
        my_menu_structure: [],
        my_filter_structure: [],

        register: function (name, label, iconCls, Class, groupMenu, checked, value) {
            rh.afastamento.ManagerEmployeeGrid.my_types.push({
                name: name,
                label: label,
                iconCls: iconCls,
                Class: Class,
                groupMenu: groupMenu,
                checked: checked,
                value: value
            });
        },

        getGroupMenu: function (menu, name) {
            var groupMenu;
            menu.forEach(
                function (item) {
                    if (item.name == name) {
                        groupMenu = item;
                        return false;
                    }
                }
            );
            return groupMenu;
        },

        getMenuFilterStructure: function (scope, department) {
            if (!scope._menuFilterStructureEmployee) {
                rh.afastamento.ManagerEmployeeGrid.my_types.forEach(
                    function (item) {
                        if (item.Class === '') {
                            rh.afastamento.ManagerEmployeeGrid.my_filter_structure.push({
                                name: item.name,
                                groupMenu: item.groupMenu,
                                text: item.label,
                                scope: scope,
                                iconCls: item.iconCls,
                                menu: []
                            });
                        } else {
                            rh.afastamento.ManagerEmployeeGrid.my_filter_structure.push(
                                new Ext.menu.CheckItem({
                                    groupMenu: item.groupMenu,
                                    inherited: true,
                                    name: item.name,
                                    text: item.label,
                                    checked: item.checked,
                                    value: item.value,
                                    scope: scope,
                                    hideOnClick: false,
                                })
                            );
                        }
                    }
                );

                scope._menuFilterStructureEmployee = [
                    {
                        name: 'selection',
                        groupMenu: '',
                        text: 'Selecionar',
                        scope: scope,
                        menu: [
                            {
                                name: 'all',
                                groupMenu: 'selection',
                                text: 'Todos',
                                scope: scope,
                                hideOnClick: false,
                                handler: function () {
                                    this.markFilter(rh.afastamento.ManagerEmployeeGrid.getMenuFilterStructure(this, department), 'all');
                                },
                            },
                            {
                                name: 'none',
                                groupMenu: 'selection',
                                text: 'Nenhum',
                                scope: scope,
                                hideOnClick: false,
                                handler: function () {
                                    this.markFilter(rh.afastamento.ManagerEmployeeGrid.getMenuFilterStructure(this, department), 'none');
                                },
                            }
                        ]
                    },
                    {
                        name: 'servidor__tipo',
                        groupMenu: '',
                        text: 'Tipos de Servidores',
                        scope: scope,
                        menu: [
                            new Ext.menu.CheckItem({
                                name: 'administrative',
                                groupMenu: 'servidor__tipo',
                                text: 'Administrativos',
                                checked: department == 'rh' ? true : false,
                                value: 'S',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'member',
                                groupMenu: 'servidor__tipo',
                                text: 'Membros',
                                checked: department == 'expediente' || department == 'rh' ? true : false,
                                value: 'M',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'trainee',
                                groupMenu: 'servidor__tipo',
                                text: 'Estagiários',
                                checked: false,
                                value: 'E',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'voluntary',
                                groupMenu: 'servidor__tipo',
                                text: 'Voluntário',
                                checked: false,
                                value: 'V',
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'outsourced',
                                groupMenu: 'servidor__tipo',
                                text: 'Terceirizado',
                                checked: false,
                                value: 'T',
                                hideOnClick: false,
                                scope: scope
                            }),
                        ]
                    },
                    {
                        name: 'estado',
                        groupMenu: '',
                        text: 'Situação',
                        scope: scope,
                        menu: [
                            new Ext.menu.CheckItem({
                                name: 'active',
                                groupMenu: 'estado',
                                text: 'EM FRUIÇÃO',
                                checked: true,
                                value: 2,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'scheduled',
                                groupMenu: 'estado',
                                text: 'AGENDADO',
                                checked: true,
                                value: 1,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'canceled',
                                groupMenu: 'estado',
                                text: 'CANCELADO',
                                checked: false,
                                value: 4,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'finished',
                                groupMenu: 'estado',
                                text: 'USUFRUÍDO',
                                checked: true,
                                value: 3,
                                hideOnClick: false,
                                scope: scope
                            })
                        ]
                    },
                    {
                        name: 'alteracao',
                        groupMenu: '',
                        text: 'Tipo de alteração',
                        scope: scope,
                        menu: [
                            new Ext.menu.CheckItem({
                                name: 'interruption',
                                groupMenu: 'alteracao',
                                text: 'Interrupção',
                                checked: false,
                                value: 5,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'request',
                                groupMenu: 'alteracao',
                                text: 'Alteração a pedido',
                                checked: false,
                                value: 2,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'revocation',
                                groupMenu: 'alteracao',
                                text: 'Revogação',
                                checked: false,
                                value: 1,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'suspension',
                                groupMenu: 'alteracao',
                                text: 'Suspensão',
                                checked: false,
                                value: 3,
                                hideOnClick: false,
                                scope: scope
                            }),
                            new Ext.menu.CheckItem({
                                name: 'canceled',
                                groupMenu: 'alteracao',
                                text: 'Cancelado',
                                checked: false,
                                value: 4,
                                hideOnClick: false,
                                scope: scope
                            })
                        ]
                    }
                ];
                if (scope._menuFilterStructureEmployee && rh.afastamento.ManagerEmployeeGrid.my_filter_structure) {
                    rh.afastamento.ManagerEmployeeGrid.my_filter_structure.forEach(
                        function (item) {
                            if (item.groupMenu !== '' && item.groupMenu !== undefined) {
                                var groupMenu = rh.afastamento.ManagerEmployeeGrid.getGroupMenu(
                                    rh.afastamento.ManagerEmployeeGrid.my_filter_structure, item.groupMenu);
                                if (groupMenu !== undefined) {
                                    groupMenu.menu.push(item);
                                }
                            }
                        }
                    );
                    var newMenu = rh.afastamento.ManagerEmployeeGrid.getGroupMenu(rh.afastamento.ManagerEmployeeGrid.my_filter_structure, 'new');
                    if (newMenu) {
                        scope._menuFilterStructureEmployee = scope._menuFilterStructureEmployee.concat(newMenu.menu);
                    }
                }
            }
            return scope._menuFilterStructureEmployee;
        },
    },

    getColumnModelItems: function () {
        return rh.afastamento.ManagerEmployeeGrid.superclass.getColumnModelItems.call(this, {});
    },

    getToolbar: function (cfg) {
        var newComponent;
        var editComponent;
        var deleteComponent;
        var downloadComponent;

        if (!this._toolbar) {
            cfg = core.nullValue(cfg, {});
            Ext.apply(cfg, { gridAutoLoad: false, });

            this._toolbar = rh.afastamento.ManagerEmployeeGrid.superclass.getToolbar.call(this, cfg);
            this._toolbar.findBy(
                function (item) {
                    if (item.text == 'Novo')
                        newComponent = item;
                    if (item.text == 'Editar')
                        editComponent = item;
                    if (item.text == 'Remover')
                        deleteComponent = item;
                    if (item.text == 'Download')
                        downloadComponent = item;
                }
            );
            this._toolbar.remove(newComponent);
            this._toolbar.remove(editComponent);
            this._toolbar.remove(deleteComponent);
            this._toolbar.remove(downloadComponent);

            this._toolbar.insert(this._toolbar.items.length,
                {
                    xtype: 'button',
                    text: 'Localizar',
                    iconCls: true,
                    icon: '/' + global.Context + '/static/images/find.png',
                    handler: this.setFilterParams,
                    scope: this
                }
            );

            this._dateStartField = new Ext.form.DateField({
                emptyText: 'Início',
                format: 'd/m/Y',
                width: 90,
                enableKeyEvents: true,
                listeners: {
                    scope: this,
                    keypress: function (text, event) {
                        if (event.getCharCode() == event.RETURN || event.getCharCode() == event.TAB) {
                            this.setFilterParams();
                        }
                    }
                }
            });
            this._dateEndField = new Ext.form.DateField({
                emptyText: 'Fim',
                format: 'd/m/Y',
                width: 90,
                enableKeyEvents: true,
                listeners: {
                    scope: this,
                    keypress: function (text, event) {
                        if (event.getCharCode() == event.RETURN || event.getCharCode() == event.TAB) {
                            this.setFilterParams();
                        }
                    }
                }
            });
            this._checkChangeField = new Ext.form.Checkbox({
                boxLabel: 'Alterações',
                checked: false
            });
            this._toolbar.insert(5,
                this._checkChangeField
            );
            this._toolbar.insert(6,
                '-'
            );
            this._toolbar.insert(7,
                this._dateStartField
            );
            this._toolbar.insert(9,
                this._dateEndField
            );
            this._toolbar.insert(12,
                '-'
            );
            this._toolbar.insert(14,
                '-'
            );
            this._toolbar.insert(15,
                downloadComponent
            );

            this.setFilterParams();
        }
        return this._toolbar;
    },

    getFilterMenu: function () {
        return rh.afastamento.ManagerEmployeeGrid.getMenuFilterStructure(this, this.department);
    },

    setFilterParams: function () {
        var store = this.getStore();

        this.setKeyWord(this.getKeywordField().getValue());

        this._filterParams = { 'tipo': [], 'estado': [], 'alteracao': [], 'servidor__tipo': [] };
        this._filterSituation = [];
        this._filterChange = [];
        this.toFind(rh.afastamento.ManagerEmployeeGrid.getMenuFilterStructure(this, this.department));

        var dateStart = Ext.util.Format.date(this._dateStartField.getValue(), 'Y-m-d');
        var dateEnd = Ext.util.Format.date(this._dateEndField.getValue(), 'Y-m-d');

        var params = [];
        params.push({ 'property': 'tipo__in', 'value': this._filterParams.tipo, 'stage': 0 });
        params.push({ 'property': 'estado__in', 'value': this._filterParams.estado, 'stage': 0 });
        params.push({ 'property': 'alteracao__in', 'value': this._filterParams.alteracao, 'stage': 0 });
        params.push({ 'property': 'servidor__tipo__in', 'value': this._filterParams.servidor__tipo, 'stage': 0 });

        if (dateStart !== '' && dateEnd === '') {
            if (this._checkChangeField.checked) {
                params.push({ 'property': 'modified_at__gte', 'value': dateStart, 'stage': 1 });
            }
            else {
                params.push({ 'property': 'data_inicio__gte', 'value': dateStart, 'stage': 1 });
            }
        } else if (dateEnd !== '' && dateStart === '') {
            if (this._checkChangeField.checked) {
                params.push({ 'property': 'modified_at__lte', 'value': dateEnd, 'stage': 1 });
            } else {
                params.push({ 'property': 'data_fim__lte', 'value': dateEnd, 'stage': 1 });
            }
        } else if (dateStart !== '' && dateEnd !== '') {
            if (this._checkChangeField.checked) {
                params.push({ 'property': 'modified_at__gte', 'value': dateStart, 'stage': 1 });
                params.push({ 'property': 'modified_at__lte', 'value': dateEnd, 'stage': 2 });
            } else {
                params.push({ 'property': 'data_inicio__gte', 'value': dateStart, 'stage': 1 });
                params.push({ 'property': 'data_fim__lte', 'value': dateEnd, 'stage': 2 });
            }
        }

        this.setFilter(params);
    },

    doKeywordFilter: function (keyword) {
        this.setKeyWord(keyword);
        this.setFilterParams();
    },

    setKeyWord: function (keyword) {
        var store = this.getStore();
        if (keyword !== '')
            store.baseParams.keyword = keyword;
        else {
            store.baseParams.keyword = null;
            delete store.baseParams.keyword;
        }
    },

    toFind: function (menu) {
        menu.forEach(
            function (item) {
                if (item.menu) {
                    this.toFind(item.menu);
                } else {
                    if (!item.handler) {
                        if (item.inherited && item.checked && item.value) {
                            this._filterParams.tipo.push(item.value);
                        }
                        if (item.groupMenu == 'estado' && item.checked) {
                            this._filterParams.estado.push(item.value);
                        }
                        if (item.groupMenu == 'alteracao' && item.checked) {
                            this._filterParams.alteracao.push(item.value);
                        }
                        if (item.groupMenu == 'servidor__tipo' && item.checked) {
                            this._filterParams.servidor__tipo.push(item.value);
                        }
                    }
                }
            },
            this
        );
    },

    markFilter: function (menu, option) {
        var value = option == 'all' ? true : false;
        this._dateStartField.setValue('');
        this._dateEndField.setValue('');
        this._checkChangeField.setValue(false);
        menu.forEach(
            function (item) {
                if (item.menu) {
                    this.markFilter(item.menu, option);
                } else {
                    if (!item.handler) {
                        if (item.inherited) {
                            item.setChecked(value);
                        }
                        if (value === true && item.groupMenu == 'estado' && (item.name == 'active' || item.name == 'scheduled')) {
                            item.setChecked(value);
                        }
                        if (value === true && item.groupMenu == 'alteracao') {
                            item.setChecked(!value);
                        }
                        if (item.groupMenu == 'servidor__tipo') {
                            item.setChecked(value);
                        }
                    }
                }
            },
            this
        );
    },

    createItem: function (values, _class) { },

    updateItem: function (record) { },

    removeItems: function (record, cfg) { },

});

core.RestfulGrid.register(
    'rh.afastamento.ManagerEmployeeRestful',
    'rh.afastamento.ManagerEmployeeGrid'
);

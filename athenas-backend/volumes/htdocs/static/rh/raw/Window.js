/** autogenerated **/
/**
 *
 **/
Ext._define('rh.raw.Window', {
    extend: 'core.RestfulWindow',

    width: 550,
    tabPanelHeight: 450,
    formHeight: 700,


    constructor: function(cfg) {
        rh.raw.Window.superclass.constructor.call(this, cfg);
        this._observe();
    },

    readDataCallback: function(instance){
        this._observe();
    },

    _observe: function() {},

    getFormPanel: function(cfg_window, cfg) {
        if(!this._formPanel){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    border: false,
                    height: this.formHeight,
                    items: [
                        this.getTabPanel(cfg_window, {})
                    ]
                }
            );
            this._formPanel = Ext._create('Ext.form.FormPanel', cfg);
        }
        return this._formPanel;
    },

    getTabPanel: function(cfg_window, cfg) {
        if(!this._tabPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    border: false,
                    activeTab: 0,
                    height: this.tabPanelHeight,
                }
            );
            Ext.apply(
                cfg,
                {
                    items: [
                        this.getTabPanelItems(cfg_window)
                    ]
                }
            );
            this._tabPanel = Ext._create('Ext.TabPanel', cfg);
        }
        return this._tabPanel;
    },

    getTabPanelItems: function(cfg_window){
        return [
            this.getPanelInformation(cfg_window, {}),
            this.getPanelText(cfg_window, {})
        ];
    },

    getPanelInformation: function(cfg_window, cfg) {
        if(!this._informationPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Dados',
                    frame: true,
                    border: false,
                    layout: 'form',
                    defaults: {
                        width: 400
                    }
                }
            );
            Ext.apply(
                cfg,
                {
                    items: [
                        this.getPanelInformationItems(cfg_window)
                    ]
                }
            );
            this._informationPanel = Ext._create('Ext.Panel', cfg);
        }
        return this._informationPanel;
    },

    getPanelInformationItems: function(cfg_window){
        return [];
    },

    changeValidEmployeeField: function(combo, value, oldvalue, valid){
        // console.debug('changeValidEmployeeField: implement your code here');
    },

    getPanelText: function(cfg_window, cfg) {
        if(!this._textPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Texto',
                    frame: true,
                    border: false,
                    layout: 'form',
                    // autoHeight: true,
                }
            );
            Ext.apply(
                cfg,
                {
                    items: this.getPanelTextItems(cfg_window),
                }
            );
            this._textPanel = Ext._create('Ext.Panel', cfg);
        }
        return this._textPanel;
    },

    getPanelTextItems: function(cfg_window) {
        return [];
    },
});


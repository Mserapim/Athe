/** autogenerated **/
/**
 *
 **/
Ext._define('rh.raw.employee.Window', {
    extend: 'rh.raw.Window',

    width: 550,

    constructor: function(cfg) {
        rh.raw.employee.Window.superclass.constructor.call(this, cfg);
    },

    _observe: function() {
        rh.raw.employee.Window.superclass._observe.call(this, {});
        var values = this.getParams();
        if(values != undefined && values.servidor != undefined){
            if(this.getEmployeeField().getValue() != values.servidor)
                this.getEmployeeField().setValue(values.servidor);
            this.getEmployeeField().setReadOnly(true);
        }
        else
            this.getEmployeeField().setReadOnly(false);
    },

    getPanelInformationItems: function(cfg_window){
        return [
            this.getEmployeeField(cfg_window, {}),
        ];
    },

    getEmployeeField: function(cfg_window, cfg){
        if(!this._employeeField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    fieldLabel: 'Servidor *',
                    allowBlank: false,
                    rest: 'rh.employee.Restful',
                    name: 'servidor',
                    readOnly: false,
                    comboListeners: {
                        scope: this,
                        select: function(combo, record, index){
                            this._observe();
                        },
                        changevalid: function(combo, value, oldvalue, valid) {
                            try{
                                this.changeValidEmployeeField(combo, value, oldvalue, valid);
                            }catch(err){}
                        }
                    }
                }
            );
            Ext.apply(
                cfg,
                {
                    name: 'servidor',
                }
            );
            this._employeeField = Ext._create('rh.raw.AutocompleteField', cfg);
        }
        return this._employeeField;
    },

    getPanelTextItems: function(cfg_window) {
        return [
            {
                xtype: 'checkbox',
                boxLabel: 'Gera Anota\u00e7\u00e3o',
                allowBlank: true,
                hideLabel: true,
                checked: true,
                name: 'anota'
            },
            {
                hideLabel: true,
                name: 'texto',
                xtype: 'ckeditor'
            }
        ];
    },
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gratifications_manager.gratifications.WorkplaceGrid', {
    extend: 'rh.employee.workplace.Grid',

    restWindow: 'rh.gratifications_manager.gratifications.Window',

    keywordFieldWidth: 120,

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(cfg, {});
        rh.gratifications_manager.gratifications.WorkplaceGrid.superclass.constructor.call(this, cfg);
    },

    getColumnModelItems: function() {
        if(!this._columnModelItems){
            this._columnModelItems = rh.gratifications_manager.gratifications.WorkplaceGrid.superclass.getColumnModelItems.call(this, {});
            this.setItemsConfig(this._columnModelItems, [
                {header: 'Servidor', dataIndex: 'servidor_unicode', width: 220, hidden: true},
                {header: 'Chefe imediato', dataIndex: 'chefe_imediato_unicode', width: 200, hidden: true},
            ]);
        }
        return this._columnModelItems;
    },

    filterWorkAssignment: function() {
        alert('Este filtro não é permitido neste local');
    },

    _createWorkAssignment: function(pk) {
        var rest = this.factoryRestful();
        var mask = new Ext.LoadMask(this.getEl(), {msg: 'criando Exercício...'});

        mask.show();
        rest.createWorkAssignment(
            pk,
            {
                scope: this,
                fn: function() {
                    this.getStore().reload();
                }
            },
            {
                fn: function(message) {
                    Ext.Msg.show({
                        title: 'Adicionando',
                        msg: message,
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK
                    });
                }
            },
            {
                fn: function() {
                    mask.hide();
                }
            }
        );
    },

    getCreateWorkAssignmentAction: function(cfg) {
        if(!this._reportsAction){
            this._reportsAction = Ext._create('Ext.Button', {
                text: 'Criar Exercício',
                iconCls: 'icon-core icon-core-reports',
                scope: this,
                handler: function() {
                    if(this.getSelectionModel().getSelected())
                        this._createWorkAssignment(this.getSelectionModel().getSelected().id);
                    else
                        Ext.Msg.show({
                            title: 'Criando Exercício...',
                            msg: 'Primeiro selecione uma lotação.',
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK
                        });

                }
            });
        }
        return this._reportsAction;
    },

    getToolbar: function(cfg) {
        this._toolbar = rh.gratifications_manager.gratifications.WorkplaceGrid.superclass.getToolbar.call(this, cfg);
        this._toolbar.insert(5, '-');
        this._toolbar.insert(6, this.getCreateWorkAssignmentAction());
        return this._toolbar;
    },
});

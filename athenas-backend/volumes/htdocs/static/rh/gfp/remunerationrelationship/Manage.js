/** autogenerated **/
Ext._define('rh.gfp.remunerationrelationship.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getRemunerationRelationshipGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.gfp.remunerationrelationship.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Múltiplos Vínculos'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getRemunerationRelationshipGrid(),
                ]
            }
        );

        rh.gfp.remunerationrelationship.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.conference.payroll.ConferenceRestful', {
    extend: 'core.Restful',

    resource: 'GFPConferencePayroll',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = rh.gfp.conference.payroll.ConferenceRestful.superclass.getFields.call(this, cfg).concat([
                { name: "created_by",type: "int",useNull: true},
                { name: "created_by_unicode",type: "string"},
                { name: "finished_by",type: "int",useNull: true},
                { name: "finished_by_unicode",type: "string"},
                { name: "payroll",type: "int",useNull: true},
                { name: "payroll_unicode",type: "string"},
                { name: "created_at",type: "date",dateFormat: "d/m/Y H:i"},
                { name: "finished_at",type: "date",dateFormat: "d/m/Y H:i"},
                { name: "payroll_pendencies", type: "float",useNull: true},
            ]);

        return this._fields;
    },

    _process: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            params.actionCustom,
            null,
            'POST',
            {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    if (rst.success) {
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    }
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },
});

/** autogenerated **/
Ext._define('rh.gfp.conference.payroll.ConferenceManage', {
    extend: 'toolkit.widget.TabPanel',

    getConferenceGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.gfp.conference.payroll.ConferenceGrid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Conferência de Folha'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getConferenceGrid(),
                ]
            }
        );

        rh.gfp.conference.payroll.ConferenceManage.superclass.constructor.call(this, cfg);
    }
});


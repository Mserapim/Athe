/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.payroll.PeriodRestful', {
    extend: 'core.Restful',

    resource: 'GFPPeriod',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = rh.gfp.payroll.PeriodRestful.superclass.getFields.call(this, cfg).concat([
                {type: 'string', name: 'id'},
                {type: 'float', name: 'auxilio_creche', useNull: true},
                {type: 'float', name: 'auxilio_alimentacao', useNull: true},
                {type: 'float', name: 'salario_familia', useNull: true},
                {type: 'string', name: 'mes'},
                {type: 'string', name: 'mes_display'},
                {type: 'string', name: 'ano'},
                {type: 'float', name: 'salario_teto_adm', useNull: true},
                {type: 'float', name: 'salario_teto_membros', useNull: true},
                {type: 'float', name: 'salario_minimo', useNull: true},
                {type: 'int', name: 'next_period', useNull:true},
                {type: 'int', name: 'previous_period', useNull:true},
                {type: 'str', name: 'data_corte_ferias', useNull:true},
            ]);

        return this._fields;
    },

    _process: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            params.actionCustom,
            null,
            'POST',
            {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if (rst.success) {
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    } else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    }

});

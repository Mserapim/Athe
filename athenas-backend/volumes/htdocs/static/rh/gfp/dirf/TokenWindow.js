/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.dirf.TokenWindow', {
    extend: 'core.RestfulWindow',

    rest: 'rh.gfp.dirf.TokenRestful',

    width: 550,

    _observe: function() {
        var grid_events = this.getEventsPanel();
        if(this.oId) {
            grid_events.enable();
        }else{
            grid_events.disable();
        }
    },

    getEventsPanel: function(cfg) {
        if(!this._eventsPanel)
            this._eventsPanel = Ext._create('core.fields.RelatedRestfulField', {
                frame: true,
                border: false,
                title: "Eventos", 
                        // fieldLabel: 'Funcionalidades',
                name: 'eventos',
                relatedname: 'as_token',
                width: 540,
                height: 370,
                rest: this.rest,
                sourceRest: 'rh.gfp.payroll.EventRestful',
                oId: cfg.oId,
            });

        return this._eventsPanel;
    },

    getTabPanel: function(cfg) {
        if(!this._tabPanel)
            this._tabPanel = Ext._create('Ext.TabPanel', {
                height: 400,
                border: false,
                activeTab: 0,
                deferredRender: false,
                items: [
                    this.getTokenPanel(cfg),
                    this.getEventsPanel(cfg),
                ]
            });

        return this._tabPanel;
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                items: this.getTabPanel(cfg),
                submit_all_checks: true
            });

        return this._formPanel;
    },

    getTokenPanel: function(cfg) {
        if(!this._tokenPanel)
            this._tokenPanel = Ext._create('Ext.Panel', {
                frame: true,
                border: false,
                defaults: {
                    width: 400
                },
                title: 'Geral',
                layout: 'form',
                items: [
                    {
                        xtype: "rest-autocompletefield", 
                        fieldLabel: "dialect", 
                        allowBlank: false, 
                        rest: "rh.gfp.dirf.DialectRestful", 
                        name: "dialect"
                    }, 
                    {
                        maxLength: 60, 
                        allowBlank: false, 
                        fieldLabel: "nome", 
                        name: "nome", 
                        xtype: "textfield"
                    },
                    {
                        maxLength: 60, 
                        allowBlank: true, 
                        fieldLabel: "slug", 
                        name: "slug", 
                        xtype: "textfield"
                    },
                    {
                        xtype: 'choicefield',
                        fieldLabel: 'Identificador',
                        hiddenName: 'identifier',
                        choiceId: 'dirf.IDENTIFIERS_DIRF'
                    },                    
                    {
                        maxLength: 60, 
                        allowBlank: true, 
                        fieldLabel: "Info", 
                        name: "extra_info",
                        allowBlank: true,
                        xtype: "textfield"
                    }, 
                    {
                        xtype: 'choicefield',
                        fieldLabel: 'Tipo',
                        hiddenName: 'tipo',
                        choiceId: 'dirf.TYPE_DIRF_TOKENS'
                    },  
                ]
            });

        return this._tokenPanel;
    },

    constructor: function(cfg) {
        rh.gfp.dirf.TokenWindow.superclass.constructor.call(this, cfg);
        this._observe();
    }     
});

/** autogenerated **/
Ext._define('rh.gfp.dirf.DemonstrativoManage', {
    extend: 'toolkit.widget.TabPanel',

    getDemonstrativoGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.gfp.dirf.DemonstrativoGrid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Demonstrativo'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getDemonstrativoGrid(),
                ]
            }
        );

        rh.gfp.dirf.DemonstrativoManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.provisionplan.ProvisionEmployeeGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.gfp.provisionplan.ProvisionEmployeeWindow',
    rest: 'rh.gfp.provisionplan.ProvisionEmployeeRestful',

    configOrderToolBar: ['search', '->', 'download'], 

    getConfigActionsItems: function(cfg){
        var menu = rh.gfp.provisionplan.ProvisionGrid.superclass.getConfigActionsItems.call(this, cfg);

        menu['filterProvisionPlan'] = {
            xtype: 'rest-combofield',
            rest: 'rh.gfp.provisionplan.ProvisionPlanRestful',
            fieldLabel: "Plano",
            hiddenName: 'provision_plan',
            triggerAction: 'all',
            lazyRender: true,
            lazyInit: true,
            displayField: 'title',
            width: 200,
            allowBlank: true,
            blankText: '',
            listeners: {
                scope: this,
                select: function(combo, record) {
                    var store = this.getStore();
                    
                    if(record.get('pk') == 0) 
                        this.removeFilterProperty('provision_plan__pk', 1000);
                    else this.setFilterProperty("provision_plan__pk", record.get('pk'), 1000);
                    
                    store.load({});
                },
                // beforequery: function(qevent){

                // }
            }
        };
        return menu;
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Servidor', dataIndex: 'employee_unicode',  id: 'autoExpandColumn'},
                    {header: 'Plano de Provisão', dataIndex: 'provision_plan_unicode', width: 300},
                    {header: 'Info', dataIndex: 'info', width: 90},
                    {header: 'Início Aquisição', dataIndex: 'start_acquisition', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Fim Aquisição', dataIndex: 'end_acquisition', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Quantidade max.', dataIndex: 'quantity', width: 90}
                ]
            );

        return this._columnModel;
    }
});

core.RestfulGrid.register(
    'rh.gfp.provisionplan.ProvisionEmployeeRestful',
    'rh.gfp.provisionplan.ProvisionEmployeeGrid'
);


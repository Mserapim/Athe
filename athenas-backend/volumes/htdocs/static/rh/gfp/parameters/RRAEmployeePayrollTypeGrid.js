/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.parameters.RRAEmployeePayrollTypeGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.gfp.parameters.RRAEmployeePayrollTypeWindow',
    rest: 'rh.gfp.parameters.RRAEmployeePayrollTypeRestful',

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Servidor', dataIndex: 'servidor_unicode', id: 'autoExpandColumn'},
                    {header: 'Folha', dataIndex: 'folha_tipo_unicode', width: 120},
                    {header: 'Quantidade', dataIndex: 'quantidade', width: 90}
                ]
            );

        return this._columnModel;
    },

    'getFilterMenu': function() {
        return [
            {
                'text': 'Por Folha',
                'scope': this,
                'handler': this.filterFolha
            }
        ];
    },

    'filterFolha': function() {
        Ext._create('core.GridSelectWindow', {
            'rest': 'rh.gfp.payroll.PayrollTypeRestful',
            'title': 'Selecione um tipo de folha para filtrar',
            'width': Ext.getBody().getBox().width * 0.9,
            'height': Ext.getBody().getBox().height * 0.9,
            'callback': {
                'scope': this,
                'fn': function(instance) {
                    if(instance)
                        this.setFilterProperty("folha_tipo", instance.get('pk'), 1003);
                    else
                        this.removeFilterProperty('folha_tipo', 1003);
                }
            }
        }).show();
    },
});

core.RestfulGrid.register(
    'rh.gfp.parameters.RRAEmployeePayrollTypeRestful',
    'rh.gfp.parameters.RRAEmployeePayrollTypeGrid'
);


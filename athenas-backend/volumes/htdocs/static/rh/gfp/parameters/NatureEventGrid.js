/** autogenerated **/
/**
 *
 **/
Ext._define('rh.gfp.parameters.NatureEventGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.gfp.parameters.NatureEventWindow',

    getFilterMenu: function() {
        return [
            {
                'text': 'Apenas ativos',
                'checked': true,
                'hideOnClick': false,
                'scope': this,
                'handler': this.filterActive
            }
        ];
    },

    filterActive: function() {
        this._filterActive = core.nullValue(this._filterActive, true);

        if(this._filterActive == false) {
            this._filterActive = true;
            this.setFilterProperty('active', this._filterActive, 1011);
        }else {
            this.removeFilterProperty('active', 1011);
            this._filterActive = false;
        }
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Descricao', dataIndex: 'unicode', width: 300, hidden: true},
                    {header: 'Número', dataIndex: 'code', width: 80},
                    {header: 'Título', dataIndex: 'title', id: 'autoExpandColumn'},
                    {header: 'Ativo', dataIndex: 'active', width: 90, renderer: function(value) { return (value==true ? 'SIM' : 'NÃO'); }},
                ]
            );

        return this._columnModel;
    }
});

core.RestfulGrid.register(
    'rh.gfp.parameters.NatureEventRestful',
    'rh.gfp.parameters.NatureEventGrid'
);


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.anotacao.anotacaogeral.Window', {
    extend: 'rh.raw.employee.Window',
    rest: 'rh.anotacao.anotacaogeral.Restful',

    constructor: function(cfg) {
        rh.anotacao.anotacaogeral.Window.superclass.constructor.call(this, cfg);
    },

    _observe: function(){
        var params = core.nullValue(this.getAttachmentGrid().params, {});
        if(this.oId && this.oId != params['annotation']){
            this.getAttachmentGrid().setParam('annotation', this.oId);
            this.getAttachmentGrid().setParam('employee', this.values.servidor);
            this.getAttachmentGrid().setParam('document_type', 57);
            this.getAttachmentGrid().setFilterProperty('annotation', this.oId, 100);
        }

        if(this.oId)
            this.getPanelAttachment().enable();
        else
            this.getPanelAttachment().disable();
    },

    getPanelInformationItems: function(cfg_window){
        var items = rh.anotacao.anotacaogeral.Window.superclass.getPanelInformationItems.call(this, cfg_window);
        items.push({
            maxLength: 20,
            allowBlank: true,
            fieldLabel: "Número Documento",
            name: "numero_documento",
            xtype: "textfield"
        });
        items.push({
            maxLength: 50,
            allowBlank: true,
            fieldLabel: "Número do Processo",
            name: "numero_processo",
            xtype: "textfield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Data Documento",
            name: "data_documento",
            xtype: "datefield"
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Data Portaria Início",
            name: "data_portaria_inicio",
            xtype: "datefield"
        });
        items.push({
            xtype: "rest-autocompletefield",
            fieldLabel: "Publicação",
            allowBlank: true,
            rest: "rh.publicacao.Restful",
            name: "publicacao"
        });
        items.push({
            maxLength: 150,
            allowBlank: true,
            fieldLabel: "Resumo",
            name: "resumo",
            xtype: "textfield"
        });
        items.push({
            xtype: 'choicefield',
            fieldLabel: 'Tipo Documento *',
            hiddenName: 'tipo_documento',
            choiceId: 'rh.TIPO_DOCUMENTO'
        });
        items.push({
            xtype: "checkbox",
            checked: true,
            boxLabel: "Ativa",
            allowBlank: true,
            hideLabel: true,
            name: "ativa"
        });
        return items;
    },

    getPanelTextItems: function(cfg_window) {
        return [
            {
                hideLabel: true,
                name: "texto",
                xtype: "ckeditor"
            }
        ];
    },

    getPanelAttachment: function(cfg_window, cfg) {
        if(!this._attachmentPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Anexos',
                    frame: true,
                    border: false,
                    layout: 'form',
                    width: 510,
                    items: [
                        this.getAttachmentGrid(cfg_window, {})
                    ]
                }
            );
            this._attachmentPanel = Ext._create('Ext.Panel', cfg);
        }
        return this._attachmentPanel;
    },

    getAttachmentGrid: function(cfg_window, cfg) {
        if(!this._attachmentGrid) {
            this._attachmentGrid = Ext._create('rh.digitaldocument.attachment.Grid', {
                title: 'Anexos',
                gridAutoLoad: false,
                height: 300,
                width: 510,
                hideColumns: [
                    'my_origin_cache_unicode',
                    'date_start',
                    'date_end',
                ]
            });
        }
        return this._attachmentGrid;
    },

    getTabPanelItems: function(cfg_window){
        return [
            this.getPanelInformation(cfg_window, {}),
            this.getPanelAttachment(cfg_window, {}),
            this.getPanelText(cfg_window, {})
        ];
    },
});


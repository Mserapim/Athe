/** autogenerated **/
/**
 *
 **/
Ext._define('estagio.avaliado.EstagioAvaliacaoGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'estagio.avaliado.EstagioAvaliacaoWindow',

    hideItemsToolbar: ['add','edit', 'remove', 'download'],

    configOrderToolBar: ['actionManifestar', 'actionAlterar', 'actionCiencia', 'openVisualizacao', 'openRelatorio', 'search', '-', '->'],

    getActionManifestarAction: function(cfg) {
        if(!this._actionAvaliar)
            this._actionAvaliar = Ext._create('Ext.Button', {
                text: 'Fazer Manifestação',
                iconCls: 'icon-estagio icon-efetivado',
                scope: this,
                handler: this.getManifestacaoWindow
            });
    
        return this._actionAvaliar;
    },

    getActionAlterarAction: function(cfg) {
        if(!this._actionAlterar)
            this._actionAlterar = Ext._create('Ext.Button', {
                text: 'Alterar Manifestação',
                scope: this,
                iconCls: 'icon-estagio icon-authorization',
                handler: this.getAlteracaoManifestacaoWindow
            });
    
        return this._actionAlterar;
    },

    getActionCienciaAction: function(cfg) {
        if(!this._actionCiencia)
            this._actionCiencia = Ext._create('Ext.Button', {
                text: 'Ciência da Decisão',
                scope: this,
                icon: '/' + global.Context + '/static/rh/images/autorizado.png',
                handler: this.cienciaDecisao
            });
    
        return this._actionCiencia;
    },

    getManifestacaoWindow: function(){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            var rest = this.factoryRestful();
            var mask = new Ext.LoadMask(this.getEl(), {msg: 'Carregando dados...'});
            mask.show();
            rest.doRequest(
                rest.getRoute('get_list_questionario', [selected.data.questionario_manifestacao_id,selected.data.avaliado], 'POST', {
                    params:{periodo: selected.data.periodo_avaliado},
                    scope: this,
                    callback: function() {
                        mask.hide();
                        mask = undefined;
                    },
                    success: function(xhr) {
                        var rst = Ext.decode(xhr.responseText);
                        if(rst.success) {
                            var montaQuestionario = new toolkit.questionario.MontaQuestionario({
                                'title': selected.data.questionario_manifestacao,
                                'action': 'create',
                                'values':rst.collection,
                                'callback': {
                                    'success': {
                                        'scope': this,
                                        'handler': function() { 
                                            Ext.Ajax.request({
                                                url: toolkit.util.Normalize.controller_action('GepEstagioAvaliacao','save_manifestacao_estagio'),
                                                scope:this,
                                                params:{
                                                    pk_gestor_estagio: selected.data.avaliado,
                                                    pk_questionario_resposta:montaQuestionario.retorno,
                                                    pk_avaliacao_estagio: selected.data.pk,
                                                },
                                                'success': function(request) {
                                                    var obj = Ext.decode(request.responseText);
                                                    Ext.Msg.show({
                                                        'title': 'Estágio Probatório',
                                                        'icon': Ext.Msg.INFO,
                                                        'buttons': Ext.Msg.OK,
                                                        'msg': obj.message
                                                    });
                                                    this.getStore().reload();
                                                },
                                                failure: function(response, opts) {
                                                    console.log(opts.result.message)
                                                    Ext.Msg.show({
                                                        'title': 'Atenção!',
                                                        'msg': 'Erro ao salvar os dados',
                                                        'icon': Ext.Msg.WARNING,
                                                        'buttons': Ext.Msg.OK
                                                    });
                                                }
                                            });
                                            this.getStore().reload();
                                        }
                                    }
                                }
                            }, rst.collection, selected.data.questionario_manifestacao);

                            montaQuestionario.show();

                            this.getStore().reload();
                        }
                        else
                            Ext.Msg.show({
                                title: 'Atenção',
                                icon: Ext.Msg.ERROR,
                                buttons: Ext.Msg.OK,
                                msg: rst.message
                            });
                    },
                    failure: function(xhr) {
                        console.log('>>> ERRO');
                        Ext.Msg.show({
                            title: 'Atenção',
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK,
                            msg: 'Recurso indisponível no momento.'
                        });
                    },
                })
            );

        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um servidor'
            });
        }

    },

    getAlteracaoManifestacaoWindow: function(){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            var rest = this.factoryRestful();
            var mask = new Ext.LoadMask(this.getEl(), {msg: 'Carregando dados...'});
            mask.show();
            rest.doRequest(
                rest.getRoute('get_questionario_alteracao', [selected.data.questionario_manifestacao_id,selected.data.avaliado], 'POST', {
                    params: {tipo:2, periodo: selected.data.periodo_avaliado},
                    scope: this,
                    callback: function() {
                        mask.hide();
                        mask = undefined;
                    },
                    success: function(xhr) {
                        var rst = Ext.decode(xhr.responseText);
                        if(rst.success) {
                            var montaQuestionarioAlteracao = new toolkit.gep.MontaQuestionarioAlteracao({
                                'title': selected.data.questionario_manifestacao,
                                'action': 'update',
                                'values':rst.collection,
                                'callback': {
                                    'success': {
                                        'scope': this,
                                        'handler': function() { 
                                            Ext.Ajax.request({
                                                url: toolkit.util.Normalize.controller_action('GepEstagioAvaliacao','save_alteracao_manifestacao_estagio'),
                                                scope:this,
                                                params:{
                                                    pk_gestor_estagio: selected.data.avaliado,
                                                    pk_questionario_resposta:montaQuestionarioAlteracao.retorno,
                                                    pk_avaliacao_estagio: selected.data.pk,
                                                },
                                                'success': function(request) {
                                                    var obj = Ext.decode(request.responseText);
                                                    Ext.Msg.show({
                                                        'title': 'Estágio Probatório',
                                                        'icon': Ext.Msg.INFO,
                                                        'buttons': Ext.Msg.OK,
                                                        'msg': obj.message
                                                    });
                                                    this.getStore().reload();
                                                },
                                                failure: function(response, opts) {
                                                    console.log(opts.result.message)
                                                    Ext.Msg.show({
                                                        'title': 'Atenção!',
                                                        'msg': 'Erro ao salvar os dados',
                                                        'icon': Ext.Msg.WARNING,
                                                        'buttons': Ext.Msg.OK
                                                    });
                                                }
                                            });
                                            this.getStore().reload();
                                        }
                                    }
                                }
                            }, rst.collection, selected.data.questionario_manifestacao);

                            montaQuestionarioAlteracao.show();

                            this.getStore().reload();
                        }
                        else
                            Ext.Msg.show({
                                title: 'Atenção',
                                icon: Ext.Msg.ERROR,
                                buttons: Ext.Msg.OK,
                                msg: rst.message
                            });
                    },
                    failure: function(xhr) {
                        console.log('>>> ERRO');
                        Ext.Msg.show({
                            title: 'Atenção',
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK,
                            msg: 'Recurso indisponível no momento.'
                        });
                    },
                })
            );

        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um servidor'
            });
        }

    },

    getOpenVisualizacaoAction: function(){
        if(!this.openVisualizacao)
            this.openVisualizacao = Ext._create('Ext.Button', {
                text: 'Visualizar',
                iconCls: 'icon-estagio icon-view',
                scope: this,
                menu: [
                    {
                        text: 'Avaliações',
                        scope: this,
                        group: 'tipo',
                        filter: 'todos',
                        iconCls: 'icon-estagio icon-view',
                        handler: this.getAvaliacoes
                    },
                    {
                        text: 'Manifestações',
                        scope: this,
                        iconCls: 'icon-estagio icon-view',
                        handler: this.getManifestacao
                    },
                ]
            });
    
        return this.openVisualizacao;
    },

    getOpenRelatorioAction: function(){
        if(!this._openRelatorio)
            this._openRelatorio = Ext._create('Ext.Button', {
                text: 'Relatório',
                iconCls: 'icon-estagio icon-archive-pdf',
                scope: this,
                menu: [
                    {
                        text: 'Relatório Notas de Avaliação',
                        scope: this,
                        group: 'tipo',
                        filter: 'todos',
                        iconCls: 'icon-estagio icon-archive-pdf',
                        handler: function() { this.getReportSimple(1); }
                    },
                    {
                        text: 'Relatório de Avaliação',
                        scope: this,
                        group: 'tipo',
                        filter: 'bloquear',
                        iconCls: 'icon-estagio icon-archive-pdf',
                        handler: this.getReport
                    },
                    {
                        text: 'Relatório Decisão Estágio',
                        scope: this,
                        iconCls: 'icon-estagio icon-archive-pdf',
                        handler: function() { this.getReportSimple(2); }
                    },
                ]
            });
    
        return this._openRelatorio;
    },

    getAvaliacoes: function(){
        var selected = this.getSelectionModel().getSelected();
        if(selected) {
            var url = toolkit.util.Normalize.controller_action('GepEstagioProbatorioServidor','get_resposta_avaliacao',[selected.data.questionario_id,selected.data.avaliado]);
            new toolkit.questionario.VerResposta({
                'title': 'Avaliações de Estágio Probatório Servidor: ' + selected.data.posse_servidor_unicode,
                'callback': {
                    'success': {
                        'scope': this,
                        'handler': function() {
                            this.getStore().reload()
                        }
                    }
                }
            }, selected.data.avaliado, url ).show();
        }
        else {
            Ext.Msg.show({
                'title': 'Atenção',
                'msg': 'Selecione',
                'icon': Ext.Msg.WARNING,
                'buttons': Ext.Msg.OK
            });
        }

    },

    getManifestacao: function(){
        var selected = this.getSelectionModel().getSelected();
        if(selected) {
            var url = toolkit.util.Normalize.controller_action('GepEstagioProbatorioServidor','get_resposta_avaliacao',[selected.data.questionario_manifestacao_id,selected.data.avaliado]);
            
            new toolkit.questionario.VerResposta({
                'title': 'Manifestação de Estágio Probatório: ' + selected.data.posse_servidor_unicode,
                'callback': {
                    'success': {
                        'scope': this,
                        'handler': function() {
                            this.getStore().reload()
                        }
                    }
                }
            }, selected.data.avaliado, url ).show();
        }
        else {
            Ext.Msg.show({
                'title': 'Atenção',
                'msg': 'Selecione',
                'icon': Ext.Msg.WARNING,
                'buttons': Ext.Msg.OK
            });
        }
    }, 

    cienciaDecisao: function(){
        var selected = this.getSelectionModel().getSelected();
        var rest = this.factoryRestful();
        var mask = new Ext.LoadMask(this.getEl(), {msg: 'Persistindo dados...'});

        mask.show();
        rest.doRequest(
            rest.getRoute('ciencia_decisao_estagio', false, 'POST', {
                scope: this,
                params: {
                    pk: selected.data.avaliado
                },
                callback: function() {
                    mask.hide();
                    mask = undefined;
                },
                success: function(xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if(rst.success) {
                        this.getStore().reload();
                    }
                    else
                        Ext.Msg.show({
                            title: 'Atenção',
                            icon: Ext.Msg.ERROR,
                            buttons: Ext.Msg.OK,
                            msg: rst.message
                        });
                },
                failure: function(xhr) {
                    Ext.Msg.show({
                        title: 'Atenção',
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: 'Recurso indisponível no momento.'
                    });
                },
            })
        );
    },

    getReport: function(){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            var wnd = Ext._create('estagio.gestor.RelatorioWindow', {
                modal: true,
                params: {
                    'servidor': selected.data.servidor_id,
                    'cargo': selected.data.cargo_id,
                    'questionario_avaliacao': selected.data.questionario_id,
                    'questionario_manifestacao': selected.data.questionario_manifestacao_id,
                },
            });
            wnd.show()
        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um servidor'
            });
        }
    },

    getReportSimple: function(param){
        var selected = this.getSelectionModel().getSelected();
        if (selected){
            if (param ==1)
                // new toolkit.widget.ExtReportBuild('GepPrintAvaliacao', '/to/mpe/rh/estagio_probatorio/notas/rh_ep_main').runReport(
                //     '', {servidor: selected.data.servidor_id, cargo: selected.data.cargo_id}
                // );
                engine.mq.Report.request({
                    report: '/to/mpe/rh/estagio_probatorio/notas/notas',
                    el: this.getEl(),
                    waitMessage: 'Gerando os documentos...',
                    params: {
                        outfile: 'relatorio-' + selected.data.posse_servidor_unicode,
                        report_name: 'Relatorio Estágio Probatório',
                        servidor: selected.data.servidor_id, 
                        cargo: selected.data.cargo_id
                    }
                });

            else if (param ==2)
                // new toolkit.widget.ExtReportBuild('GepPrintDecisaoEstagio', '/to/mpe/rh/estagio_probatorio/especial/avaliacao/rh_ep_especial_avaliacao_main').runReport(
                //     '', {servidor: selected.data.servidor_id, cargo: selected.data.cargo_id}
                // );
                engine.mq.Report.request({
                    report: '/to/mpe/rh/estagio_probatorio/especial/avaliacao/avaliacao',
                    el: this.getEl(),
                    waitMessage: 'Gerando os documentos...',
                    params: {
                        outfile: 'relatorio-' + selected.data.posse_servidor_unicode,
                        report_name: 'Relatorio Estágio Probatório',
                        servidor: selected.data.servidor_id, 
                        cargo: selected.data.cargo_id
                    }
                });


        } else {
            Ext.Msg.show({
                title: 'Atenção',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Primeiro selecione um servidor'
            });
        }

    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Status', dataIndex: 'status_display', width: 120, renderer: core.rendererIconGrid},
                    {header: 'Avaliador', dataIndex: 'avaliador_unicode', id: 'autoExpandColumn'},
                    {header: 'Período Avaliado', dataIndex: 'periodo_avaliado', width: 150},
                    {header: 'Data da Avaliação', dataIndex: 'criado_em', width: 150, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    {header: 'Data Início da Etapa', dataIndex: 'data_inicio_etapa', width: 150, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Data Fim da Etapa', dataIndex: 'data_fim_etapa', width: 150, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    // {header: '', dataIndex: 'status_display', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }},
                    // {header: 'avaliado', dataIndex: 'avaliado_unicode', width: 120},
                    // {header: 'lotacao avaliado', dataIndex: 'lotacao_avaliado_unicode', width: 120},
                    // {header: 'cargo avaliador', dataIndex: 'cargo_avaliador_unicode', width: 120},
                    // {header: 'dias interrompidos', dataIndex: 'dias_interrompidos', width: 90},
                    // {header: 'finalizado em', dataIndex: 'finalizado_em', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')},
                    // {header: 'lotacao avaliador', dataIndex: 'lotacao_avaliador_unicode', width: 120},
                    // {header: 'cargo avaliado', dataIndex: 'cargo_avaliado_unicode', width: 120},
                    // {header: 'finalizado por', dataIndex: 'finalizado_por_unicode', width: 120}
                ]
            );

        return this._columnModel;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.apply(
            cfg,
            {
                columnAction: false,
                allowCreate: false,
                allowUpdate: false,
                allowRemove: false,
            }
        );

        estagio.avaliado.EstagioAvaliacaoGrid.superclass.constructor.call(this, cfg);
    }
});

core.RestfulGrid.register(
    'estagio.avaliado.EstagioAvaliacaoRestful',
    'estagio.avaliado.EstagioAvaliacaoGrid'
);


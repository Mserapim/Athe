/** autogenerated **/
/**
 *
 **/
Ext._define('rh.person.repeatPersonIncident.Grid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.person.repeatPersonIncident.Window',

    configOrderToolBar: ['merge', 'markFalse', '-', 'search', '->', 'download'],

    _markFalseSelected: function (pk) {
        var rest = this.factoryRestful();
        var mask = new Ext.LoadMask(this.getEl(), { msg: 'Processando sua requisição...' });

        mask.show();
        rest.markFalse(
            pk,
            {
                scope: this,
                fn: function() {
                    this.getStore().reload();
                }
            },
            {
                fn: function(message) {
                    Ext.Msg.show({
                        title: 'Falso positivo',
                        msg: message,
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK
                    });
                }
            },
            {
                fn: function() { mask.hide(); }
            }
        );
    },

    markFalseSelected: function () {
        var selection = this.getSelectionModel().getSelections();

        if (selection.length === 1) {
            Ext.Msg.show({
                title: 'Falso positivo',
                msg: 'Tem certeza que deseja marcar como falso positivo o item selecionado?',
                icon: Ext.Msg.QUESTION,
                buttons: Ext.Msg.YESNO,
                scope: this,
                fn: function (btn) {
                    if (btn === 'no') return;
                    this._markFalseSelected(selection[0].get('pk'));
                }
            });
        } else if (selection.length > 1) {
            Ext.Msg.show({
                title: 'Falso positivo',
                msg: 'Selecione apenas um item por vez, para prosseguir com a resolução.',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK
            });
        } else {
            Ext.Msg.show({
                title: 'Falso positivo',
                msg: 'Selecione um item, para prosseguir com a resolução.',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK
            });
        }
    },

    getMarkFalseAction: function (cfg) {
        if (!this._markFalseAction) {
            this._markFalseAction = Ext._create('Ext.Button', {
                text: 'MarkFalse',
                scope: this,
                handler: function () { this.markFalseSelected() }
            });
        }

        return this._markFalseAction;
    },

    _mergeSelected: function (pk) {
        var rest = this.factoryRestful();
        var mask = new Ext.LoadMask(this.getEl(), { msg: 'Processando sua requisição...' });

        mask.show();
        rest.merge(
            pk,
            {
                scope: this,
                handler: function () {
                    this.getStore().reload();
                }
            },
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: 'Falso positivo',
                        msg: message,
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK
                    });
                }
            },
            {
                fn: function () { mask.hide(); }
            }
        );
    },

    mergeSelected: function () {
        var selection = this.getSelectionModel().getSelections();

        if (selection.length === 1) {
            Ext.Msg.show({
                title: 'Mesclando',
                msg: 'Tem certeza que deseja mesclar o item selecionado?',
                icon: Ext.Msg.QUESTION,
                buttons: Ext.Msg.YESNO,
                scope: this,
                fn: function (btn) {
                    if (btn === 'no') return;
                    this._mergeSelected(selection[0].get('pk'));
                }
            });
        } else if (selection.length > 1) {
            Ext.Msg.show({
                title: 'Mesclando',
                msg: 'Selecione apenas um item por vez, para prosseguir com a mesclagem.',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK
            });
        } else {
            Ext.Msg.show({
                title: 'Mesclando',
                msg: 'Selecione um item, para prosseguir com a mesclagem.',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK
            });
        }
    },

    getMergeAction: function (cfg) {
        if (!this._mergeAction) {
            this._mergeAction = Ext._create('Ext.Button', {
                text: 'Merge',
                scope: this,
                handler: function () { this.mergeSelected() }
            });
        }

        return this._mergeAction;
    },

    rendererPercent: function(value) {
        return [
            '<div style="text-align: right">',
                (Number.parseFloat(value) * 100.0).toFixed(1) + '%',
            '</div>'
        ].join('');
    },

    defaultClickFunction: function () {
        var selected = this.getSelectionModel().getSelected();

        if (selected) {
            var className = rh.person.Grid.getClassByName(selected.get('target_person_kind'));
            Ext._create(className, {
                action: 'update',
                oId: selected.get('target_person'),
                values: 'remote',
                params: this.getParams(),
                callback: {
                    success: {
                        scope: this,
                        fn: function () {
                            this.getStore().reload();
                        }
                    }
                }
            }).show();
        };
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Nome', dataIndex: 'target_person_unicode', id: 'autoExpandColumn'},
                    {header: 'Principal', dataIndex: 'main_person_unicode', width: 120, hidden: true},
                    {header: 'Estado', dataIndex: 'current_state_display', width: 120},
                    {header: 'Qualidade', dataIndex: 'target_person_rate_fill', width: 70, renderer: this.rendererPercent},
                    {header: 'Coincidencia', dataIndex: 'ratio', width: 70, renderer: this.rendererPercent},
                    {header: 'Resolvido por', dataIndex: 'asigned_by_unicode', width: 120},
                    {header: 'Resolvido em', dataIndex: 'asigned_at', width: 110, renderer: Ext.util.Format.dateRenderer('d/m/Y H:i')}
                ]
            );

        return this._columnModel;
    }
});

core.RestfulGrid.register(
    'rh.person.repeatPersonIncident.Restful',
    'rh.person.repeatPersonIncident.Grid'
);


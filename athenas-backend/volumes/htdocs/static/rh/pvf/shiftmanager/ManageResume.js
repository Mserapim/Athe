/** autogenerated **/
Ext._define('rh.pvf.shiftmanager.ManageResume', {
    extend: 'toolkit.widget.TabPanel',

    Grid: function(cfg) {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.shiftmanager.GridResume', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Controle de plantões'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.Grid(cfg),
                ]
            }
        );

        rh.pvf.shiftmanager.ManageResume.superclass.constructor.call(this, cfg);
    }
});


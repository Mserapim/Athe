/** autogenerated **/
Ext._define('rh.pvf.absence.training.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getTrainingAbsenceGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.absence.training.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Licença para Curso/Aperfeiçoamento'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getTrainingAbsenceGrid(),
                ]
            }
        );

        rh.pvf.absence.training.Manage.superclass.constructor.call(this, cfg);
    }
});


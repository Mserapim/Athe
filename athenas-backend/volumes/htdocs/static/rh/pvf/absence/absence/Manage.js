/** autogenerated **/
Ext._define('rh.pvf.absence.absence.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getAbsenceGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.pvf.absence.absence.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Absence'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getAbsenceGrid(),
                ]
            }
        );

        rh.pvf.absence.absence.Manage.superclass.constructor.call(this, cfg);
    }
});


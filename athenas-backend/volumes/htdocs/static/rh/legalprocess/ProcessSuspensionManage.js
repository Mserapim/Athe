/** autogenerated **/
Ext._define('rh.legalprocess.ProcessSuspensionManage', {
    extend: 'toolkit.widget.TabPanel',

    getProcessSuspensionGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.legalprocess.ProcessSuspensionGrid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'ProcessSuspension'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getProcessSuspensionGrid(),
                ]
            }
        );

        rh.legalprocess.ProcessSuspensionManage.superclass.constructor.call(this, cfg);
    }
});


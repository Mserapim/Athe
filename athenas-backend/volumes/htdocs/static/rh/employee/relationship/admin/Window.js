/** autogenerated **/
/**
 *
 **/
Ext._define('rh.employee.relationship.Admin.Window', {
    'extend': 'rh.raw.Window',

    'rest': 'rh.employee.relationship.Admin.Restful',

    width: 535,

    getTabPanel: function(cfg_window, cfg) {
        if(!this._tabPanel) {
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    height: 205,
                }
            );
            this._tabPanel = rh.employee.relationship.Admin.Window.superclass.getTabPanel.call(this, cfg_window, cfg);
        }
        return this._tabPanel;
    },

    getTabPanelItems: function(cfg_window) {
        return [
            this.getPanelInformation(cfg_window, {}),
        ];
    },

    getPanelInformationItems: function(cfg_window) {
        return rh.employee.relationship.Admin.Window.superclass.getPanelInformationItems.call(this, cfg_window).concat([
            this.getGiverField(),
            {
                xtype: "rest-autocompletefield",
                fieldLabel: "Para",
                allowBlank: true,
                rest: "rh.employee.Restful",
                name: "receiver",
                preFilter: [
                    {property: 'ativo', value: true, stage: 1}
                ]
            },
            this.getWorkplaceResponsibleField(),
            {
                allowBlank: false,
                fieldLabel: "Início",
                name: "date_start",
                xtype: "datefield"
            },
            {
                allowBlank: true,
                fieldLabel: "Fim",
                name: "date_end",
                xtype: "datefield"
            },
            this.getAppChoiceField(),
        ]);
    },

    getGiverField: function() {
        if (!this._giverField) {
            this._giverField = Ext._create('core.fields.AutocompleteField', {
                fieldLabel: "De",
                name: "giver",
                allowBlank: false,
                rest: 'rh.employee.Restful',
                width: 410,
                preFilter: [
                    {property: 'responsavel_por', value: null, stage: -1}
                ]
            });
        }

      return this._giverField;
    },

    getWorkplaceResponsibleField: function() {
        if (!this._workplaceResponsibleField) {
            this._workplaceResponsibleField = Ext._create('core.fields.AutocompleteField', {
                fieldLabel: "Lotação",
                name: "workplace",
                allowBlank: true,
                rest: 'rh.workplace.Restful',
                width: 410,
                preFilter: [
                    {property: 'ativo', value: false, stage: -1}
                ]
            });
        }

        return this._workplaceResponsibleField;
    },

    getAppChoiceField: function(cfg) {
        if (!this._appChoiceField) {
            this._appChoiceField = Ext._create('standard.fields.ChoiceField', {
                fieldLabel: 'Aplicativo',
                hiddenName: 'app',
                choiceId: 'rh.RELATIONSHIP_APP',
            });
        }
        return this._appChoiceField;
    },

});

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.employee.relationship.Window', {
    'extend': 'rh.raw.Window',

    'rest': 'rh.employee.relationship.Restful',

    width: 535,

    getTabPanel: function(cfg_window, cfg) {
        if(!this._tabPanel) {
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    height: 185,
                }
            );
            this._tabPanel = rh.employee.relationship.Window.superclass.getTabPanel.call(this, cfg_window, cfg);
        }
        return this._tabPanel;
    },

    getTabPanelItems: function(cfg_window) {
        return [
            this.getPanelInformation(cfg_window, {}),
        ];
    },

    getPanelInformationItems: function(cfg_window) {
        return rh.employee.relationship.Window.superclass.getPanelInformationItems.call(this, cfg_window).concat([
            this.getReceiverField(),
            this.getWorkplaceResponsibleField(),
            {
                allowBlank: false,
                fieldLabel: "Início",
                name: "date_start",
                xtype: "datefield"
            },
            {
                allowBlank: true,
                fieldLabel: "Fim",
                name: "date_end",
                xtype: "datefield"
            },
            this.getAppChoiceField(),
        ]);
    },

    getReceiverField: function() {
        if (!this._receiverField) {
          this._receiverField = Ext._create('core.fields.AutocompleteField', {
              fieldLabel: "Para",
              name: "receiver",
              allowBlank: true,
              rest: 'rh.employee.Restful',
              width: 410,
              preFilter: [
                  {property: 'ativo', value: true, stage: 1}
              ]
          });

        }

      return this._receiverField;
    },

    getWorkplaceResponsibleField: function() {
        if (!this._workplaceResponsibleField) {
            this._workplaceResponsibleField = Ext._create('core.fields.ComboField', {
                fieldLabel: "Lotação",
                hiddenName: "workplace",
                allowBlank: true,
                displayField: 'description',
                store: Ext._create('Ext.data.Store', {
                    proxy: Ext._create('Ext.data.HttpProxy', {
                        url: core.callAction('RHRelationship', 'workplacesResponsible')
                    }),
                    reader: Ext._create('Ext.data.JsonReader', {
                        totalProperty: 'count',
                        root: 'collection',
                        fields: [
                            {name: 'pk', type: 'int'},
                            {name: 'description', type: 'string'},
                        ]
                    })
                }),
                autoLoad: true
            });
        }

      return this._workplaceResponsibleField;
    },

    getAppChoiceField: function(cfg) {
        if (!this._appChoiceField) {
            this._appChoiceField = Ext._create('standard.fields.ChoiceField', {
                fieldLabel: 'Aplicativo',
                hiddenName: 'app',
                choiceId: 'rh.RELATIONSHIP_APP',
            });
        }

        return this._appChoiceField;
    }
});

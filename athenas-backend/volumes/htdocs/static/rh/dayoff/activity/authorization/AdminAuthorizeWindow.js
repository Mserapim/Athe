/** autogenerated **/
/**
 *
 **/
Ext._define('rh.dayoff.activity.authorization.AdminAuthorizeWindow', {
    extend: 'core.RestfulWindow',

    rest: 'rh.dayoff.activity.Restful',
    resource: 'DAYOFFAdminAuthorization',

    width: 640,

    getMediateBossField: function () {
        if (!this._mediateField) {
            this._mediateField = Ext._create('core.fields.AutocompleteField', {
                fieldLabel: "Autorizado por (chefe mediato)",
                allowBlank: true,
                rest: "rh.employee.Restful",
                name: "mediate_authorization_by",
                preFilter: [
                    { property: 'ativo__in', value: [true], stage: 1001 },
                    { property: 'tipo__in', value: ['M', 'S'], stage: 1002 }
                ]
            });

        }

        return this._mediateField;
    },

    getImmediateBossField: function () {
        if (!this._immediateField) {
            this._immediateField = Ext._create('core.fields.AutocompleteField', {
                fieldLabel: "Autorizado por (chefe imediato)",
                allowBlank: true,
                rest: "rh.employee.Restful",
                name: "immediate_authorization_by",
                preFilter: [
                    { property: 'ativo__in', value: [true], stage: 1001 },
                    { property: 'tipo__in', value: ['M', 'S'], stage: 1002 }
                ]
            });

        }

        return this._immediateField;
    },

    getAttachmentField: function () {
        if (!this._attachmentField)
            this._attachmentField = Ext._create('Ext.Panel', {
                border: false,
                frame: false,
                layout: 'form',
                items: [
                    this.getAttachmentButton(),
                    {
                        name: 'attachment',
                        fieldLabel: 'Anexo',
                        xtype: 'textfield',
                        allowBlank: true,
                        hidden: true
                    },
                ]
            });

        return this._attachmentField;
    },

    getAttachmentButton: function () {
        if (!this._attachmentButton) {
            this._attachmentButton = Ext._create('Ext.Button', {
                text: 'Visualizar Anexo',
                fieldLabel: 'Anexo',
                anchor: '50%',
                scope: this,
                handler: this.getAttachmentWindow,
            });
        }

        return this._attachmentButton;
    },

    getAttachmentWindow: function () {
        attachment_value = this.getFormPanel().getForm().findField('attachment').getValue()
        if (attachment_value === undefined || attachment_value === "") {
            action = 'create'
            oId_value = null
        }
        else {
            action = 'update';
            oId_value = attachment_value;
        }
        Ext._create('rh.dayoff.attachment.Window', {
            title: 'Anexo',
            oId: oId_value,
            action: action,
            values: 'remote',
            callback: {
                success: {
                    scope: this,
                    fn: function (instance) {
                        this.getFormPanel().getForm().findField('attachment').setValue(instance.pk)
                    }
                }
            }
        }).show();

    },
    getFormPanel: function (cfg) {
        if (!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    this.getImmediateBossField(),
                    this.getMediateBossField(),
                    this.getAttachmentField(),
                ]
            });

        return this._formPanel;
    },

    _process: function (params) {
        var rest = Ext._create('rh.dayoff.activity.authorization.AdminAuthorizeRestful', {});
        var mask = Ext._create('Ext.LoadMask', this.getEl(), { msg: 'Processando informações.' });
        var wnd = this;

        mask.show();
        rest._process(
            params,
            {
                scope: this,
                fn: function (rst) {
                    core.invokeCallback((wnd.externalCallback || { fn: Ext.emptyFn }), rst.message);
                    wnd.close();
                }
            },
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: 'Informando',
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: message
                    });
                }
            },
            {
                fn: function () {
                    mask.hide();
                }
            }
        );
    },

    save: function () {
        var params = this.getParams();
        var form = this.getFormPanel().getForm();

        params.mediate_employee_pk = form.getValues().mediate_authorization_by;
        params.immediate_employee_pk = form.getValues().immediate_authorization_by;
        params.attachment = form.getValues().attachment;
        this._process(params);
    }

});

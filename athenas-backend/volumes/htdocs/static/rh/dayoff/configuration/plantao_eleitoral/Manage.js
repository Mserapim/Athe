/** autogenerated **/
Ext._define('rh.dayoff.configuration.plantao_eleitoral.Manage', {
    extend: 'toolkit.widget.TabPanel',

    Grid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.dayoff.configuration.plantao_eleitoral.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Configuração de Plantão Eleitoral'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.Grid(),
                ]
            }
        );

        rh.dayoff.configuration.plantao_eleitoral.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('rh.dayoff.mpmt.activity.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getActivityGrid: function () {
        if (!this._grid)
            this._grid = Ext._create('rh.dayoff.mpmt.activity.Grid', { region: 'center' });
        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(
            cfg,
            {
                title: 'Activity'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getActivityGrid(),
                ]
            }
        );

        rh.dayoff.mpmt.activity.Manage.superclass.constructor.call(this, cfg);
    }
});


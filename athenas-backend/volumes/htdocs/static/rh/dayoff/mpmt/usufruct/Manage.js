/** autogenerated **/
Ext._define('rh.dayoff.mpmt.usufruct.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getUsufructGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.mpmt.usufruct.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Usufrutos'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getUsufructGrid(),
                ]
            }
        );

        rh.dayoff.mpmt.usufruct.Manage.superclass.constructor.call(this, cfg);
    }
});


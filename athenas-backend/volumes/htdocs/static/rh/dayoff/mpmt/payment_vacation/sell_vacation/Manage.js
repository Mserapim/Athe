/** autogenerated **/
Ext._define('rh.dayoff.mpmt.payment_vacation.sell_vacation.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getVacationGrid: function (cfg) {
        if (!this._grid) {
            this._grid = Ext._create('rh.dayoff.mpmt.payment_vacation.sell_vacation.Grid', {
                region: 'center',
                gridAutoLoad: true,
                vacation_deadline: cfg.vacation_deadline
                
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(
            cfg,
            {
                title: 'Conferência de Vendas de Férias'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getVacationGrid(cfg),
                ]
            }
        );

        rh.dayoff.mpmt.payment_vacation.sell_vacation.Manage.superclass.constructor.call(this, cfg);
    }
});


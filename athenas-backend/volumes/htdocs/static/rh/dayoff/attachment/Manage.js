/** autogenerated **/
Ext._define('rh.dayoff.attachment.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getAttachmentGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.dayoff.attachment.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Attachment'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getAttachmentGrid(),
                ]
            }
        );

        rh.dayoff.attachment.Manage.superclass.constructor.call(this, cfg);
    }
});


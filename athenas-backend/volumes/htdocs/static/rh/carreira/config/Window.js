/** autogenerated **/
/**
 *
 **/
Ext._define('rh.carreira.config.Window', {
    extend: 'rh.raw.Window',
    rest: 'rh.carreira.config.Restful',

    getTabPanel: function(cfg_window, cfg) {
        if(!this._tabPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    height: 350,
                }
            );
            this._tabPanel = rh.carreira.config.Window.superclass.getTabPanel.call(this, cfg_window, cfg);
        }
        return this._tabPanel;
    },

    getTabPanelItems: function(cfg_window){
        return [
            this.getPanelInformation(cfg_window, {}),
            // this.getPanelInformationDataPanel(cfg_window, {}),
            this.getPanelText(cfg_window, {})
        ];
    },

    getPanelInformationItems: function(cfg_window){
        return [
            {
                maxLength: 100,
                allowBlank: false,
                fieldLabel: 'Nome',
                name: 'name',
                xtype: 'textfield',
            },
            {
                maxLength: 12,
                allowBlank: false,
                fieldLabel: 'Código',
                name: 'code',
                xtype: 'textfield',
            },
            {
                allowBlank: false,
                fieldLabel: 'In\u00edcio vig\u00eancia',
                name: 'start_validity',
                xtype: 'datefield'
            },
            {
                allowBlank: true,
                fieldLabel: 'Fim vig\u00eancia',
                name: 'end_validity',
                xtype: 'datefield'
            },
            {
                xtype: 'rest-autocompletefield',
                fieldLabel: 'Publicação',
                allowBlank: true,
                rest: 'rh.publicacao.Restful',
                name: 'publication_restructuring'
            },
        ];
    },

    getPanelInformationDataPanel: function(cfg_window, cfg) {
        if(!this._informationDataPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Informações',
                    frame: true,
                    border: false,
                    layout: 'form',
                    defaults: {
                        width: 400
                    },
                    items: [
                    ]
                }
            );
            this._informationDataPanel = Ext._create('Ext.Panel', cfg);
        }
        return this._informationDataPanel;
    },

    getPanelText: function(cfg_window, cfg) {
        if(!this._textPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Texto',
                    frame: true,
                    border: false,
                    layout: 'form',
                    items: [
                        {
                            allowBlank: true,
                            hideLabel: true,
                            height: 180,
                            name: 'descricao',
                            xtype: 'ckeditor'
                        }
                    ]
                }
            );
            this._textPanel = Ext._create('Ext.Panel', cfg);
        }
        return this._textPanel;
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.movimentacao.teletrabalho.meta.Grid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.movimentacao.teletrabalho.meta.Window',

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    {header: 'Descricao', dataIndex: 'unicode', id: 'autoExpandColumn', hidden: true},
                    {header: 'Data Início', dataIndex: 'data_inicio', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Data Fim', dataIndex: 'data_fim', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                    {header: 'Descrição', dataIndex: 'descricao', width: 90,hidden: true},
                    {header: 'Meta', dataIndex: 'meta', width: 90},
                    {header: 'Periodicidade', dataIndex: 'periodicity_display', width: 90},
                    {header: 'Ativo', dataIndex: 'active', width: 50, renderer: toolkit.util.formatIconYesNo},
                    //{header: 'Movimentação Teletrabalho', dataIndex: 'mov_teletrabalho_unicode', width: 120}
                ]
            );

        return this._columnModel;
    },
    removeItems: function(record, cfg) {
        /*
        * Com variavel cfg é possivel adicionar callbacks que podem ser
        * executados antes ou depois da confirmação de exclusão de algum item do grid.
        * As propriedades que denotam os callbacks são: beforeConfirm e afterConfirm.
        */

        cfg = cfg || {};

        Ext.applyIf(
            cfg,
            {
                afterSuccess: Ext.emptyFn
            }
        );

        if(!this.allowRemove)
            return;


        var selection,
            rest = (cfg.rest ? Ext._create(cfg.rest) : this.factoryRestful());

        if(!(record instanceof Ext.Button))
            selection = [record];
        else
            selection = this.getSelectionModel().getSelections();

        if(selection.length === 0) {
            Ext.Msg.show({
                title: 'Inativando',
                icon: Ext.Msg.ERROR,
                buttons: Ext.Msg.OK,
                msg: 'Não foi selecionado nenhum item para inativação.'
            });
        }
        else {
            var message = 'Deseja inativar o item selecionado?',
                args = [
                    selection[0].get('pk'),
                    {
                        externalCallback: {
                            success: {
                                fn: function(instance) {
                                    if(this.driver === 'restful') this.getStore().reload();
                                    (this.afterSuccess || Ext.emptyFn)();
                                    this.fireEvent('removedItemGrid', instance);
                                },
                                scope: this
                            },
                            failure: {
                                scope: this,
                                fn: function() {
                                    if(this.driver === 'restful') this.getStore().reload();
                                    this.fireEvent('failureGrid', 'remove');
                                }
                            }
                        }
                    },
                    {
                        el: this.getEl(),
                        msg: 'Inativando item.'
                    }
                ];


            var grid = this;

            Ext.Msg.confirm('Alerta', message,
                function(btn)
                {
                    if(btn == 'yes')
                    {
                        if(cfg.beforeConfirm)
                            cfg.beforeConfirm(grid);

                        if(grid.driver === 'restful')
                            rest.remove.apply(rest, args);
                        else if (grid.driver === 'local' )
                        {
                            Ext.each(selection, function(item){
                                grid.getStore().remove(item);
                            });
                        }
                        else
                            Ext.emptyFn();

                        if(cfg.afterConfirm)
                            cfg.afterConfirm(grid);
                    }
                }
            );
        }
    },

    filterSituation: function () {
        var values = [];
        this.getSituationItemsMenu().forEach(
            function (item) {
                if (item.checked)
                    values.push(item.value);
            }
        );
        this.setFilterProperty('active__in', values, 1000);
    },

    getSituationItemsMenu: function () {
        if (this._situationItems == undefined) {
            this._situationItems = [];
            for (var i = 0; i < this.situationMenuValue.length; i++) {
                if (this.situationMenuValue[i].name == 'active') {
                    this._situationItems.push(
                        new Ext.menu.CheckItem({
                            hideOnClick: false,
                            name: 'active',
                            groupMenu: 'situation',
                            text: 'ATIVO',
                            checked: this.situationMenuValue[i].checked,
                            value: true,
                            scope: this,
                            checkHandler: function (item, checked) {
                                this.filterSituation();
                            }
                        })
                    );
                }
                if (this.situationMenuValue[i].name == 'inactive') {
                    this._situationItems.push(
                        new Ext.menu.CheckItem({
                            hideOnClick: false,
                            name: 'inactive',
                            groupMenu: 'situation',
                            text: 'INATIVO',
                            checked: this.situationMenuValue[i].checked,
                            value: false,
                            scope: this,
                            checkHandler: function (item, checked) {
                                this.filterSituation();
                            }
                        })
                    );
                }

            }
        }
        return this._situationItems;
    },

    getFilterMenu: function () {
        return [
            {
                name: 'situation',
                groupMenu: '',
                text: 'Situação',
                scope: this,
                menu: this.getSituationItemsMenu()
            },
        ];
    },

    __setFilterPropertyDefault: function (gridAutoLoad) {
        var filterSituationMenuValue = [];
        this.situationMenuValue.forEach(
            function (item) {

                if (item.checked) {
                    filterSituationMenuValue.push(item.value);
                }
            }
        );

        if (filterSituationMenuValue.length > 0) {
            this.setFilterProperty('active__in', filterSituationMenuValue, 1000, false);
        }
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(cfg, {
            gridAutoLoad: true,
            situationMenuValue: [
                {
                    name: 'active',
                    checked: true,
                    value: true,
                },
                {
                    name: 'inactive',
                    checked: false,
                    value: false,
                },
            ],
        });

        this.situationMenuValue = cfg.situationMenuValue;

        rh.movimentacao.teletrabalho.meta.Grid.superclass.constructor.call(this, cfg);

        this.__setFilterPropertyDefault(this.gridAutoLoad);
    },

});

core.RestfulGrid.register(
    'rh.movimentacao.teletrabalho.meta.Restful',
    'rh.movimentacao.teletrabalho.meta.Grid'
);


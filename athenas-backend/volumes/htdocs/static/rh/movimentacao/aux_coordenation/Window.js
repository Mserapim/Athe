/** autogenerated **/
/**
 *
 **/
 Ext._define('rh.movimentacao.aux_coordenation.Window', {
    extend: 'rh.movimentacao.pessoal.Window',

    rest: 'rh.movimentacao.aux_coordenation.Restful',

    width: 550,
    height: 470,

    constructor: function(cfg) {
        rh.movimentacao.aux_coordenation.Window.superclass.constructor.call(this, cfg);
    },

    getButtons: function(cfg) {
        if(!this._buttons)
            this._buttons = [

                {
                    text: 'Fechar',
                    scope: this,
                    handler: function() {
                        this.close();
                    }
                }
            ];

        return this._buttons;
    },

    getPanelInformationItems: function(cfg_window){
        var items = rh.movimentacao.aux_coordenation.Window.superclass.getPanelInformationItems.call(this, cfg_window);
        items.push(this.getDesignationField());
        items.push({
            xtype: 'choicefield',
            fieldLabel: 'Núcleo',
            allowBlank: true,
            hiddenName: 'nucleo',
            choiceId: 'rh.NUCLEO_CHOICES',
            readOnly: true,
        });
        items.push(Ext._create('core.fields.AutocompleteField', {
            fieldLabel: "Substituto",
            allowBlank: true,
            rest: "rh.employee.Restful",
            name: "substituto",
            readOnly: true,
        }));
        items.push(Ext._create('core.fields.AutocompleteField', {
            fieldLabel: "Publicação",
            allowBlank: false,
            rest: "rh.publicacao.Restful",
            name: "publicacao",
            readOnly: true,
        }));
        items.push({
            allowBlank: true,
            fieldLabel: "Data Inicio *",
            name: "data_inicio",
            xtype: "datefield",
            readOnly: true,
        });
        items.push({
            allowBlank: true,
            fieldLabel: "Data Fim",
            name: "data_fim",
            xtype: "datefield",
            readOnly: true,
        });
        items.push({
            fieldLabel: "GEDOC",
            name: "gedoc",
            xtype: "textfield",
            allowBlank: true,
            readOnly: true,
        });
        return items;
    },

    getDesignationField: function (cfg) {
        if (!this._designationField) {
            this._designationField = Ext._create('core.fields.AutocompleteField', {
                name: 'servidor_designacao',
                fieldLabel: 'Designação para gratificação *',
                allowBlank: false,
                rest: 'rh.employee.workplace.Restful',
                readOnly: true,
            });
        }

        return this._designationField;
    },

    getTabPanel: function(cfg_window, cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(
            cfg,
            {
                height: 400,
            }
        );
        return rh.movimentacao.aux_coordenation.Window.superclass.getTabPanel.call(this, {}, cfg);
    },

    getJobPosition: function(cfg_window, cfg) {
        if(!this._jobPositionField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg, 
            );
            this._jobPositionField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._jobPositionField;
    },
});
/** autogenerated **/
Ext._define('rh.movimentacao.possession.benefit.suspensionbenefit.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getSuspensionBenefitGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.movimentacao.possession.benefit.suspensionbenefit.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Suspensão de Benefício'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getSuspensionBenefitGrid(),
                ]
            }
        );

        rh.movimentacao.possession.benefit.suspensionbenefit.Manage.superclass.constructor.call(this, cfg);
    }
});


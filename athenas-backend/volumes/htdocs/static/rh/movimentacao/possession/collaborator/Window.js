/** autogenerated **/
/**
 *
 **/
Ext._define("rh.movimentacao.possession.collaborator.Window", {
    extend: "rh.movimentacao.possession.Window",

    rest: "rh.movimentacao.possession.collaborator.Restful",

    height: 500,

    getPanelInformationItems: function (cfg_window) {
        var items = rh.movimentacao.possession.collaborator.Window.superclass.getPanelInformationItems.call(
            this,
            cfg_window
        );
        rh.util.itemRemove(items, "servidor");
        rh.util.itemRemove(items, "public_concurrence");
        rh.util.itemRemove(items, "bond");
        rh.util.itemRemove(items, "publication_possession");
        rh.util.itemRemove(items, "publication_exercise");
        rh.util.itemRemove(items, "financial_effect_date");
        rh.util.itemRemove(items, "data_exercicio");
        rh.util.itemRemove(items, "aid_moving_house_paymente_date");
        rh.util.itemRemove(items, "aid_moving_house_gedoc");
        rh.util.itemRemove(items, "data_inicio_instancia");
        items.push(this.getNumberGedocField(cfg_window, {}));
        items.push(this.getNumberInsuranceField(cfg_window, {}));
        return items;
    },

    getNumberGedocField: function(cfg_window, cfg){
        if(!this._numberGedocField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    fieldLabel: "Número GEDOC",
                    allowBlank: true,
                    allowDecimals: false,
                    name: "number_gedoc",
                }
            );
            Ext.apply(cfg, {name: "number_gedoc"});
            this._numberGedocField = Ext._create('Ext.form.NumberField', cfg);
        }
        return this._numberGedocField;
    },

    getNumberInsuranceField: function(cfg_window, cfg){
        if(!this._numberInsuranceField){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    fieldLabel: "Número de Seguro",
                    allowBlank: true,
                    allowDecimals: false,
                    name: "number_insurance",
                }
            );
            Ext.apply(cfg, {name: "number_insurance"});
            this._numberInsuranceField = Ext._create('Ext.form.NumberField', cfg);
        }
        return this._numberInsuranceField;
    },
});

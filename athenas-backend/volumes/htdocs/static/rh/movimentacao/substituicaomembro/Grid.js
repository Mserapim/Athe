/** autogenerated **/
/**
 *
 **/
Ext._define('rh.movimentacao.substituicaomembro.Grid', {
    extend: 'rh.movimentacao.substituicao.Grid',

    restWindow: 'rh.movimentacao.substituicaomembro.Window',

    constructor: function(cfg) {
        rh.movimentacao.substituicaomembro.Grid.superclass.constructor.call(this, cfg);
    },

    getColumnModelItems: function(){
        if(!this._columnModelItems){
            this._columnModelItems = rh.movimentacao.substituicaomembro.Grid.superclass.getColumnModelItems.call(this, {});
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'publicacao_movimentacao_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'posse_cargo_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'designation_substituted_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'servidor_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'situation_unicode');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'data_inicio');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'data_prevista');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'data_fim');
            this._columnModelItems = this.itemRemove(this._columnModelItems, 'publicacao_fim_unicode');
            this._columnModelItems = this._columnModelItems.concat([
                {header: 'Posse', dataIndex: 'posse_cargo_unicode', width: 120, hidden: true},
                {header: 'Designação', dataIndex: 'designation_substituted_unicode', width: 250},
                {header: 'Substituto', dataIndex: 'servidor_unicode', width: 200, id: 'autoExpandColumn'},
                {header: 'Publicação', dataIndex: 'publicacao_movimentacao_unicode', width: 120},
                {header: 'Situação', dataIndex: 'situation_unicode', width: 80},
                {header: 'Substituto automático', dataIndex: 'automatic_substitute', width: 90, hidden: true},
                {header: 'Início', dataIndex: 'data_inicio', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                {header: 'Prevista Fim', dataIndex: 'data_prevista', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y'), hidden: true},
                {header: 'Fim', dataIndex: 'data_fim', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                {header: 'Publicação Fim', dataIndex: 'publicacao_fim_unicode', width: 120, hidden: true},
            ]);
        }
        return this._columnModelItems;
    }

});

core.RestfulGrid.register(
    'rh.movimentacao.substituicaomembro.Restful',
    'rh.movimentacao.substituicaomembro.Grid'
);


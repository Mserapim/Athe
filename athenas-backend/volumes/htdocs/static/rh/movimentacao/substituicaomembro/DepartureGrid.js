/** autogenerated **/
/**
 *
 **/
Ext._define('rh.movimentacao.substituicaomembro.DepartureGrid', {
    extend: 'rh.movimentacao.substituicaomembro.Grid',

    restWindow: 'rh.movimentacao.substituicaomembro.DepartureWindow',

    height: 260,

    constructor: function(cfg) {
        rh.movimentacao.substituicaomembro.DepartureGrid.superclass.constructor.call(this, cfg);
        this.departure = cfg.departure;
        this.employee = cfg.employee;
        this.employee_registry = cfg.employee_registry;
    },

    createItem: function(values) {
        values = {
            afastamento: this.departure,
            servidor_substituido: this.employee,
            employee_registry: this.employee_registry,
        };
        rh.movimentacao.substituicaomembro.DepartureGrid.superclass.createItem.call(this, values);
    },

    getColumnModelItems: function(){
        if(!this._columnModelItems){
            this._columnModelItems = rh.movimentacao.substituicaomembro.DepartureGrid.superclass.getColumnModelItems.call(this, {});
            this.setItemsConfig(this._columnModelItems, [
                {header: 'Afastamento', dataIndex: 'afastamento_unicode', width: 120, hidden: true},
                {header: 'Substituído', dataIndex: 'servidor_substituido_unicode', width: 200, hidden: true},
                {header: 'Motivo', dataIndex: 'departure_reason_unicode', width: 120, hidden: true},
                {header: 'Posse', dataIndex: 'posse_cargo_unicode', width: 120, hidden: true},
                {header: 'Designação', dataIndex: 'designation_substituted_unicode', width: 170},
                {header: 'Substituto automático', dataIndex: 'automatic_substitute', width: 90, hidden: true},
                {header: 'Publicação', dataIndex: 'publicacao_movimentacao_unicode', width: 120, hidden: true},
                {header: 'Início', dataIndex: 'data_inicio', width: 90, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                {header: 'Prevista Fim', dataIndex: 'data_prevista', width: 70, renderer: Ext.util.Format.dateRenderer('d/m/Y'), hidden: true},
                {header: 'Fim', dataIndex: 'data_fim', width: 70, renderer: Ext.util.Format.dateRenderer('d/m/Y')},
                {header: 'Publicação Fim', dataIndex: 'publicacao_fim_unicode', width: 100, hidden: true},
            ]);
        }
        return this._columnModelItems;
    }

});

core.RestfulGrid.register(
    'rh.movimentacao.substituicaomembro.Restful',
    'rh.movimentacao.substituicaomembro.DepartureGrid'
);


/** autogenerated **/
Ext._define('rh.teletrabalho.mark_telework.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getMarkTeleworkGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.teletrabalho.mark_telework.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Metas do Teletrabalho'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getMarkTeleworkGrid(),
                ]
            }
        );

        rh.teletrabalho.mark_telework.Manage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
Ext._define('rh.teletrabalho.config_periodo_envio_analisado.Manage', {
    extend: 'toolkit.widget.TabPanel',


    getPeriodoEnvioRelatorioSemestralGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.teletrabalho.config_periodo_envio_analisado.Grid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Configuração de Período de Envio de Relatorios Semestrais de Teletrabalho'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getPeriodoEnvioRelatorioSemestralGrid(),
                ]
            }
        );

        rh.teletrabalho.config_periodo_envio_analisado.Manage.superclass.constructor.call(this, cfg);
    }
});


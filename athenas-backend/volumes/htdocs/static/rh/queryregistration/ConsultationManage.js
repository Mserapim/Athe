/** autogenerated **/
Ext._define('rh.queryregistration.ConsultationManage', {
    extend: 'toolkit.widget.TabPanel',

    getConsultationGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('rh.queryregistration.ConsultationGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Consultas'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getConsultationGrid(),
                ]
            }
        );

        rh.queryregistration.ConsultationManage.superclass.constructor.call(this, cfg);
    }
});


/** autogenerated **/
/**
 *
 **/
Ext._define('rh.documento.DocumentoGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.documento.DocumentoWindow',

    getColumnModel: function () {
        if (!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    { header: 'Chave', dataIndex: 'pk', width: 60 },
                    { header: 'Tipo de Documento', dataIndex: 'tipo_documento_display', id: 'autoExpandColumn' },
                    { header: 'Número', dataIndex: 'numero', width: 90 },
                    { header: 'Validade', dataIndex: 'data_validade', width: 70, renderer: Ext.util.Format.dateRenderer('d/m/Y') },
                    { header: 'Dt. Expedição', dataIndex: 'data_expedicao', width: 70, renderer: Ext.util.Format.dateRenderer('d/m/Y') },
                    { header: 'Est. Expedição', dataIndex: 'estado_expedicao_unicode', width: 80 },
                    { header: 'Arquivo', dataIndex: 'arquivo_unicode', width: 50 },
                    { header: 'Órgão de Classe', dataIndex: 'class_organ', width: 50, hidden: true },
                ]
            );

        return this._columnModel;
    },

    updateItem: function (record) {
        if (record instanceof Ext.Button)
            record = undefined;

        var selected = core.nullValue(record, this.getSelectionModel().getSelected());

        if (this.canEditCpfRg === false && (selected.data.tipo_documento == 13 || selected.data.tipo_documento == 14)) {
            var message = 'Utilize os campos em pessoa física para alterar ' + selected.data.tipo_documento_display + '.';
            if (this.messageToUser != undefined)
                message = this.messageToUser + ' para alterar ' + selected.data.tipo_documento_display + '.';

            Ext.Msg.show({
                title: 'Aviso',
                msg: message,
                buttons: Ext.Msg.OK,
                icon: Ext.MessageBox.WARNING
            });
            return;
        }

        rh.documento.DocumentoGrid.superclass.updateItem.call(this, record);
    },
});

core.RestfulGrid.register(
    'rh.documento.DocumentoRestful',
    'rh.documento.DocumentoGrid'
);

/** autogenerated **/
/**
 *
 **/
Ext._define('rh.hoursworkcontract.employeeworkload.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.hoursworkcontract.employeeworkload.Restful',

    width: 500,

    constructor: function(cfg) {
        rh.hoursworkcontract.employeeworkload.Window.superclass.constructor.call(this, cfg);
        this._observe();
    },

    _observe: function() {
        var values = this.getParams();
        if(values.hours_work_contract_workload != undefined)
            this.getHoursWorkload().setReadOnly(true);
        else
            this.getHoursWorkload().setReadOnly(false);
    },

    getFormPanel: function(cfg) {
        if(!this._formPanel)
            this._formPanel = Ext._create('Ext.form.FormPanel', {
                border: false,
                frame: true,
                items: [
                    {
                        xtype: 'rest-autocompletefield',
                        fieldLabel: 'Servidor',
                        allowBlank: true,
                        rest: 'rh.employee.Restful',
                        name: 'employee'
                    },
                    this.getHoursWorkload({}, {}),
                    {
                        allowBlank: false,
                        fieldLabel: 'In\u00edcio de Vigência',
                        name: 'date_start',
                        xtype: 'datefield',
                        width: 365
                    },
                    {
                        allowBlank: true,
                        fieldLabel: 'Fim de Vigência',
                        name: 'date_end',
                        xtype: 'datefield',
                        width: 365
                    },
                ]
            });
        return this._formPanel;
    },

    getHoursWorkload: function(cfg_window, cfg){
        if(!this._hoursWorkloadField){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    name: 'hours_work_contract_workload',
                    fieldLabel: 'Horário',
                    allowBlank: false,
                    rest: 'rh.hoursworkcontract.workload.Restful',
                    readOnly: false,
                }
            );
            this._hoursWorkloadField = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._hoursWorkloadField;
    },
});


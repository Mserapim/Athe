/** autogenerated **/
/**
 *
 **/
Ext._define('rh.hoursworkcontract.employeeworkload.Restful', {
    extend: 'core.Restful',

    resource: 'RHEmployeeHoursWorkContractWorkload',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = rh.hoursworkcontract.employeeworkload.Restful.superclass.getFields.call(this, cfg).concat([
                {type: 'date', name: 'created_at', dateFormat: 'd/m/Y H:i'},
                {type: 'int', name: 'created_by', useNull: true},
                {type: 'string', name: 'created_by_unicode'},
                {type: 'int', name: 'modified_by', useNull: true},
                {type: 'string', name: 'modified_by_unicode'},
                {type: 'date', name: 'modified_at', dateFormat: 'd/m/Y H:i'},
                {type: 'int', name: 'employee'},
                {type: 'string', name: 'employee_unicode'},
                {type: 'int', name: 'hours_work_contract_workload'},
                {type: 'string', name: 'hours_work_contract_workload_unicode'},
                {type: 'date', name: 'date_start', dateFormat: 'd/m/Y'},
                {type: 'date', name: 'date_end', dateFormat: 'd/m/Y'},
                {type: 'bool', name: 'active'},
            ]);
        return this._fields;
    },

    applyEmployeeWorkplace: function(params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function(message) {
                Ext.Msg.show({
                    title: 'Aplicando Escala',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            'apply_employee_workload',
            null,
            'PUT',
            {
                params: params,
                success: function(xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if(rst.success)
                        core.invokeCallback((cbSuccess || {fn: Ext.emptyFn}), rst.message);
                    else
                        core.invokeCallback((emptyFailure || {fn: Ext.emptyFn}), rst.message);
                },
                failure: function(xhr) {
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function() {
                    core.invokeCallback((cbCallback || {fn: Ext.emptyFn}));
                }
            }
        ));
    },

    removeByDateStart: function(params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function(message) {
                Ext.Msg.show({
                    title: 'Aplicando Escala',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            'remove_by_date_start',
            null,
            'POST',
            {
                params: params,
                success: function(xhr) {
                    var rst = Ext.decode(xhr.responseText);

                    if(rst.success)
                        core.invokeCallback((cbSuccess || {fn: Ext.emptyFn}), rst.message);
                    else
                        core.invokeCallback((emptyFailure || {fn: Ext.emptyFn}), rst.message);
                },
                failure: function(xhr) {
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function() {
                    core.invokeCallback((cbCallback || {fn: Ext.emptyFn}));
                }
            }
        ));
    },
});

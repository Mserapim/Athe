/** autogenerated **/
/**
 *
 **/
Ext._define('rh.hoursworkcontract.workload.manager.Window', {
    extend: 'rh.hoursworkcontract.workload.Window',

    rest: 'rh.hoursworkcontract.workload.Restful',

    width: 800,

    height: 515,

    tabPanelHeight: 450,

    constructor: function(cfg) {
        rh.hoursworkcontract.workload.manager.Window.superclass.constructor.call(this, cfg);
    },

    _observe: function() {
        if(this.oId){
            this.getWorkloadGrid().setParam('hours_work_contract_workload', this.oId);
            this.getWorkloadGrid().setFilterProperty('hours_work_contract_workload__pk', this.oId);
            this.getPanelEmployee().setDisabled(false);
        }
        else{
            this.getWorkloadGrid().setParam('hours_work_contract_workload', undefined);
            this.getWorkloadGrid().setFilterProperty('hours_work_contract_workload__pk', 0);
            this.getPanelEmployee().setDisabled(true);
        }
    },

    getTabPanelItems: function(cfg_window){
        return [
            this.getPanelInformation(cfg_window, {}),
            this.getPanelEmployee(cfg_window, {})
        ];
    },

    getPanelEmployee: function(cfg_window, cfg) {
        if(!this._employeePanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Servidores da Escala',
                    frame: true,
                    border: false,
                    layout: 'form',
                    width: 60
                }
            );
            Ext.apply(cfg, {items: this.getPanelEmployeeItems(cfg_window),});
            this._employeePanel = Ext._create('Ext.Panel', cfg);
        }
        return this._employeePanel;
    },

    getPanelEmployeeItems: function(){return [this.getWorkloadGrid()];},

    getWorkloadGrid: function() {
        if(!this._cworkloadGrid) {
            this._cworkloadGrid = Ext._create('rh.hoursworkcontract.employeeworkload.specialize.Grid', {
                region: 'center',
                height: 415
            });
        }
        return this._cworkloadGrid;
    },

});
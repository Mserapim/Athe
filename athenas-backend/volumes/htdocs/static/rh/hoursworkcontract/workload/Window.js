/** autogenerated **/
/**
 *
 **/
Ext._define('rh.hoursworkcontract.workload.Window', {
    extend: 'core.RestfulWindow',

    rest: 'rh.hoursworkcontract.workload.Restful',

    width: 540,

    height: 380,

    tabPanelHeight: 380,

    constructor: function(cfg) {
        rh.hoursworkcontract.workload.Window.superclass.constructor.call(this, cfg);
        this._observe();
        this.observeTimeField();
    },

    _observe: function() {},

    getFormPanel: function(cfg_window, cfg) {
        if(!this._formPanel){
            cfg = core.nullValue(cfg, {});
            Ext.apply(cfg, {border: false, items: [this.getTabPanel(cfg_window, {})]});
            this._formPanel = Ext._create('Ext.form.FormPanel', cfg);
        }
        return this._formPanel;
    },

    getTabPanel: function(cfg_window, cfg) {
        if(!this._tabPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(cfg, {border: false, activeTab: 0, height: this.tabPanelHeight,});
            Ext.apply(cfg, {items: [this.getTabPanelItems(cfg_window)]});
            this._tabPanel = Ext._create('Ext.TabPanel', cfg);
        }
        return this._tabPanel;
    },

    getTabPanelItems: function(cfg_window){
        return [
            this.getPanelInformation(cfg_window, {}),
        ];
    },

    getPanelInformation: function(cfg_window, cfg) {
        if(!this._informationPanel){
            cfg = core.nullValue(cfg, {});
            Ext.applyIf(
                cfg,
                {
                    title: 'Dados',
                    frame: true,
                    border: false,
                    layout: 'form',
                    defaults: {
                        width: 530
                    }
                }
            );
            Ext.apply(cfg, {items: [this.getPanelInformationItems(cfg_window)]});
            this._informationPanel = Ext._create('Ext.Panel', cfg);
        }
        return this._informationPanel;
    },

    getPanelInformationItems: function(cfg) {
        return [
            {
                maxLength: 100,
                allowBlank: false,
                fieldLabel: 'T\u00edtulo',
                name: 'title',
                xtype: 'textfield',
                width: 530
            },
            {
                maxLength: 400,
                allowBlank: true,
                fieldLabel: 'Texto',
                name: 'text',
                xtype: 'textfield',
                width: 530
            },
            this.getDay1Field(),
            this.getDay2Field(),
            this.getDay3Field(),
            this.getDay4Field(),
            this.getDay5Field(),
            this.getDay6Field(),
            this.getDay7Field(),
            this.getDurationField()
        ];
    },

    observeTimeField: function(){
        var duration = 0;
        if(this.getDay1Field().getComboField().getValue() && this.getDay1Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay1Field().getComboField().getStore().data.items[0].data.duration);
        if(this.getDay2Field().getComboField().getValue() && this.getDay2Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay2Field().getComboField().getStore().data.items[0].data.duration);
        if(this.getDay3Field().getComboField().getValue() && this.getDay3Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay3Field().getComboField().getStore().data.items[0].data.duration);
        if(this.getDay4Field().getComboField().getValue() && this.getDay4Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay4Field().getComboField().getStore().data.items[0].data.duration);
        if(this.getDay5Field().getComboField().getValue() && this.getDay5Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay5Field().getComboField().getStore().data.items[0].data.duration);
        if(this.getDay6Field().getComboField().getValue() && this.getDay6Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay6Field().getComboField().getStore().data.items[0].data.duration);
        if(this.getDay7Field().getComboField().getValue() && this.getDay7Field().getComboField().getStore().data.items[0])
            duration += parseInt(this.getDay7Field().getComboField().getStore().data.items[0].data.duration);

        this.getDurationField().setValue(toolkit.util.extractMinuteFormat(duration));
    },

    getListenersAutocomplete: function(){
        return {
            scope: this,
            blur: function(fld, newValue, oldValue) {
                this.observeTimeField();
            },
            select: function(combo, record, index){
                this.observeTimeField();
            },
        };
    },

    getDay1Field: function(cfg){
        if(!this._day1Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Domingo',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_1',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day1Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day1Field;
    },

    getDay2Field: function(cfg){
        if(!this._day2Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Segunda',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_2',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day2Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day2Field;
    },

    getDay3Field: function(cfg){
        if(!this._day3Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Terça',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_3',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day3Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day3Field;
    },

    getDay4Field: function(cfg){
        if(!this._day4Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Quarta',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_4',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day4Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day4Field;
    },

    getDay5Field: function(cfg){
        if(!this._day5Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Quinta',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_5',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day5Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day5Field;
    },

    getDay6Field: function(cfg){
        if(!this._day6Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Sexta',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_6',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day6Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day6Field;
    },

    getDay7Field: function(cfg){
        if(!this._day7Field){
            cfg = core.nullValue(cfg, {});
            Ext.apply(
                cfg,
                {
                    fieldLabel: 'Sábado',
                    allowBlank: true,
                    rest: 'rh.hoursworkcontract.Restful',
                    name: 'day_7',
                    comboListeners: this.getListenersAutocomplete()
                }
            );
            this._day7Field = Ext._create('core.fields.AutocompleteField', cfg);
        }
        return this._day7Field;
    },

    getDurationField: function () {
        if (!this._durationField) {
            this._durationField = Ext._create('Ext.form.TextField', {
                allowBlank: false,
                fieldLabel: 'Dura\u00e7\u00e3o da Jornada',
                name: 'durationshow',
                width: 530,
                readOnly: true,
            });
        }
        return this._durationField;
    },


});
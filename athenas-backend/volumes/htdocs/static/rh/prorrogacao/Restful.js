/** autogenerated **/
/**
 *
 **/
Ext._define('rh.prorrogacao.Restful', {
    extend: 'core.Restful',

    resource: 'RHProrrogacaoRestful',

    getFields: function(cfg) {
        if(!this._fields)
            this._fields = rh.prorrogacao.Restful.superclass.getFields.call(this, cfg).concat([
                {type: "date",  name: "data_inicio",  dateFormat: "d/m/Y"},
                {type: "int",  name: "publicacao",  useNull: true},
                {type: "string",  name: "publicacao_unicode"},
                {type: "date",  name: "data_fim",  dateFormat: "d/m/Y"}
            ]);

        return this._fields;
    },
    _validate_departure_period: function (params, cbSuccess, cbFailure, cbCallback) {
        var emptyFailure = {
            fn: function (message) {
                Ext.Msg.show({
                    title: 'Processando',
                    icon: Ext.Msg.WARNING,
                    buttons: Ext.Msg.OK,
                    msg: message
                });
            }
        };

        this.doRequest(this.getRoute(
            'validate_departure_period',
            null,
            'POST',
            {
                params: params,
                success: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    if (rst.success) {
                        core.invokeCallback((cbSuccess || { fn: Ext.emptyFn }), rst.message);
                    }
                    else
                        core.invokeCallback((emptyFailure || { fn: Ext.emptyFn }), rst.message);
                },
                failure: function (xhr) {
                    var rst = Ext.decode(xhr.responseText);
                    core.invokeCallback((cbFailure || emptyFailure), 'Recurso indisponivel no momento.');
                },
                callback: function () {
                    core.invokeCallback((cbCallback || { fn: Ext.emptyFn }));
                }
            }
        ));
    },
});

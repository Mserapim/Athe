/** autogenerated **/
/**
 *
 **/
Ext._define('rh.pessoa.BankingDataPersonGrid', {
    extend: 'core.RestfulGrid',

    restWindow: 'rh.pessoa.BankingDataPersonWindow',
    rest: 'rh.pessoa.BankingDataPersonRestful',

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    {header: 'Chave', dataIndex: 'pk', width: 50},
                    {header: 'Agência com DV', dataIndex: 'agencia', width: 90},
                    {header: 'Conta Corrente com DV', dataIndex: 'conta_corrente_completa', width: 100},
                    {header: 'Tipo de Conta', dataIndex: 'tipo_conta_display', width: 90},
                    {header: 'Banco', dataIndex: 'banco_unicode', width: 120},
                    {header: 'Servidor', dataIndex: 'pessoa_display', id: 'autoExpandColumn'},
                    {header: 'Principal', dataIndex: 'principal', width: 80, renderer: function(value) { return (value ? 'Sim' : 'Não'); }}
                ]
            );

        return this._columnModel;
    },

    'getFilterMenu': function() {
        return [
            {
                'text': 'Por Tipo de Conta',
                'menu': [
                    {
                        'text': 'Corrente',
                        'checked': true,
                        'scope': this,
                        'hideOnClick': false,
                        'handler': function() { this.toggleTipoConta(1); }
                    },
                    {
                        'text': 'Poupança',
                        'checked': true,
                        'scope': this,
                        'hideOnClick': false,
                        'handler': function() { this.toggleTipoConta(2); }
                    },
                    {
                        'text': 'Investimento',
                        'checked': true,
                        'scope': this,
                        'hideOnClick': false,
                        'handler': function() { this.toggleTipoConta(3); }
                    }
                ]
            },
            '-',
            {
                'text': 'Por Banco',
                'scope': this,
                'handler': this.filterBanco
            }

        ];
    },

    'filterBanco': function() {
        Ext._create('core.GridSelectWindow', {
            'rest': 'rh.bank.BankRestful',
            'title': 'Selecione um banco para filtrar',
            'width': Ext.getBody().getBox().width * 0.9,
            'height': Ext.getBody().getBox().height * 0.9,
            'callback': {
                'scope': this,
                'fn': function(instance) {
                    if(instance)
                        this.setFilterProperty("banco", instance.get('pk'), 1003);
                    else
                        this.removeFilterProperty('banco', 1003);
                }
            }
        }).show();
    },

    'toggleTipoConta': function(tipo) {
        if(!this._filterTipoConta)
            this._filterTipoConta = [1, 2, 3];

        if(this._filterTipoConta.indexOf(tipo) >= 0)
            this._filterTipoConta.remove(tipo);
        else
            this._filterTipoConta.push(tipo);

        this.setFilterProperty('tipo_conta__in', this._filterTipoConta, 1000);
    },


});

core.RestfulGrid.register(
    'rh.pessoa.BankingDataPersonRestful',
    'rh.pessoa.BankingDataPersonGrid'
);


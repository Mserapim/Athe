/** autogenerated **/
Ext._define('esocial.manager.BatchEventManage', {
    extend: 'toolkit.widget.TabPanel',

    getBatchEventGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('esocial.manager.BatchEventGrid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'BatchEvent'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getBatchEventGrid(),
                ]
            }
        );

        esocial.manager.BatchEventManage.superclass.constructor.call(this, cfg);
    }
});


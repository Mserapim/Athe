/** autogenerated **/
Ext._define('esocial.itemtable.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getConfigurationGrid: function () {
        if (!this._grid) {
            this._grid = Ext._create('esocial.itemtable.ItemGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function (cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Item de tabela'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getConfigurationGrid(),
                ]
            }
        );

        esocial.itemtable.Manage.superclass.constructor.call(this, cfg);
    }
});


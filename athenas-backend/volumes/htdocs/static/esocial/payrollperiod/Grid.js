/** autogenerated **/
/**
 *
 **/
Ext._define('esocial.payrollperiod.Grid', {
    extend: 'core.RestfulGrid',

    restWindow: 'esocial.payrollperiod.Window',

    hideActions: ['remove', 'copy', 'edit'],

    hideItemsToolbar: ['remove', 'edit', 'add'],

    configOrderToolBar: ['analysis', '-', 'search', '->', 'reload', '-','download'],

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});
        Ext.applyIf(
            cfg,
            {
                gridAutoLoad: true,
                archivedHideMenu: true,
                archivedShowAllMenu: false,
                sentShowMenu: true,
                closedShowMenu: true,
                pendencyCacheShowMenu: true
            }
        );

        var gridAutoLoad = cfg.gridAutoLoad;
        cfg.gridAutoLoad = false;

        esocial.payrollperiod.Grid.superclass.constructor.call(this, cfg);
        this.gridAutoLoad = gridAutoLoad;
        this.canLoad = true;
        this.removeAllFilterProperty(this.gridAutoLoad);
    },

    __setFilterPropertyDefault: function (gridAutoLoad) {
        this.filterArchiveds(false, false);
        this.getSentMenu(false, false);
        this.getClosedMenu(false, false);
        this.getPendencyCacheMenu(false, false);

        this.runLoad(gridAutoLoad);
    },

    removeAllFilterProperty: function (load) {
        this.removeFilterProperty('archived', 1020, false);
        this.removeFilterProperty('sent', 1030, false);
        this.removeFilterProperty('closed', 1040, false);
        this.removeFilterProperty('pendency_cache', 1050, false);

        this.canLoad = false;

        if (this.archivedHideMenu == true)
            this.getArchivedHideMenu().setChecked(true);

        if (this.archivedShowAllMenu == true)
            this.getArchivedShowAllMenu().setChecked(true);

        if (this.sentShowMenu == true)
            this.getSentMenu().setChecked(true);

        if (this.closedShowMenu == true)
            this.getClosedMenu().setChecked(true);

        if (this.pendencyCacheShowMenu == true)
            this.getPendencyCacheMenu().setChecked(true);

        this.canLoad = true;

        this.__setFilterPropertyDefault(load);
    },

    runLoad: function(gridAutoLoad){
        if (gridAutoLoad) {
            var store = this.getStore();
            store.load({});
        }
    },

    getColumnModel: function() {
        if(!this._columnModel)
            this._columnModel = Ext._create(
                'Ext.grid.ColumnModel',
                [
                    Ext._create('Ext.grid.RowNumberer'),
                    {header: 'Cod', dataIndex: 'pk', width: 50, hidden: true},
                    { header: '', dataIndex: 'icons', width: 85, menuDisabled: true, renderer: toolkit.util.formatStatus },
                    {header: 'Período', dataIndex: 'unicode', id: 'autoExpandColumn'},
                    {header: 'Mês', dataIndex: 'month', width: 90},
                    {header: 'Ano', dataIndex: 'year', width: 90},
                    {header: 'S1298', dataIndex: 's1298_unicode', width: 180, hidden: true},
                    {header: 'S1299', dataIndex: 's1299_unicode', width: 180, hidden: true},
                    {header: 'Enviado', dataIndex: 'sent', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }, hidden: true},
                    {header: 'Fechado', dataIndex: 'closed', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }, hidden: true},
                    {header: 'Pendência', dataIndex: 'pendency_cache', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }, hidden: true},
                    {header: 'Arquivado', dataIndex: 'archived', width: 90, renderer: function(value) { return (value ? 'SIM' : 'NÃO'); }, hidden: true},
                    {
                        header: 'Moficado em',
                        dataIndex: 'modified_at',
                        width: 120,
                        renderer: Ext.util.Format.dateRenderer('d/m/Y H:i'),
                        hidden: false,
                    },
                    { header: 'Moficado por', dataIndex: 'modified_by_unicode', width: 120, hidden: true },
                    {
                        header: 'Criado em',
                        dataIndex: 'created_at',
                        width: 90,
                        renderer: Ext.util.Format.dateRenderer('d/m/Y H:i'),
                        hidden: true,
                    },
                    { header: 'Criado por', dataIndex: 'created_by_unicode', width: 120, hidden: true },
                ]
            );

        return this._columnModel;
    },

    getFilterMenu: function () {
        return [
            {
                text: 'Limpar filtro',
                scope: this,
                handler: function (params) {
                    this.removeAllFilterProperty(true);
                }
            },
            '-',
            this.getSentMenu(),
            '-',
            this.getClosedMenu(),
            '-',
            this.getPendencyCacheMenu(),
            '-',
            this.getArchivedHideMenu(),
            this.getArchivedShowAllMenu(),
        ];
    },

    getArchivedHideMenu: function(){
        if (!this._archivedHideMenu)
            this._archivedHideMenu = Ext._create('Ext.menu.CheckItem', {
                text: 'Ocultar arquivados',
                checked: this.archivedHideMenu,
                group: 'archivedE',
                scope: this,
                hideOnClick: false,
                handler: function(chk, checked) {
                    this.filterArchiveds(false, true);
                }
            });
        return this._archivedHideMenu;
    },

    getArchivedShowAllMenu: function(){
        if (!this._archivedShowAllMenu)
            this._archivedShowAllMenu = Ext._create('Ext.menu.CheckItem', {
                text: 'Mostrar todos',
                checked: this.archivedShowAllMenu,
                group: 'archivedE',
                scope: this,
                hideOnClick: false,
                handler: function(chk, checked) {
                    this.filterArchiveds(null, true);
                }
            });
        return this._archivedShowAllMenu;
    },

    getSentMenu: function(){
        if (!this._sentMenu)
            this._sentMenu = Ext._create('Ext.menu.CheckItem', {
                text: 'Enviados',
                checked: this.sentShowMenu,
                scope: this,
                hideOnClick: false,
                handler: function(chk, checked) {
                    this.filterSent(false, true);
                }
            });
        return this._sentMenu;
    },

    getClosedMenu: function(){
        if (!this._closedMenu)
            this._closedMenu = Ext._create('Ext.menu.CheckItem', {
                text: 'Fechados',
                checked: this.closedShowMenu,
                scope: this,
                hideOnClick: false,
                handler: function(chk, checked) {
                    this.filterClosed(false, true);
                }
            });
        return this._closedMenu;
    },

    getPendencyCacheMenu: function(){
        if (!this._pendencyCacheMenu)
            this._pendencyCacheMenu = Ext._create('Ext.menu.CheckItem', {
                text: 'Com pendências',
                checked: this.pendencyCacheShowMenu,
                scope: this,
                hideOnClick: false,
                handler: function(chk, checked) {
                    this.filterPendencyCache(false, true);
                }
            });
        return this._pendencyCacheMenu;
    },

    filterArchiveds: function (archived, load) {
        if (this.canLoad){
            if(archived === null)
                this.removeFilterProperty('archived', 1020, load);
            else
                this.setFilterProperty('archived', archived, 1020, load);
        }
    },

    filterSent: function (sent, load) {
        if (this.canLoad){
            if(sent === null)
                this.removeFilterProperty('sent', 1030, load);
            else
                this.setFilterProperty('sent', sent, 1030, load);
        }
    },

    filterClosed: function (closed, load) {
        if (this.canLoad){
            if(closed === null)
                this.removeFilterProperty('closed', 1040, load);
            else
                this.setFilterProperty('closed', closed, 1040, load);
        }
    },

    filterPendencyCache: function (pendencyCache, load) {
        if (this.canLoad){
            if(pendencyCache === null)
                this.removeFilterProperty('pendency_cache', 1050, load);
            else
                this.setFilterProperty('pendency_cache', pendencyCache, 1050, load);
        }
    },

    getConfigActionsItems: function (cfg) {
        var menu = esocial.payrollperiod.Grid.superclass.getConfigActionsItems.call(this, cfg);
        menu['analysis'] = {
            text: 'Analisar período',
            iconCls: 'icon-esocial icon-run',
            handler: this.analysis,
            scope: this,
        };
        menu['reload'] = {
            text: 'Atualizar',
            enableToggle: true,
            toggleHandler: function (item, pressed) {
                if (pressed === true) {
                    Ext.TaskMgr.start(this.reloadGridAuto());
                } else {
                    Ext.TaskMgr.stop(this.reloadGridAuto());
                }
            },
            pressed: false,
            scope: this
        };
        return menu;
    },

    reloadGridAuto: function () {
        if (!this._reloadGridAuto) {
            this._reloadGridAuto = {
                scope: this,
                run: function () {
                    this.getStore().reload();
                },
                interval: 5 * 1000, //1 minute
            };
        }

        return this._reloadGridAuto;
    },

    analysis: function(){
        var periods = [];
        var selecteds = this.getSelectionModel().getSelections();
        var scope = this;

        if (selecteds.length > 0) {
            var i = 0, item;
            while ((item = selecteds[i++]))
                periods.push(item.id);
            this.executeAction(
                'analisys',
                {'payroll_period': periods},
                'Analisando períodos...',
                undefined,
                'esocial.payrollperiod.Restful'
            );
        }else{
            Ext.Msg.show({
                wait: true,
                scope: this,
                title: 'PERGUNTA',
                icon: Ext.Msg.QUESTION,
                buttons: Ext.Msg.OKCANCEL,
                style: 'background-color: yellow',
                msg: 'Deseja analisar os últimos 12 períodos?',
                fn: function (button) {
                    if (button == 'ok'){
                        scope.executeAction(
                            'analisys',
                            {'all_periods': true},
                            'Analisando períodos...',
                            undefined,
                            'esocial.payrollperiod.Restful'
                        );
                    }
                }
            });
        }
    },

    executeAction: function (action, params, msg, callback, rest, callbackSuccess) {
        if (rest == undefined) rest = this.rest;
        var rest = Ext._create(rest);
        var rMask = new Ext.LoadMask(this.ownerCt.getEl(), { msg: msg ? msg : 'Aguarde...' });
        rMask.show();

        var wnd = this;

        if(callbackSuccess === undefined)
            callbackSuccess = {
                scope: this,
                fn: function (request) {
                    rMask.hide();
                    if (request.message)
                        Ext.MessageBox.show({
                            title: 'Informando',
                            msg: request.message,
                            buttons: Ext.MessageBox.OK,
                            icon: Ext.MessageBox.INFO,
                        });
                    if (!request.success) {
                        Ext.MessageBox.show({
                            title: 'Erro',
                            msg: request.message,
                            buttons: Ext.MessageBox.OK,
                            icon: Ext.MessageBox.ERROR,
                        });
                    }
                },
            };

        rest.executeAction(
            action,
            params,
            callbackSuccess,
            {
                fn: function (message) {
                    Ext.Msg.show({
                        title: 'Informando',
                        icon: Ext.Msg.ERROR,
                        buttons: Ext.Msg.OK,
                        msg: message,
                    });
                },
            },
            {
                fn: function () {
                    rMask.hide();
                },
            }
        );
    },

});

core.RestfulGrid.register(
    'esocial.payrollperiod.Restful',
    'esocial.payrollperiod.Grid'
);


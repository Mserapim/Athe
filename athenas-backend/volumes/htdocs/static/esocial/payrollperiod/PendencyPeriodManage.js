/** autogenerated **/
Ext._define('esocial.payrollperiod.PendencyPeriodManage', {
    extend: 'toolkit.widget.TabPanel',

    getPendencyPeriodGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('esocial.payrollperiod.PendencyPeriodGrid', {
                region: 'center',
                gridAutoLoad: true
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Pendência do Período'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getPendencyPeriodGrid(),
                ]
            }
        );

        esocial.payrollperiod.PendencyPeriodManage.superclass.constructor.call(this, cfg);
    }
});

